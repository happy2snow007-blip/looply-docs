# looply 红布林订单对接 PRD — 修订记录

> 本文件独立维护 PRD 的版本修订记录，不并入 PRD 正文。
> 关联文档：`looply-红布林订单对接-PRD-v1.1.md`（现行）、`looply-红布林订单对接-PRD-v1.0.md`

| 版本 | 日期 | 修改人 | 修改内容 |
|---|---|---|---|
| v1.1 评论对齐 | 2026-07-21 | 晴天 | 按飞书 v4 评论批复对齐三处口径：①**取消触发约束**——红布林取消必须由 looply 订单先转取消状态才触发，不允许支付单 failed 直接调红布林取消（改 S3、§2.4 处理流程）；②**支付超时口径**——looply 支付超时改为"以订单支付模块最新规则为准（非 1 自然日，待核实）"（改 §2.4 触发条件）；③**红布林侧超时**——补记红布林侧超时自动取消 = 120H、由红布林控制、looply 知晓即可（改 §2.4 规则说明，原"时长是否一致"遗留已闭环）；④§2.1.1 收货信息补入评论 9 实证的两类类目路由真实数据（腕表/非腕表，同采购账号不同收货地址）。去掉 TR（术语表 + §3 依赖表）、清理原型 RHL_STATUS_TAG 未用的"已签收"死映射。 |
| v1.1 修订补充 | 2026-07-21 | 晴天 | 按定稿原型 v2 交叉校验对齐 §2.6 履约卡片展示、§2.7 退款弹窗字段。§2.6 由「字段区 + 明细表」改为「平铺卡片列表」（每张红布林订单一卡片，账号/收货信息随卡片各一份），去掉商品行 TR 列、补 国内承运商 字段；§2.7 弹窗头部由四项收窄为「订单号/买家」，红布林订单号与履约阶段移至退款商品表逐行展示，删除「退回物流单号」字段（线下退回结果记入备注），联动说明改为方案 A 按勾选商品跨阶段汇总；全文原型引用 v1→v2。 |
| v1.1 | 2026-07-21 | 晴天 | 落地一单一件（1:N）下单变更，推翻原 1:1 一单多件 + 方案 B 部分退；解除 3 个 TBD（processing 有 / 红布林订单挂父订单 / 阶段③运费系统不处理）；标注合并发货为前置依赖待红布林确认。 |
| v1.0 | 2026-07-21 | 晴天 | 首版。定义红布林订单对接的正向链路（建单锁库存 / 驱动发货 / 发货与国内签收回调）与逆向链路（待付款自动取消、运营发起退款含 4 阶段联动与部分退），含国内红布林履约信息展示、发起退款弹窗（列表页 + 详情页共用同一弹窗）。含 3 处待确认项（TBD-1 `processing` 存废 / TBD-2 父子订单层级 / TBD-3 阶段③运费承担），确认后出 v1.1。 |

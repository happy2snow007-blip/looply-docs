# Looply 类目管理 PRD 修订记录

| 版本 | 日期 | 修改人 | 修改内容 |
|---|---|---|---|
| v0.1 | 2026-06-04 | 产品团队 | 首次起草，基于分类页设计方案讨论稿（83条决策）、竞品分析、四份调研报告、后台原型v0.11、前台原型v0.3 生成 |
| v0.2 | 2026-06-04 | 产品团队 | 对齐商品系统PRD v0.8：①条件构建器 condition 字段枚举从 5 级（Like New/Excellent/Very Good/Good/Fair）改为 3 级（A/B/C）；②字段来源从 `used_item.condition_level` 改为 `used_item.grade`；③URL示例参数同步更新 |
| v0.3 | 2026-06-08 | 产品团队 | 新增 §2.10「品牌驱动 Collection（品牌页）」：复用现有 rule 引擎 brand 条件实现品牌页，URL 仍为 /collections/{brand-slug}，collection 表加 linked_brand_id 关联品牌管理，SEO 字段实时降级读品牌管理避免双写；新增"从品牌生成 Collection"入口；明确品牌页不做品类筛选及交叉词 MVP 取舍；补充 R-16~R-18 规则、品牌管理 SEO 字段依赖；更新 §1.2 品牌页条目 |
| v0.4 | 2026-06-08 | 产品团队 | 新增 R-19：商品一对多归属时 PDP 面包屑/schema 恒定取主归属 Collection（primary_collection，优先品类），不随来路变；商品管理依赖补 primary_collection 字段。详见 SEO 合集 §2.2.1 |
| v0.5 | 2026-06-08 | 产品团队 | 基于 SEO 实证修正字段表述：Collection 描述、brand_description 从"可索引前提/必填导语"降级为"可选增强（1–2 句、勿堆砌）"；页面可索引依赖足量商品+H1+面包屑，非导语。详见 SEO 合集 §4.2.1 |
| v0.2（合并版）| 2026-06-24 | 产品团队 | **前后台合一 + 重大重构**，独立文件 `looply-类目管理-PRD-v0.2.md`。合并「类目管理 PRD（后台）」与「Collection 落地页 PRD v1.6（前台 Mobile）」，对齐后台原型 v0.12。核心变更：①筛选体系重构为图文筛/快筛/通用筛选器三模块，通用筛选器零配置动态生成、快筛仅挑维度+排序（废弃 quick_filter_preset）、图文筛新增；②展示类型精简为 4 种（文字卡片/折叠面板/滑块/颜色卡片），同维度按筛选模块分别配置样式，前端按 display_type 渲染；③Collection 状态升级三态（draft/active/inactive）；④价格只支持滑块（$0~商品池最高价⌈百位⌉），价格带移 V2；⑤多市场/多渠道落地，价格币种跟随市场支持币种枚举；⑥值归一化与 size_system 高级能力精简（枚举值取商品模块仅调序）；⑦条件构建器扁平化（去嵌套分组）；⑧前台范围以 v1.6 为准（Mobile 优先，PC 端 V2）。数据模型相应调整（新增 market_id/channel_id、fc_visual_filter/fc_quick_filter，废弃 fc_filter_config、value_mapping、size_system 配置）。保留 Meta 降级链、商品准入与状态同步、前台收藏、品牌驱动 Collection 等机制章节。新增规则 R-20~R-22 |
| v0.3（合并版）| 2026-06-25 | 产品团队 | 独立文件 `looply-类目管理-PRD-v0.3.md`，基于合并版 v0.2 按 PRD 写作规范自检整改（无业务范围变更）：①将后端核心逻辑（§4.5 商品池构建顺序、候选值计算口径）、前台候选值范围（§3.2.2）、自动选品规则结构（§2.1 rule_config JSON）由伪代码/JSON 改写为文字描述业务逻辑，符合「PRD 不写技术契约」；②§5 依赖与风险表去除「接口」字样，「筛选候选值接口」改为「筛选候选值服务能力」、「tree 多级结构接口」改为「tree 多级结构返回数据」、异常表「接口报错」改为「请求失败 / 服务无响应」；③统一术语「Collection 落地页」，正文残留「类目页」均为前台原型真实文件名予以保留；④保留页面流转地图、页面布局示意图等可视化辅助代码块（非逻辑伪代码）|
| v0.3（合并版·补充）| 2026-06-26 | 产品团队 | §2.5 筛选维度管理补充两项数据源专项约定，对齐后台原型 v0.13/v0.14：①**attribute「按作用域分组」开关**——数据源选 attribute 即显示，未选关联属性时置灰提示；默认态挂在属性定义建议值（size/bag_size 建议开，color/material 关）可覆盖；开启后枚举值按命中的类目/品牌/系列作用域自然归组，组间序由「类目树序→作用域具体度→品牌/系列名」自动决定。②**backend_category 全量纳入与顺序合并**——固定三级、全量纳入、无需勾选；顺序按「锚定区+跟随区」合并：未调整项跟随商品侧 `backend_category.sort_order` 升序，**`sort_order` 为空或并列时按 `category_id` 升序兜底**（对齐 ER v2.6，该表无创建时间字段，以自增主键近似创建先后），拖拽=锚定到绝对位置（绑 key），新增类目入跟随区按源序落位，删除时锚定记录同步清除。同步修正过时描述（关联类目「勾选节点/至少勾选1个」改为「全量纳入、无需勾选」）；将数据源类型从「6 种」更正为「7 种」（补 tag，含说明文案、列表字段、字段表、可用样式矩阵、source_type ENUM、R-20）；新增规则 R-26、R-27；filter_dimension 表补 `scope_grouped`、`category_anchors` 字段 |
| v0.4（合并版·补充2）| 2026-06-26 | 产品团队 | 同步原型 v0.14 三项排序/分组细化：①**属性源排序默认「数据源默认序」**（确认沿用，无变更）。②**「按作用域分组」开关默认关闭**——原 size/bag_size 默认开启改为一律默认关闭，运营按需开启；size/bag_size 仅保留「建议开启」文案提示、不改默认态（§2.5 默认值、R-26、scope_grouped 字段注、原型 DimEditPage）。③**成色（condition）不提供排序配置**——固定业务序，维度编辑页不展示排序策略，新值按「数据源默认序」插入（§2.5 移出 condition 排序策略行 + 加只读说明、原型移除 condition 的 STRATEGY_BY_SOURCE 与 showValueSort）。④**标签待选池按名称 A→Z 排序**——英文字母序、中文拼音序，混合形态沿用三段式兜底；新标签进入自动落到正确位置（§2.5 tag 专项、原型 TagValuePicker 候选区排序）|
| v0.4（合并版·补充3）| 2026-06-26 | 产品团队 | §2.5 补「字母序 / 拼音序实现说明」：中文拼音排序由前端 `localeCompare` 与服务端/数据库**拼音排序规则（collation，如 MySQL `utf8mb4_zh_0900_as_cs`、PG ICU `und-u-co-pinyin`）**实现，**无需为枚举值/标签存拼音字段**（后端只配中文即可）；多音字按 collation 默认读音排序，个别不符预期用 Pin 调整，不引入逐条拼音配置。tag 专项约定同步引用该说明 |
| v0.4（合并版·补充4）| 2026-06-26 | 产品团队 | 同步原型 v0.14 展示样式四项调整：①**展示样式「树形」更名「分组」**（内部 key `tree` 不变）——前台呈现为「标题 + 值」，全文术语统一（§1.6/§2.5/§3.2.2/§3.6/§8.3/§8.5、原型 DISP_TYPES label、列表页说明）。②**属性源去掉「滑块」**展示样式（滑块仅价格用），DISP_BY_SOURCE.attribute 移除 slider。③**属性源开启「按作用域分组」时展示样式固定为「分组」**、各筛选模块锁定不可改（§2.5、R-26、原型 DimEditPage 展示类型选择器 groupedFixed 锁定）。④**按作用域分组组标题由后端用中点直接拼接「类目名·品牌名·系列名」下发**（不再前端拼接），且与类目「分组」样式数据结构一致（标题+值），前端同一套分组组件渲染（§2.5 作用域分组专项 + 候选值返回结构、§3.2.2、原型 SizeGroupedSort 说明与 groupTitle 注释）。新增 R-33、R-34 |
| v0.4（合并版·补充5）| 2026-06-26 | 产品团队 | **作用域分组的组间排序改为嵌套规则**（原「类目树序 → 作用域具体度 → 品牌/系列名」三键并列）：① 先按一级类目排序（商品端类目 sort_order），同类目聚为一片；② 同类目内按品牌名 A→Z（空品牌的纯类目组排最前）；③ 同品牌内按系列名 A→Z（空系列排最前），中文按拼音。同步原型 SizeGroupedSort 的 sortGroups（移除 LEVEL_RANK 具体度排序、改嵌套 catSort→品牌→系列）、组内说明文案（删去「按作用域层级自动分组…自然归组避免混排」与「与类目分组结构一致」两句）、PRD §2.5 组间排序键、R-26 |
| v0.4（合并版·补充6）| 2026-06-26 | 产品团队 | 两项调整：①**成色（condition）改为「保留枚举值 + 可 Pin、但不提供排序策略」**（原误为完全去掉枚举值/排序）：固定业务序，新值按数据源默认序插入，运营可 Pin 个别位置；原型 DimValueSort 加 `noStrategy`（隐藏策略选择器、保留 Pin 列）、condition 走该分支，PRD §2.5、R-36。②**「颜色卡片」仅限色系（color_family）属性**：商品侧新建「色系」属性并维护「色系→颜色值」映射表；筛选维度管理仅当属性=color_family 时展示类型才出现「颜色卡片」选项,其它属性/数据源不出现；原型加 `COLOR_FAMILY_ATTRS`、`dispOptionsFor(srcType,styles,attr)` 条件追加 color、DIM_ATTRS 用 color_family 替换 color/bag_color、Color 维度 mock 改 color_family，PRD §2.5 矩阵/说明/dimension key 表、R-35 |
| v0.4（合并版·补充7）| 2026-06-26 | 产品团队 | 作用域分组三处细化：①组标题拼接的「类目名」明确为**「一级类目名」**；②删去组间排序文案中的「(无品牌纯类目组排最前)」括注（纯类目组本就随一级类目排序,无需特别说明）；③**「分组」展示样式仅在属性开启「按作用域分组」时出现**——关闭分组时无「分组」选项(无分组结构可下发,避免选了不知如何返回数据),原型 DISP_BY_SOURCE.attribute 基础集移除 tree(改由 groupedFixed 动态补);④补**组合异步生成**说明——开启分组后后端异步扫描商品归类,组合非即时生成,可保存后回页面刷新查看(原型加黄色提示条 + 刷新按钮)。PRD §2.5 矩阵/作用域分组专项、R-20/R-26/R-34 |
| **v0.5（合并版）** | 2026-06-26 | 产品团队 | 独立文件 `looply-类目管理-PRD-v0.5.md`，对齐后台原型 **v0.14 → v0.15**：**移除「终端」字段,Collection 不再区分终端**。①新建/编辑向导由 4 步回到 **3 步**——取消独立「适用范围」步,市场+渠道并入「基本信息」步下方(适用范围只剩市场、渠道,无终端);②每个 Collection 默认同时服务 PC + 移动端,**基本信息(标题/副标题/Banner)与图文筛/快筛恒按 PC + 移动端两端分别配置**(不再"按终端多选后才分端");③**列表页去掉「终端」列与终端筛选**;④数据模型移除 collection 表 `device` 字段(SET 类型),标题/副标题/Banner 仍分端 JSON 存储;⑤选品规则渠道锁定提示由「回到第 1 步」改为「回到『基本信息』步」。同步 §1.6/§1.7/§2.1/§2.2/§2.3/§2.4/§2.6/§8.3/§8.6、R-28/R-32、流程图;原型新建页 CollectionNewPage、编辑页 CollectionDetailPage 去掉 stepScope/终端 Checkbox、合并适用范围进基本信息、Steps 改 3 步,列表页去终端列/筛选,validateDeviceSave 文案调整。商品端/前台 C 端逻辑不变 |
| **v0.7（合并版）** | 2026-06-29 | 产品团队 | 独立文件 `looply-类目管理-PRD-v0.7.md`，对齐后台原型 **v0.16**（本轮净变更，PC/移动端筛选形态明确化）：①**快筛改造为「移动端快筛」**——PC 端形态本就无快筛栏（吸顶 Chip 栏为移动端专属交互），快筛自此仅作用于移动端；后台模块名「快筛」→「移动端快筛」，**去掉 v0.6 短暂引入的快筛「适用终端」分端配置**（去掉 `devices` 字段、列表行内终端勾选、添加弹窗的终端选择），快筛项仅保留 维度 + 排序；保存校验对快筛只校验「模块启用却无选项」（不再校验适用终端）；前台预览 PC 端不渲染快筛栏（原型 QuickFilterBlock 改名+去 devices 列、AddFilterModal 去终端选择、PreviewFrame 加 `{isM && ...}`）。②**图文筛「适用终端」交互细化（PC 端仍保留图文筛）**——每条图文筛选项「适用终端」可直接在列表行内勾选 PC/移动端，与编辑弹窗内「适用终端」双向联动（弹窗保存回写到行）；「分开配图」仅多端时出现、减至单端自动关闭（原型 VisualFilterBlock 行内 Checkbox.Group + setRowDevices、VisualEditModal onSave 回写）。③**「至少勾选一端」强校验对齐两条路径**——行内取消最后一端不生效并提示「至少勾选一端」；编辑弹窗允许过程中取消、保存时非空校验拦截 + 红字提示。同步 §1.6/§1.7/§2.3/§2.4/§2.6/§2.6.1/§2.6.2/§3.6、R-28、版本规划、§8.2/§8.6（fc_visual_filter 补 devices/image_enable/split_image/image_url_pc/mobile 字段；fc_quick_filter 注明仅移动端无 device）、附录设计稿索引版本 v0.14→v0.16。前台 C 端其余逻辑、商品端逻辑不变 |
| **v0.8（合并版）** | 2026-06-29 | 产品团队 | 独立文件 `looply-类目管理-PRD-v0.8.md`，三项产品决策细化：①**移动端商品列表触底接「For You」推荐区**——本 Collection 商品每 20 个一屏无限流加载，全部滑完后不循环、不停止，接分割线 + 文案「Explore our picks for you」+ 全站商品推荐 feed，**推荐逻辑复用主页 For You（本模块不另定算法）**；适用移动端全部形态（APP + 移动端 Web/H5）逻辑一致。**PC（Web）端传统翻页（页码，每页数量待 UI 定）、到末页结束、不接推荐区**。②**category 一级类目改为「仅作分组标题、不可勾」**——用户只勾末级（三级）类目，匹配 = 所勾三级类目覆盖范围（多选 OR）；废弃 v0.7「各层级可勾 / 勾父节点覆盖即匹配 / 子节点 UI 自动全选」口径。③**category 与「按作用域分组」属性的分组(tree)返回结构完全等价化**——均为「分组标题（不可勾）+ 值（可勾多选）」两层，节点协议统一（值节点 value/display_name/count、分组节点 group_title），前端同一套分组组件渲染、不分叉。同步 §3.1/§3.2.2/§3.6/§3.10（新增分端分页表 + 触底推荐区段）、§2.5 候选值返回结构、R-23/R-27、新增 R-37（类目一级不可勾）/R-38（分端分页与触底推荐）、§8.7 补 PC 每页数 + 触底推荐区样式待确认项。前台原型 category 一级若已画成可勾需同步改（待核对）；商品端逻辑不变 |
| **v0.9（合并版）** | 2026-06-29 | 产品团队 | 独立文件 `looply-类目管理-PRD-v0.9.md`，修正「按作用域分组」枚举值排序的**跨组联动口径**与**组内排序策略**。①**跨组联动按"全局位次"而非"各组同一格位"**：v0.8 把"联动到相同位置"误表述为各组里的第几格，各组枚举值集合不一致时自相矛盾（成员不同、越界、后配置组覆盖前组、「M 算哪组」无解）。v0.9 明确固定位置按**属性值全局位次**记忆，各组按全局位次给本组拥有的值排序、缺值跳过、相对先后一致；交互仍为「固定位置输入框」（与原型一致，原型交互无需改）。结合商品系统数据结构核对（`商品系统-PRD-v1.7` / `商品主数据 ER v2.6`：尺码值来自独立 size_system/size_value 表按体系归组、普通枚举属性值是 attribute_option 全局候选列表无体系概念）分两类——**普通枚举属性**全属性一套全局位次（复用 attribute_option.sort_order）；**尺码属性**按尺码体系各一套位次（复用 size_value 序），同体系组联动、跨体系组互不影响。②**分组态组内排序固定「数据源默认序」、不展示策略下拉**（分组后组内值少，靠固定位置微调即可；原型 SizeGroupValues 移除策略 Select、锁 source）。③**打平态（排序策略 + 固定位置 value_pins）与分组态（数据源默认序 + 固定位置 value_order_grouped）两套独立存储**，切换 scope_grouped 互不串味。组间排序沿用现有嵌套规则（一级类目序→品牌 A→Z→系列 A→Z）。同步 §2.5 重写「组内排序」段、更新 R-26、新增 R-39、§8.6 数据模型（value_pins 限定打平态 + value_order_grouped 改存全局位次 + scope_grouped 注两态）。原型 v0.16 SizeGroupValues 同步改（去策略下拉、锁数据源默认序），括号配平通过。前台 C 端/商品端逻辑不变 |
| **v0.10（合并版）** | 2026-06-29 | 产品团队 | 独立文件 `looply-类目管理-PRD-v0.10.md`，明确**分组标题 / 节点展示名的多语言取数口径**。依据商品系统数据结构核对（`商品系统-PRD-v1.7` / `商品主数据 ER v2.6`）：一级类目名 category_name **单列、无英文列、无翻译表**；品牌 brand_name+brand_name_en、系列 series_name+series_name_en 双列双语；广义多语言由平台**翻译模块**统一管，前台按当前语言取、缺则 fallback 默认语言原值。据此定四点：①**取数优先级**＝运营在维度配置的「前台展示名」> 翻译模块多语言名 > fallback 默认语言原值；②**运营展示名不写死单值，而是注册多语言 key 到翻译模块**做翻译，多语言切换一致生效；③**一级类目英文缺口由运营展示名兜底**（类目无英文列，运营配展示名/翻译模块补齐，避免组标题中英混排）；④**后端按当前语言解析每段、拼好整串下发，前端不拼接不翻译**。影响三处一起对齐：作用域分组组标题、类目维度分组标题（一级）+ 三级值 display_name、候选值返回 display_name 通用口径。同步 §1.7（前台语言行细化 + 新增「筛选名称多语言取数」行）、§2.5（组标题新增「多语言取数」子条、关联类目行、类目专项加多语言说明、候选值返回 display_name 补优先级）、新增 R-40。前台 C 端/商品端逻辑不变；原型无需改（取数为后端口径，UI 不变）|
| **v0.11（合并版）** | 2026-06-29 | 产品团队 | 独立文件 `looply-类目管理-PRD-v0.11.md`，**清理废弃残留字段 `h1_title` / `description`**。二者为第一版遗留，后续版本已去掉运营录入入口（§2.3 向导字段表无此两行），但数据模型与降级链未清干净，造成「数据模型有、界面无」的不一致。彻底移除：①§8.6 collection 表删 h1_title、description；②§4.1 Meta Title 降级链摘 h1_title 环（seo_title→title，三级降两级）、Meta Description 降级链摘 description 环（seo_description→默认兜底，三级降两级）、规则说明措辞同步；③§4.4 品牌驱动降级链 H1 段改取 collection.title、描述段摘掉，入口措辞「H1/描述/Meta/图」→「Meta/图」；④§2.3 旁注去 description。前台 `<h1>` 文案直接走标题降级链。同步升 ER v1.4。前台 C 端/商品端逻辑不变 |
| ER v1.2→v1.3 | 2026-06-29 | 产品团队 | 实体关系图从 v1.2（停留在 PRD v0.2~v0.3）一次拉齐到 PRD v0.10：collection 表 PK fc_id→collection_id、name_en→title(分端)、删 icon_url/visual_image_enabled、新增 banner_enabled/description/h1_title/linked_brand_id；filter_dimension 删 value_sort、新增 sort_strategy/value_pins/value_order_grouped/scope_grouped/category_pins/sort_order、补 tag 数据源、disp_*→display_type_*；fc_visual_filter 改 label_text/filter_condition、新增 devices/image_enable/split_image/image_url_pc/mobile；fc_quick_filter 改名移动端快筛+status→enabled；fc_sort_config 去 manual；三关联表 FK fc_id→collection_id；新增 tag 外部实体 + brand→collection(linked_brand_id)/tag→filter_dimension 关系；DESIGN_RULES 重写 21 条。生成 looply-类目管理-ER-v1.3.svg（17 表）|
| ER v1.3→v1.4 | 2026-06-29 | 产品团队 | 随 PRD v0.11 清理：collection 表删废弃残留字段 h1_title、description（24 列→22 列）。生成 looply-类目管理-ER-v1.4.svg |
| **v0.12（合并版）** | 2026-06-29 | 产品团队 | 独立文件 `looply-类目管理-PRD-v0.12.md`，两块。**A. 删除「品牌驱动 Collection」独立逻辑**——品牌页实质=选品规则含 brand 条件的普通 Collection，无任何区别，不应有专属机制。删 §4.4 原整节、linked_brand_id 字段、R-16/R-17、「从品牌批量生成 Collection」入口、§5 品牌管理 SEO 依赖项、§6 品牌驱动 P1 项、§3.12 Brand schema 括注、sitemap-brands；§4.4 改写为「商品多归属与 PDP 面包屑主归属」（保留原误挂的 R-19）；空 Collection noindex 由品牌页专属降为通用规则（R-18 改写、§3.12 补通用条）。同步 ER 删 linked_brand_id（v1.5）。**B. 多语言梳理结论落地**——核对 `多语言模块-PRD-v3.1`/`商品系统-PRD-v1.7`/`商详页-PRD-v1.6`：①翻译模块四元组(resource_type+resource_id+field_name+language_code)寻址，业务表不存 translation_key、仅补 source_language_code(硬编码 en)，务实保留源文本字段；②属性枚举值(resource_type=attribute_option,商品系统已注册,单品页同源)/品牌名(多禁译)/系列名/类目名均由上游统一管,本模块只取数;③本模块仅 7 类运营录入文案接入(标题/副标题/Meta Title/Meta Desc/图文筛文案/维度名称/节点展示名),走 entity_field API 自动注册;④SEO 兜底串多语言、{title}取已译内容拼接不重译;⑤前端固定 UI 文案走 static_content 由前端/运营后台维护、不在本模块后端注册。新增 §4.6 前台文案多语言接入机制、R-40 改写、R-41。前台 C 端/商品端逻辑不变 |
| ER v1.4→v1.5 | 2026-06-29 | 产品团队 | 随 PRD v0.12 删品牌驱动 Collection：collection 表删 linked_brand_id 字段(21 列)、删 brand→collection 关系(关系 20)、brand 实体备注改「brand 作为 rule_config 普通条件」、R-19 改写为「品牌页=普通 Collection 无独立机制」。生成 looply-类目管理-ER-v1.5.svg |
| 原型 v0.16→v0.17 | 2026-06-29 | 产品团队 | 后台原型 `looply-类目管理-后台-v0.17-antd.html`：**去掉 PC 端图文筛，图文筛改为「移动端图文筛」**（与移动端快筛对齐，均为移动端专属）。①VisualEditModal 去掉「适用终端」多选 + 非空校验、去掉「分开配图」开关 + PC/移动端双图,图片改单图(移动端 160×160);②VisualFilterBlock 模块名「图文筛」→「移动端图文筛」、去掉表格「适用终端」列与行内勾选 setRowDevices、去掉「图片」列的分端配图标识、说明文案改「图文筛为移动端专属，PC 端无图文筛栏形态」;③前台预览 PreviewFrame 圆形图文筛卡用 {isM &&} 包裹(PC 不渲染,与快筛一致);④基本信息注释、保存校验文案(validateDeviceSave 提示「移动端图文筛/移动端快筛」)、标题/loading 版本号同步。括号配平通过。⚠️待同步 PRD：§2.6.1 图文筛分端逻辑、§3 前台图文筛、R-28、§8.6 fc_visual_filter 的 devices/split_image/image_url_pc/mobile 字段需相应删除(下一版 PRD 处理) |
| **v0.13（合并版）** | 2026-06-29 | 产品团队 | 独立文件 `looply-类目管理-PRD-v0.13.md`，对齐后台原型 **v0.17**——**去掉 PC 端图文筛，图文筛改为「移动端图文筛」**（与移动端快筛对齐，二者均为移动端专属）。①后台模块名「图文筛」→「移动端图文筛」，仅移动端、不分端、无适用终端字段；②§2.6.1 删选项级 devices(适用终端)/split_image(分开配图)/image_url_pc/image_url_mobile，图片回单 image_url，删「适用终端交互」整段；③保存校验对图文筛只校验「模块启用却无 active 选项」(不再校验适用终端)；④前台预览 PC 端不渲染图文筛栏。同步 §1.6/§1.7 术语、§2.3 Step3、§2.4 校验(含上线强校验+拦截弹窗)、§2.6 引言+父标题+目录锚点、§3.5 前台标题、R-28、§6 版本规划、§8.5 约束分端行、§8.6 fc_visual_filter 表(12 列→8 列)+注脚。升 ER v1.6。前台 C 端其余逻辑、商品端逻辑不变 |
| ER v1.5→v1.6 | 2026-06-29 | 产品团队 | 随 PRD v0.13 图文筛改移动端专属：fc_visual_filter 表删 devices/split_image/image_url_pc/image_url_mobile(12 列→8 列)、表名改「移动端图文筛项配置」、note 改仅移动端单图、R-10 改写为「图文筛/快筛均移动端专属」。生成 looply-类目管理-ER-v1.6.svg |
| **v0.14（合并版）** | 2026-06-29 | 产品团队 | 独立文件 `looply-类目管理-PRD-v0.14.md`，完善 §4.6 多语言接入并**修正存储口径**。①**业务表不存任何文案文本（含 en 源文）、只存 `translation_key`（=resource_type.field_name）**，文本统一存翻译服务，后台按 en 取源文展示——覆盖 v0.12「业务表务实保留源文本」。②§4.6 ① 后台业务动态文案(entity_field) 7 类补全「resource_type/field_name/定位主键 resource_id/业务表存储」四列；§4.6 ④ 前端固定 UI 文案(static_content) 给出完整清单表(footer_recommend_title=Explore our picks for you、sort.recommended/newest/price_asc/price_desc、toast.fav_added/removed、filter.apply/clear_all/view_all/entry、empty.no_results 等)供 AI coding 直接落地。③§8.6 数据模型 title/subtitle/seo_title/seo_description/dimension_name/label_text 标注「存 translation_key、文本在翻译服务」；R-41 改写。前台 C 端/商品端逻辑不变 |
| 原型 v0.17(微调) | 2026-06-29 | 产品团队 | 枚举值排序/类目排序的「固定位置」输入框 placeholder 改为「输入序号固定」(更自解释)、宽度 72→120;三处统一(类目 Pin、分组态、打平态)。括号配平通过,版本号不变 |
| v0.14（补充） | 2026-06-29 | 产品团队 | 维度名称 dimension_name 改存 key 后，**全局唯一性口径明确为「锚定 en 源文查重」**：业务列存 translation_key（非文本）、唯一性不能跨语言（译文可碰撞），故保存时拿 en 源文向翻译服务查重、重复拦截，属写时业务校验而非 DB 唯一约束。同步 §2.5 字段表+校验规则、§4.6 ①、§8.6 dimension_name 注释。ER 本轮不动（字段语义变化、列名未变，待后续批量同步标注）|
| 原型 v0.17→v0.18 | 2026-06-29 | 产品团队 | 后台原型 `looply-类目管理-后台-v0.18-antd.html`：**品牌（brand）维度排序固定「字母序 A→Z」、不可改、不提供固定位置**。DimValueSort 新增 `fixedAlpha` 锁定模式（锁 strategy=alpha、隐藏「排序策略」选择器、隐藏「固定位置」InputNumber 列，仅只读展示已按字母序排好的枚举值 + 说明文案）；DimEditPage 的 brand 走 `fixedAlpha={srcType==='brand'}` 分支（仍在 showValueSort 内，区别于 condition 的 noStrategy——condition 锁数据源默认序但保留 Pin，brand 连 Pin 也去掉）。结果通知栏为前台 C 端形态，后台原型预览区本就未渲染，无需改动。括号配平通过、JS 语法自查通过（script 标签 7 对、body6 括号差与 v0.17 一致）、版本号 / loading 文案同步 v0.18 |
| **v0.15（合并版）** | 2026-06-29 | 产品团队 | 独立文件 `looply-类目管理-PRD-v0.15.md`，两项产品决策，对齐后台原型 **v0.18**。**A. 品牌（brand）维度排序固定「字母序 A→Z」、不可改、无固定位置**——brand 取消排序策略选择与 Pin 能力，前台一律字母序 A→Z（中文拼音 A→Z），新增品牌自动落位；同步 §1.6 排序策略术语（补 brand 例外）、§2.5 ① 引言适用源去 brand、② 可选范围表 brand 行改「固定字母序 A→Z 不可改不提供固定位置」、新增 brand 专项 blockquote（与 condition 对比说明）。**B. 删除前台「结果通知栏」整个能力（PC + 移动端）**——商品数量实时计数 + 已选条件标签 + 单标签 ✕ 逐项移除 + Clear All 全部去除，两端均无该区域；清除/调整筛选改由快筛二级面板 Clear 与通用筛选器 Reset 承担。删 §3.8 结果通知栏整节并**重编号** §3.9→§3.8（通用筛选器）/§3.10→§3.9（商品网格）/§3.11→§3.10（Auto-Scroll）/§3.12→§3.11（SEO）、§3.9.8→§3.8.8；§3.1 布局图去结果通知栏行；§3.8.8 去「移除同步/Clear All」对结果通知栏引用；§3.10 Auto-Scroll 去「已选条件 ✕ 逐项移除 / Clear All」两行；§1.4 前台场景 3 改写；§4.6 固定文案清单去 filter.clear_all；§6 P0 清单去结果通知栏；§8.7 去「结果通知栏高度」与「已选条件标签样式」两行；修正 §3.2.2 / §3.7 / R-11 / R-18 / R-25 / R-38 交叉引用。ER 本轮不动（结果通知栏与排序策略均不涉及数据模型字段，brand 排序为前端展示口径）。其余逻辑沿用 v0.14 |
| v0.15（增补） | 2026-06-29 | 产品团队 | 同版三项增补（同步原型 v0.18）。①**Pin 输入框 placeholder「输入序号固定」→「输入序号固定排序」**（更自解释，原型三处统一：类目 Pin、分组态、打平态）。②**标题 / 副标题新增「文字色值」配置（分端、必填）**——基本信息区标题、副标题各加前台文字颜色，按 PC + 移动端分别配置（适配各端 Banner 背景），色盘（光谱 + 预设）取色或输入 hex，二者同步；上线必填、未配红框提示；色值为 hex 视觉配置、非翻译文本不接翻译服务。同步 §2.3 旁注 + 字段表（加 2 行）+ 解锁条件 + 校验、§8.6 collection 表新增 `title_color`/`subtitle_color`（JSON 分端）；原型加 ColorField + ColorPicker import、NewPage/DetailPage 各加两行、基本信息提示文案更新。③**前端固定 UI 文案改「语言包」口径、不逐条注册 key**——核对 `多语言模块-PRD-v3.1`（§2.2 注 / §3.3 实现方式说明）：static_content 的后台 key 注册仅参考方案，技术最终走语言包；据此改写 §4.6 ④ 标题 / 正文 / 表头 / 注脚、R-41，下表 field_name 改为「语言包 key 参考」（前端组织语言包用，非后台注册项）。业务动态文案 7 类仍走 entity_field 不变。**升 ER v1.6→v1.7**（collection 表 +title_color/+subtitle_color，gen 脚本同步、重新生成 SVG，17 表不变）。原型括号配平 + JS 语法自查通过 |
| ER v1.6→v1.7 | 2026-06-29 | 产品团队 | 随 PRD v0.15 增补：collection 表新增 `title_color`、`subtitle_color`（分端 JSON、hex 视觉配置、必填、非翻译文本），note 同步标注。生成 looply-类目管理-ER-v1.7.svg（17 表、20 关系、21 规则不变）|
| v0.15（增补2·A-Z索引） | 2026-06-29 | 产品团队 | 明确**前台品牌筛选的 A-Z 字母索引**口径（纯文档补充，不改数据模型/原型）。背景：前端为品牌维度加了 A-Z 字母索引条 + 滑动定位，需依赖品牌列表「严格首字母单调有序、同字母连续相邻」——而 brand 维度后台已**完全锁死字母序 A→Z、无排序策略、无固定位置 Pin**（上一轮决策），恰好保证此单调性，故同字母品牌不会被拆散、点任一字母定位到唯一连续区段。补三处：①§3.6「各 display_type 二级面板」新增「品牌（brand）维度的 A-Z 字母索引」段（依赖前提、`#` 档承接数字开头品牌如 `3.1 Phillip Lim`、空字母档置灰、中文按拼音首字母归档）；②§2.5 brand 专项补「锁死的另一原因＝保证前台 A-Z 索引定位，开 Pin 会拆散字母区段破坏定位」；③§8.7 新增「品牌 A-Z 字母索引条」样式待确认项。结论：维持 brand 不开 Pin。三段式混合形态防呆（数字/字母数字品牌）口径不变，仍按「纯字母→字母+数字→纯数字」自动排序 |
| **v0.16（合并版）** | 2026-06-29 | 产品团队 | 独立文件 `looply-类目管理-PRD-v0.16.md`，依据 AI 评审 `recommendation-handoff-and-summary` 的 OQ-007/OQ-008 回执，两项产品决策。**A. 前台 PC 端落地页纳入 V1**（原 V2，本期提前）——PC 做完整筛选体验：Banner + **左侧常驻可收起筛选栏**（=通用筛选器全维度、PC 形态非抽屉、勾选即时收窄、待前端 UI 稿）+ 工具栏（仅排序）+ **多列自适应网格** + **传统翻页**；图文筛/快筛仍移动端专属、不上 PC，触底 For You 仍仅移动端（PC 翻页不接推荐，利于深链与 SEO）；数据逻辑层（候选值/互不影响/count=0隐藏/brand A-Z 锁序与索引/SEO）两端共享。同步 §1.2 范围、合并说明、§3 标题+引言（去「Mobile 优先/PC V2」）、目录、§3.1 加 PC 布局示意、§3.9 列数分端+PC 翻页 V1、**新增 §3.12 PC 端落地页专章（6 小节 + 与移动端差异速查表）**、§6.1 加 PC 落地页+触底推荐 P0 行 / §6.2 删「前台 PC 端」V2 行、§8.7 加 PC 列数/筛选栏/每页数待确认项。**B. 触底 For You 推荐区相对本 Collection 去重**（OQ-007 决策点①）——推荐 feed 排除本 Collection 商品集（传 `collection_id` 推荐侧自查），**去重仅限本合集商品、非用户看过即永久消失**，合集本体可往回滚/刷新照常；卡片补全＝推荐只返 listing_id、Collection 复用首页 Feed 卡片（决策点②）。同步 §3.9 触底推荐区 + R-38。同步回填 AI 评审文档 OQ 状态与回执。ER 本轮不动（PC 入 V1 与去重均为前端/契约层，无 schema 变更）。其余逻辑沿用 v0.15 |
| 原型 v0.18(微调) | 2026-06-30 | 产品团队 | 后台原型 `looply-类目管理-后台-v0.18-antd.html` 三处交互微调（数据模型不变，PRD v0.16 就地增补 + 本记录留痕）。①**标题/副标题色值控件位置调整**——色值由独立成行/缩进子块改为**紧跟对应标题、副标题输入框右侧**（同一行：文字输入框 + 色盘控件），直观体现「这段文字→这个颜色」；同步合并 helper 文案、去掉中间试验过的「缩进子块（淡紫填充）」方案（视觉过重）。②**色值默认值定为 `#070707`**（近黑）——新建页两端标题/副标题色值默认带出 `#070707`，运营可点击色块取色或直接输入色值覆盖（编辑页保留各自既有示例色值）。③**固定位置输入框 placeholder 改版**——原 placeholder「输入序号固定排序」在窄框内被截断显示不全，改为**外置标签「输入序号:」+ 框内 placeholder「固定排序」**，输入框宽度收窄；三处统一（类目固定位置、分组态、打平态）。同步 PRD §2.3 旁注 + 标题色值/副标题色值字段行（补默认值与控件位置）、§2.5 ③ 固定位置加「v0.18 交互变更」注。原型括号配平、JS 语法自查通过。ER 升 v1.7→v1.8：title_color/subtitle_color 字段名与 collection note 补「默认 #070707」标注（字段仍为 v1.7 既有列，无新增/删除列）|
| ER v1.7→v1.8 | 2026-06-30 | 产品团队 | 随原型 v0.18 微调：collection 表 `title_color`/`subtitle_color` 字段说明与表 note 补充**默认值 `#070707`** 标注（无新增/删除列，仅默认值语义补全）。gen 脚本 VERSION→v1.8、输出名同步，重新生成 `looply-类目管理-ER-v1.8.svg`（17 表、20 关系、21 规则不变）|
| v0.16（增补·搜推与同步） | 2026-06-30 | 产品团队 | 两项决策落 PRD v0.16（同版增补、不升号；ER 不动——均前端/契约层）。**C. 筛选 / faceting / 排序统一交搜推服务执行、搜推读 Collection 池（真相源一致）**——`recommended` 改为搜推个性化排序（冷启动 / 匿名降级综合启发式），`newest`/`price` 等确定性排序亦由搜推执行（§4.5.4、§8.4 排序逻辑列）；**新增 §4.5.6「筛选 / 排序与搜推服务的边界」**：①商品池定义只有 Collection 一处、搜推读池不另造（化解评审第三部分「池定义双源 → count 失真 / 越筛越少」风险）；②业务规则（互不影响 / count=0 隐藏 / 类目匹配 / brand 锁序 / Pin）定义权留 Collection PRD 唯一真相源、搜推按 spec 执行；③能力分工由开发技术方案定，PRD 不写实现。§5.1 搜推升 **P0 强依赖**、§5.2 加搜推强依赖风险（未就绪则前台筛选浏览阻塞）。**D. 自动选品同步频率**——规则匹配 job「每日凌晨」→**每小时**（新上架纳入 / 不符移出 /「最近 N 天」时间窗重算）；**下架 / 售出改实时事件驱动移出**（不等整点）。同步 §4.5.5 定时任务表、§2.1 listing_date 注、§5.2 风险。回填评审文档第三部分（人工决策调整：本期即把筛选 / 排序交搜推，前提池 + 规则真相源一致）、OQ-003 状态。原型本轮不动 |
| v0.16（增补·维度显示口径修正） | 2026-06-30 | 产品团队 | 修正「筛选维度显示规则」：原「某维度有 ≥2 个不同值才展示」会**误杀稀疏单值维度**（如池 100 件中仅 10 件 material=Leather、其余为空——只 1 个有效值却被整维度隐藏，但筛出这 10 件有意义）。改为按**「能否收窄当前池」判定**：设基准范围商品总数 N，值 `count=0` 或 `count=N`（覆盖全池、筛了不收窄）为无效值不下发；维度存在至少 1 个 `0<count<N` 的有效值才展示。同步 §4.5.2（权威定义）、§2.6.3 / §3.6 / §3.8 / §3.12 / §3.2.2 候选值补充规则共 6 处。搜推对接说明 §3 同步。纯口径修正，不改数据模型/原型，ER 不动 |
| 原型 v0.18(一致性整改) | 2026-06-30 | 产品团队 | 跨文档一致性审查（原型 vs PRD），修原型 + PRD 双向缺口，确保业务闭环。**原型改 4 处**：①**商品列表批量操作分模式落地**——自动选品支持「批量固定 + 批量隐藏」（无移除，规则动态命中、移除无意义）、手动选品支持「批量固定 + 批量隐藏 + 批量移除」；批量工具条由仅手动改为两模式都有、按口径区分。②**移动端图文筛 / 快筛补「展示」启用开关列**——每项可临时隐藏而不丢配置（图文筛 status、快筛 enabled），图文筛保存校验改为按「≥1 条展示中(active)」计数。③**价格条件币种跟随市场**——删硬编码 `['USD','EUR','GBP','CNY']`（CNY 不在任何市场支持币种、属错误项），改用 `marketCurrencies(market)` 跟随所选市场，单币种自动锁定；market 状态经 CollectionNewPage/DetailPage → FilterConfigTable → VisualFilterBlock → VisualEditModal → RuleBuilder 全链路打通（7 处）。④**删废弃死代码** `ColorField`/`SubFieldRow` 组件与 `.lp-subrow` CSS（早期试验方案残留，无引用）。另删基本信息「关联商品数 856」展示字段。**PRD 改 6 处**：①§2.4 商品列表**补「成色 / 销售状态 / 可售库存 / 可见状态」列**、明确两模式通用列集合；**可售库存补公式** `max(0,在库-预占-锁定)`、数据来自库存管理、0 库存红标、仅展示不剔除（准入归商品准入逻辑）。②§2.4 批量操作口径改为「自动:固定+隐藏；手动:固定+隐藏+移除」。③§5.1 依赖表**新增「库存管理模块」P1**。④§2.5 + §8.6 **删图文筛维度级展示样式**（`display_type_visual` 字段 + 分模块表「图文筛」行 + R-12），明确图文筛每项自带卡片+规则、不消费维度级 display_type（消除 PRD 内部 §2.5/§8.6 与 §2.6.1 三方不一致的孤儿字段）。⑤§4.1 Meta Description 兜底文案**统一为原型那句**（`...expertly curated and quality-assured.`、去掉「up to 70% off retail」折扣承诺，规避合规风险）。⑥§2.3 色值**「必填红框」改为「默认 #070707 始终有值」**（字段表 ✅→默认值、解锁条件移除色值、校验规则改写），与原型 TextColorField 默认值行为一致。**升 ER v1.8→v1.9**（删 display_type_visual 列、R-12 同步）。原型括号 / 括弧配平通过、market 全链路 7 处校验通过 |
| ER v1.8→v1.9 | 2026-06-30 | 产品团队 | 随原型 v0.18 一致性整改：filter_dimension 表删 `display_type_visual` 列（图文筛不走维度级展示样式，每项自带卡片+规则）、DESIGN_RULES R-12 改「按快筛/通用筛两模块配置」。gen 脚本 VERSION→v1.9、输出名同步，重新生成 `looply-类目管理-ER-v1.9.svg`（17 表、20 关系、21 规则不变）|
| 原型 v0.18(固定位置交互补全) | 2026-06-30 | 产品团队 | 补齐商品列表固定位置的冲突提示与即时生效（此前 PosTable 用裸 `defaultValue` 输入框，无状态、无冲突检测，与枚举值/类目早有的「顶替+Toast」范式 R-30 不一致）。**原型 PosTable 重写**：①固定位置改为 `rows` 受控状态 + `applyPin`，**同位顶替原项**（原项清空、恢复自动排序）+ `message` Toast，对齐 R-30。②**单个固定 = 行内输入框直接填序号 + 实时检测 + 确认气泡**——值变化即检测：无冲突气泡问「固定「{商品}」到第 N 位?」、有冲突气泡警告「⚠️ 第 N 位当前为「{原商品}」将被顶替」，两种都需点「确定」才即时生效（回车=确定、Esc/失焦取消）；清空输入框即取消固定。③**批量连续固定先弹窗确认**——`openBatch` 预计算占用区间与被顶替项，Modal 逐条列出「第 N 位「{原商品}」将被顶替、恢复自动排序」，确定才执行、取消不生效；无冲突显示「无位置冲突」。④**单项操作即时生效**——隐藏/显示、移除（行内+批量均 Popconfirm 二次确认）、批量隐藏即时反馈，不靠顶部全局保存；表格 dataSource 改 `live`（过滤已移除）、底部统计改「已固定 N 件」。⑤antd 解构补 `Popover`/`Popconfirm`。**PRD §2.4** 新增「固定位置交互（两种模式通用）」blockquote：行内输入+实时检测+确认气泡（无冲突也需确定）、冲突顶替 Toast、清空取消固定、批量顶替前弹窗清单、单项即时生效；自动/手动模式 Position 与批量描述同步改写。原型括号/标签配平通过（Popover 1 / Popconfirm 2 / Modal 1 闭合）。ER 不动（纯交互、无 schema 变更）|
| 原型 v0.18→v0.19 + PRD（去「按商品数降序」移 V2）+ tag 第一期 | 2026-06-30 | 产品团队 | 两项。**A. 枚举值排序策略去「按商品数降序」(count_desc)、移至 V2**——该策略需商品数实时统计支撑，第一期不做。原型 v0.18→**v0.19**：SORT_STRATEGIES 删 count_desc、STRATEGY_HINT 删对应说明、STRATEGY_BY_SOURCE 四源(attribute/brand/series/tag) opts 去 count_desc、orderValues 删 count_desc 分支、hint fallback 改 source、属性未选提示文案去「按商品数降序」、mockCount 注释改（count 仍用于列表右侧商品数展示，不再用于排序）；括号配平与 v0.18 一致、JS 自查通过、版本号/loading 同步 v0.19。PRD：§1.6 术语、§2.5 ① 策略表删行+加「移至 V2」说明、§2.5 ② 可选范围表 attribute/series/tag 三行去 count_desc、R-29 改「V1 共 3 种」、§8.6 sort_strategy ENUM 去 count_desc、§6.2 V2 规划新增「枚举值按商品数降序排序策略」行。**升 ER v1.9→v2.0**：sort_strategy 列说明去 count_desc（标「移至V2」），重新生成 `looply-类目管理-ER-v2.0.svg`（17 表/20 关系/21 规则不变）。**B. tag（标签）维度明确第一期纳入**——核对商品系统 PRD v1.7 标签管理已建成，PRD §2.1 选品条件表 tag 行去「（预留）」改「仅渠道商品/通用标签，实物标签不可用」；搜推对接说明 §1.1 tag 改「第一期纳入」并补口径（运营从标签库显式勾选子集、搜推按已勾选集做筛选与 count）|
| ER v1.9→v2.0 | 2026-06-30 | 产品团队 | 随排序策略去 count_desc：filter_dimension 表 `sort_strategy` 列说明由 `source/numeric/alpha/count_desc` 改 `source/numeric/alpha(count_desc 移至V2)`。gen 脚本 VERSION→v2.0、输出名同步，重新生成 `looply-类目管理-ER-v2.0.svg`（17 表、20 关系、21 规则不变）|
| 原型 v0.19 + PRD（枚举值去 N 件 + 图文筛图片改模块级）| 2026-06-30 | 产品团队 | 两项。**A. 维度编辑页枚举值排序去掉「N 件」商品数展示**——count_desc 已移 V2，该数无展示用途，原型 DimValueSort 行删 `{it.count} 件` span（mockCount 保留但不再渲染）。**B. 移动端图文筛图片由「选项级开关」改「模块级展示形式」**——原 `image_enable` 逐项开关会导致部分项有图、部分无图，前台图文混排难处理。改为模块级 `display_mode` 二选一：`graphic` 图文模式（所有 active 选项必须配图）/ `text` 纯文本模式（所有项不展示图片）；切换保留已上传图片（后台留底、前台不展示）。原型 v0.19：VisualEditModal 去选项级 Switch、图片上传按模块模式 required/隐藏；VisualFilterBlock 加 `imageMode` 状态 + Segmented「展示形式」选择器、图片列按模式渲染（已配图/缺图/纯文本）、工具条加缺图计数；括号配平 (18,0,0) 与基线一致、JS 自查通过。PRD：§2.6.1 模块配置加 `display_mode`、选项级 image_enable 字段改写为模块级控制、保存校验加「图文模式缺图拦截」；§2.4 + §8.7 上线强校验加缺图校验；§3.5 前台改模块级形态；§8.6 删选项级 image_enable、加模块级 display_mode 说明、落库注脚同步。**升 ER v2.0→v2.1**：fc_visual_filter 删 `image_enable` 列、collection 表加 `visual_filter_display_mode`、note/R-10 同步；**顺带修 R-11**（ER 内仍是旧「≥2 个不同值」口径，改为 count<N 与 PRD §4.5.2 对齐）。重新生成 `looply-类目管理-ER-v2.1.svg`（17 表/20 关系/21 规则不变）|
| ER v2.0→v2.1 | 2026-06-30 | 产品团队 | 随图文筛图片模块级化：fc_visual_filter 删选项级 `image_enable` 列、`image_url` 注补「text 模式不展示、保留」、note 改模块级 display_mode 口径；collection 表加 `visual_filter_display_mode`(graphic/text)；R-10 去选项级 image_enable；R-11 由旧「≥2 个不同值」修正为 count<N 口径（与 PRD §4.5.2 一致）。gen 脚本 VERSION→v2.1、输出名同步，重新生成 SVG（17 表、20 关系、21 规则不变）|
| PRD v0.16（AI 评审裁决落地）| 2026-07-01 | 产品团队 | 依据 AI 评审 `prd-issues-admin`（17 条）逐条裁决后批量修订 PRD v0.16。**#6** 废除「禁止 draft→inactive」，三态可自由流转（含草稿直接关闭），改 §2.4 状态流转 + R-06。**#7** 图文模式缺图校验由「拦截上线」改「拦截保存」（保存即校验），改 §2.6.1 + §8.2，原型 v0.19 提示文案同步。**#3** 正文「v0.19」标注为**原型版本号**（非 PRD 版本，PRD 当前 v0.16），改 §8.6 image_enable/display_mode 两处注释。**#5** 成色级数收口：§2.4 商品列表成色列标注「示意，最终以商品系统 condition 枚举为准」。**#11** 无条件自动选品**保存拦截**（避免与系统级 All Products 重复），引导用「库存状态=在售」表达全选；改 §2.1 规则表、Step2 完成条件、§2.3 编辑锁定项；同步 stock_status 枚举收口注（跨库存模块 v1.2/商品系统 v1.7 口径待对齐）。**#1** fc_manual_product 加 `pin_position INT NULL`（固定绝对位置，与 sort_order 分离）。**#16** 加 `deleted_at`，移除改软删除保留审计。**#15** Pin 并发采用 last-write-wins（§4.5.4）。**#17** 商品移出与清除 pin 为**同一原子操作**（无中间不一致窗口），改 §4.2 + R-08。**#13** sort_strategy 注明对 tag 不适用（待选池 A-Z、已选池拖拽）。**#12** 按作用域分组异步生成补 UX 规范：生成期编辑页显示「分组数据归纳中…」、前台先用平铺形态、job 失败自动重跑+每小时兜底+开关重开触发、增量可见；改 §2.5 + R-26。**#10** MVP 阶段移除 slug 保留词黑名单。**#2/#14** 保留研发决定（module_enabled/display_mode 落表方式、collection_id 生成策略），不改。**#8/#9** 维度名唯一性锚定 en 源文本地留存、非空校验对象为实际文案（PRD 原已明确，未改）。共 20 处文本替换。原型 v0.19 同步「缺图项保存时拦截」文案 + 图文筛/快筛拖拽排序补全 + 缺图 mock 清理 |
| PRD v0.16（第二轮 AI 评审 + 分步必填/库存单值）| 2026-07-01 | 产品团队 | 依据第二轮 AI 评审 `prd-issues-v2` 及用户两项决策修订。**核验说明**:v2 评审中 A(R-06)/C(slug 保留词)/D-1(无条件=全量)/G(缺图校验层重叠)均为**误报**——上一轮已修，PRD 现状已一致，不再改动。落地真问题与新决策共 12 处:**问题1 分步必填**——新建/编辑向导每步必填未完成则「下一步」/「保存」置灰不可点(§2.3 Step1 解锁 + Step2 完成 + Step3 新增「分步必填总原则」blockquote)。**问题2 + D-2 库存单值**——stock_status 收敛为**单值「有货」(可售库存>0)**,控件改单选,去掉在库/在途/售罄/不限库存等值(『不限库存』=空筛会与无条件拦截冲突;二手孤品售出即移出无售罄态);操作符表新增「库存状态组」;#11 引导文案「在售」→「有货」(§2.1 字段行 + 操作符组 + §2.1 无条件拦截行 + Step2)。**B 缺图保存校验**——§2.4 保存校验段补「图文模式(graphic)缺图 active 项拦截保存」(保存即校验、不允许缺图存草稿;想先存可切纯文本或关模块),弹窗正文加缺图分支。**D-3 库存匹配依赖**——§5.1 库存模块依赖补「stock_status=有货 的匹配数据源」说明(取数方式待研发定,如实时查库存接口则对自动选品升 P0),§2.1 加取数注。**E en 源文字段**——§8.6 filter_dimension 新增 `dimension_name_en VARCHAR(100) NOT NULL UNIQUE`(本地 en 源文副本、专用唯一性校验、不实时查翻译服务),同步 §2.5 校验行、§4.6 唯一性例外说明(改为本地判重+DB 唯一索引双保险)。**F(分组 job 状态字段)/#2/#4** 按决策留研发/待 UI,不改。原型无需改(均为 PRD 侧口径与研发实现) |
| PRD v0.16（渠道放开 + 一级类目排序读维度 + 一属性一维度）| 2026-07-01 | 产品团队 | 三项产品决策。**Q2 放开选品规则渠道条件**——原「渠道条件锁定为基本信息步渠道、改需回基本信息步」改为**默认带入基本信息渠道作为初始值、运营可自行修改/多选/清空/跨渠道取品**（为未来跨渠道销售预留）;同步 §2.1 channel 行、§2.3（188 适用范围行 + 341 字段表 + 355 Step 说明）、R-32、§8.6 channel_id 注,共 6 处。**Q3 作用域分组「组间排序」一级类目排序改读维度配置**——原读商品端 `backend_category.sort_order` 改为**读筛选维度管理 `category` 维度中一级类目的展示顺序**（Pin+跟随合并次序）;组标题一级类目**文案**也优先读 category 维度前台展示名;**兜底**:category 维度不存在（未建/已删）时,排序退回商品端 sort_order、文案退回翻译模块译名>category_name 原值（排序+文案一起兜底）;同步 §2.5 组间排序、组标题多语言取数、R-26,共 3 处。**Q4 一属性一维度**——一个自定义 attribute 只能被一个筛选维度关联,已用属性在新建/编辑维度时不可再选（置灰+提示）,§8.6 filter_dimension_source 唯一约束由 `(dimension_id,attribute_id)` 收紧为 `(attribute_id)` 全局唯一;新增 R-42;**tag 数据源例外**——tag 从标签库勾选子集、支持建多个 tag 维度各勾不同标签作为不同筛选器,不走多源表、不受此约束。共 3 处。原型无需改（均 PRD 侧口径/校验,渠道放开如需原型去锁待后续原型版本同步）。注:上一轮问题1（分步必填）、问题2（库存单值有货）已在前一版落地,本次确认无需重复 |
| PRD v0.16 + 原型 v0.19（库存条件对齐上游改数值型）| 2026-07-01 | 产品团队 | 交叉核对 `商品系统-PRD-v1.7` 与 `库存管理-PRD-v1.2`:两者均**无 stock_status 状态字段**——商品系统只提供可卖判定(`active AND available_qty>0`)与派生展示态、库存模块是纯数量模型(可售库存=max(0,在库-预占-锁定))。原 stock_status 单值「有货」的字段来源是伪造的。据行业(Shopify inventory_stock ≥/≤ N)与二手孤品场景,将选品条件由 `stock_status=有货(单值枚举)` 改为 **`available_stock 可售库存(数值型)+ ≥/≤ 运算符**,读库存模块可售库存值;「选全部在售」= `可售库存 ≥ 1`。PRD 改 5 处:§2.1 字段行(改数值型+口径注)、§2.1 运算符表(删库存状态组、并入数值组 ≥/≤)、§2.1 无条件拦截引导、§2.3 Step2 引导(均 有货→可售库存≥1)、§5.1 库存依赖(匹配数据源改 available_stock、取数方式待研发、实时查则升 P0)。原型 v0.19 改 2 处:RULE_FIELDS 的 stock_status→available_stock、RuleBuilder 新增 available_stock 分支(≥/≤ 下拉 + 非负整数 InputNumber + 『件·读库存模块可售库存』提示)。括号配平 (2301) JS 自查通过 |
| 原型 v0.19（选品条件运算符全面对齐 PRD）| 2026-07-01 | 产品团队 | 全面比对原型 RuleBuilder 运算符与 PRD §2.1,修正 5 处不一致(此前多个字段走 else 兜底=in/nin+文本框,与 PRD 语义不符)。**listing_date 上架时间**:改专用分支——运算符「最近 N 天 / 介于」,前者=数字+天(动态滚动窗口)、后者=RangePicker(补 DatePicker 导入)。**tag 标签**:运算符改「包含其中之一/不包含其中任一」(OP_MULTI),值改标签多选(读 COLLECTION_TAG_POOL 渠道+通用标签)。**title 商品标题**:运算符改「包含/不包含」(OP_TEXT)+关键词模糊匹配文本。**custom_attribute 自定义属性**:补二级交互——按属性 input_type(ATTR_META)动态切运算符+值控件:single_select=等于其中之一+枚举多选、multi_select=包含+枚举多选、number_input=区间/≥/≤+数字、text_input=包含+文本。**condition 成色**:值控件由文本改枚举多选(COND_OPTS 示意值,枚举以商品系统为准)。新增 ATTR_META/COND_OPTS/OP_MULTI/OP_TEXT/OP_NUM 常量。demo 规则值经 r.val 兼容回显。brand/series 运算符本就正确(in/nin)保留,值控件仍为文本(可接受简化)。括号配平(braces/parens/brackets/fragments 全平衡)、script 7/7、JS 自查通过 |
| 原型 v0.19（运算符受控修复 + 品牌系列搜索多选）| 2026-07-01 | 产品团队 | 修两处交互 bug。**A. 运算符默认值/回显与下拉枚举不一致**——根因:多个运算符 Select 用非受控 `defaultValue` 且不读 `r.op`,切换字段时旧 op(如 brand 的 'in')残留,导致「上架时间」显示「等于其中之一」但下拉却是「最近N天/介于」。修复:①全部运算符 Select 改受控 `value={r.op}` + onChange 回写;②新增 DEFAULT_OP/ATTR_DEFAULT_OP/defaultOpFor,字段或属性 onChange 时按目标字段类型重置 op 并清空旧值(val/vals/conds/tags/channels/cats/min/max/days);③price 运算符固定「区间」补受控只读 Select;④修 demo 规则 color(multi_select) 的 op 由 in 改 contains_any。**B. 品牌/系列值控件由文本框改搜索多选**——新增 BRAND_OPTS/SERIES_OPTS,brand/series 走独立分支:运算符等于其中之一/不等于其中任一 + showSearch 多选下拉(optionFilterProp=label),对齐 PRD『搜索多选/文本多选』。括号配平(braces/parens/brackets/fragments 全平衡)、script 7/7、JS 自查通过 |
| PRD v0.16 + 原型 v0.19（渠道 placeholder / 变更历史抽离 / All Products 废弃）| 2026-07-01 | 产品团队 | 四项。**Q1** 原型渠道条件 placeholder「选择渠道(可多选、可跨渠道)」去掉「可跨渠道」→「选择渠道(可多选)」。**Q2** 确认 available_stock(可售库存 ≥/≤)运算符与逻辑上一版已入 PRD(§2.1 字段行/运算符表、无条件引导、Step2、§5.1 依赖),无需重复。**Q3** PRD 开头 14 条历次版本变更(v0.15→v0.16 … v0.2→v0.3)整体抽离到独立文件 `looply-类目管理-PRD-版本变更说明.md`(最新在最上),正文仅留版本/日期/状态/合并说明/目标读者 + 一行指向;『版本要点(v0.1→v0.2)』整段删除不保留;正文加『历次版本变更见独立文件』指引。**Q4** 原型已无 All Products 固定项,PRD 彻底废弃系统级 Collection,删 9 处:超管职责、术语表整行、#11 无条件拦截『避免与 All Products 重复』理由、列表操作列『系统级无删除』、列表特殊规则『始终显示/仅配置』、§2.4『All Products 特殊规则』整块(标题+可编辑表)、§5 P0 项『All Products 系统级』、R-07 整条、§8.6 is_system 字段;R-07 编号保留删除线占位(不复用,避免 R-08+ 交叉引用错位);图文筛/快筛『无系统级「All」项』属不同概念(无全部 chip)保留。原型 JS 无影响 |
| PRD v0.16 + 原型 v0.19（PC 端基础信息 UI 规范）| 2026-07-01 | 产品团队 | 补充 PC 端基础信息字段的 UI 规范与校验(仅 PC 改,移动端保持现状)。**主标题**:PC/移动端均最大 30 个英文字符(超出拦截+字数计数)。**副标题**:PC 最大 100 个英文字符、限两行(后端限 100 字符;前端每行最多 60 字符**整词换行、不拆词**——临界单词不拆开;超两行不接受),移动端保持 30 字符单行。**Banner(PC)**:由 16:9(1600×900)改为**固定 1280×210(宽高比约 6.1:1),仅支持等比放大/缩小上传,其它比例上传时校验拒绝**;移动端保持 2:1(1080×540)。PRD 改 10 处:§2.3 标题/副标题/Banner 字段行、§2.4 校验表新增 3 条(标题≤30、副标题≤100且≤两行、Banner PC 比例校验)、§8 常量表(副标题/Banner)、§8.6 数据模型 subtitle/banner_url 注、§3.12 PC 前台 Banner 描述 2 处(16:9→1280×210)。原型改:TextColorField 组件新增 multiline 变体(PC 副标题用 TextArea、autoSize 2 行、wordBreak:normal 整词换行、100 字符 + 每行 60 提示),NewPage/DetailPage 基本信息区主标题加 maxLength 30+showCount、PC 副标题走 multiline 100、PC Banner UploadBox 改 192×31 缩略 + hint『PC 横图 1280×210,仅等比缩放』,FormRow helper 加『仅支持等比例缩放上传,其它比例不接受』。括号配平、script 7/7、JS 自查通过 |
| PRD v0.16 HTML 同步 | 2026-07-01 | 产品团队 | 从最新 `looply-类目管理-PRD-v0.16.md` 重新生成 `looply-类目管理-PRD-v0.16.html`(193KB,目录 74 项),保留原模板(head/侧栏/content/尾部 IntersectionObserver 高亮 script)。同步进 HTML:①版本变更历史整体抽离(侧栏与正文『版本要点(v0.1→v0.2)』『vX→vY 变更』块已移除,加指向 `版本变更说明.md` 一行);②All Products 系统级 Collection 废弃(仅余 R-07 删除线占位,已用 <del> 渲染);③PC 端基础信息 UI 规范(主标题 30、副标题 PC 100+两行、Banner PC 1280×210 仅等比,旧 16:9/1600×900 清零)。slug 算法:删标点(含 .)、空格→-、小写,与原 HTML 锚点一致。结构自查:单例标签唯一、div 2/2 平衡、首尾标签完整 |

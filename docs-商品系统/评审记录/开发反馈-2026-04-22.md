## 开发反馈处理记录

> 日期：2026-04-22
> 反馈基准：交付开发 20260420（原型 v14 + PRD v0.2）
> 当前最新：原型 v22 + PRD v0.4

### 问题清单

| # | 分类 | 问题 | 处理方式 | 修改文档 |
|---|------|------|---------|---------|
| 1 | 需求澄清 | SKU 批量生成功能缺失，运营需逐个手建 SKU | 已回复：功能已在交付范围（PRD 2.10.2 SPU 新建页笛卡尔积矩阵），无需逐个手建 | — |
| 2 | PRD 问题 | pending_review 状态存在但缺审核流程页面 | 已回复：交付版清理遗漏，最新版已修正（审核→核验），需同步最新版 | —（同步新版） |
| 3 | 需求澄清 | 快捷上架缺规则定价（基准价±N%、竞品比价等） | 已回复：规则定价排在后续版本 V0.4（渠道定价规则+多币种），当前 MVP 只支持基础挂牌价 | — |
| 4 | 设计稿+PRD | 列表页筛选器数量/类型不统一，默认排序未定义 | 已修改：PRD 全局约定补充默认排序（按创建时间倒序）+ 空值显示规范（空值显示"—"） | PRD v0.4 |
| 5 | 技术建议 | 颜色/材质等视觉属性缺色块预览、富数据 | 已回复：排入后续版本，需配套前台展示一起规划，当前用文本命名区分 | — |
| 6 | 设计稿 | 所有列表页缺空态设计（零数据/无匹配/异常/权限） | 已修改：新增通用空态规范（3种空态），写入 admin-ui-spec + 原型脚本封装模板函数 + PRD 全局约定更新 | admin-ui-spec + gen_prototype_v3.py + PRD v0.4 |

### 修改汇总

- **PRD v0.4**：全局约定新增"默认排序规则"（按创建时间倒序）、"空值显示规范"（空值显示"—"）、空态分类说明（零数据引导态/筛选无匹配态/网络异常态），替换原来笼统的"暂无数据"描述
- **looply-admin-ui-spec.md**：新增 6.13 空态组件章节，定义 3 种标准空态的布局、间距、字号、色值、CTA 按钮规范 + 预留权限不足态；更新列表页模板结构加入空态位置
- **gen_prototype_v3.py**：新增 3 个空态模板函数（empty_state_zero_data / empty_state_no_match / empty_state_error）+ 对应 CSS 样式

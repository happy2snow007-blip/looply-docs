# looply 红布林商品对接 · PRD 修订记录

| 版本 | 日期 | 修改人 | 修改内容 |
|---|---|---|---|
| V1.0 | 2026-07-20 | 产品 | 首版。覆盖红布林商品对接 ①~⑥ 全流程（同步留底 / 系统自动加工 / 标品对齐+创建实体+自动上架 / 红布林状态同步 / 商品信息变更 / 价格变更商品列表）+ 数据维护三页（主数据映射 / 翻译术语库 / TRR 参考价）。不含订单履约链路（已拆分为「红布林订单对接」模块）。<br><br>基于系统流程图 V12.1、原型 v15、业务操作说明书 v1.6、商品 ER v2.6 编制。<br><br>**首版包含的三项结构性决策**：① 新增「款型ID」字段，1:1 对齐商品系统标品SKU，未命中时系统自动创建 SPU+标品SKU；② 取消人工确认上架（原为 Shopify 限流产物），创建后自动校验即上架；③ 营销选品/批量打标归属改为商品系统。<br><br>**自检修正**：价格变更列表 KPI 补全为 5 项并去除"等"的模糊表述、列名对齐原型、补充"无独立操作列"说明；新增 `list_err`「上架校验异常」枚举值（原 PRD §2.1.4 有该异常类型但 §2.1.2 枚举漏列，原型同步补齐为 v15）。<br><br>**终稿审核修正（与业务操作说明书 v1.6 交叉比对后）**：① §2.2 页面布局 KPI 项数笔误 7→8（对齐原型与下方表格）；② §1.5/§2.10 注明 18 个 Tab = 15 类术语对照 + 3 类拼接规则，避免开发按 18 类术语建表；③ 用词以原型 v15 为准统一——页面「价格变更商品列表」、单条记录「价格变更待办」（原「价格变更记录」全部改回），§2.8 补用词口径说明；④ §2.5.2 补 `value_level=ITEM` 属性值的落库表 `product_attribute_value`。<br><br>**★翻译与映射改为串联（核实 v3.1.2 业务规则后的结构性修正）**：原设计中映射表以中文原始值 `source_value_cn` 为 key，与翻译并联，导致同一中文值在术语库与标准记录处存在两个英文来源、可能漂移（title 显示 `Navy Blue`、商详属性区显示 `Blue`）。核实 v3.1.2 `rules/03-1-listing.md` L52 与 `core-business-rules.md` L8 后确认：老系统链路为 `4.3 术语库 → 4.2 翻译 → 3.1 用英文 Category L3 路由 template / taxonomy GID`，**查 ID 的 key 一直是翻译后的英文值，翻译是映射的上游**。据此改为串联：① 映射 key 由 `source_value_cn` 改为 `source_value_en`（②翻译产出）；② 术语库 `target_en` 对结构化字段须填商品系统标准记录 `name_en`，不可自由发挥；③ 归一职责业务上移交术语库，但**映射表多对一结构保留**作兜底，系统不强制；④ 新增一致性告警（`source_value_en` ≠ 目标记录 `name_en` 时标黄提示，不阻断）；⑤ §1.6/§2.5.4/§2.9/§2.10 的「正交」表述全部改为「串联/上下游」。定价链路不受影响（②查 TRR 仍按标准名匹配，不依赖映射）。<br><br>**串联调整的下游同步**：① **原型 v15 → v16**（19 项：主数据映射四个 Tab 首列改「英文标准值」并各增「来源中文」列、新增「一致性」列、编辑弹窗、KPI、批量导入、说明块口径，翻译术语库补 `name_en` 约束与归一说明；示例数据保留一致性告警与多对一兜底演示）；② **系统流程图 V12.1 → V12.2**（5 处必改：脚本 L54/L145/L196 的「正交」表述改串联，L264 流程节点「重新映射+重新翻译」**顺序纠正**为「先重新翻译→再重新映射」、L274 同步——该顺序错误在并联口径下潜伏、串联下成为实质错误，且流程图与 PRD §2.7 本就不一致；另补串联链路说明。一致性告警不上图，因其为页面列级提示、不构成流程分支）；③ 本 PRD 内引用同步更新为原型 v16、流程图 V12.2。<br><br>**★新增 §2.1.5 规则源变更处理机制**：原 PRD 只设计了规则源的「补齐」（异常→正常，重跑异常态商品），未定义「修订」（正常→改值）与「删除」对存量商品的影响，且串联架构下改术语英文等于改映射 key，会同时产生「新值查不到」与「旧值成孤儿行」，表现为新商品批量进映射异常却与运营操作无可见关联。本次按**全自动、无人工确认**原则补齐：① 确立前置铁律 **`url_slug` 一经生成永不变更**（对外契约，C 端收藏链接/外链/SEO 依赖；这是重算可全自动的前提，否则每次重算都需人工评估 SEO 影响），§2.5.4、§2.7 同步回指；② 变更分补齐/修订/删除三类，各自定义处理；③ 修订场景给出**重算范围规则**（未创建 product、已创建未上架、已上架均自动重算内部字段；已排除、已售出/已失效不重算，历史保真）；④ **术语英文变更自动联动更新映射 `source_value_en`**（新值不存在则就地改 key 保留 `target_id`，已存在则合并到已有行、原行标记失效），消除失配与孤儿行；⑤ 删除的影响面提示区分「未创建 product（回异常态）」与「已创建 product（不回滚）」；⑥ 重算异步批量、复用失败阈值、不改 `product_status`、需支持断点续跑。§1.4 场景5、§2.9、§2.10 同步改写。<br><br>**★§2.7 实体归属变更改为 product 重建（全自动）**：品牌/系列/类目挂在 SPU 上，product 只挂 `standard_sku_id` 经 `standard_sku→spu` 继承，故三者任一变更等价于**该实物商品需重新对齐到另一个标品SKU**（必要时新建 SPU + 标品SKU），性质是换了标品归属而非改几个字段。原设计「重新翻译+重新映射→**更新** product（需人工确认）」改为 **重建 product、全自动无人工确认**：新 product 挂新 `standard_sku_id` 并继承一物一码字段；旧 product 置「已失效」、旧 listing 下架、旧库存清零，**均不删除**保留追溯；快照层 `product_id` 回写指向新 product；新 listing 生成**新 url_slug**；ITEM 级属性值**不迁移**（新类目/品牌的 `attribute_scope_config` 作用域不同，按新作用域重新落值）。**只变类目也重建**（作用域按 `category_id` 配置，类目变则属性集整套变）。**已售出/已失效不重建**，转异常人工介入（已被订单引用；已售商品改品牌属业务事故）。<br><br>**连带收窄 url_slug 铁律适用范围**：§2.5.4 / §2.1.5 原写「规则源变更与⑤信息变更均不改动 slug」范围写宽了——铁律约束的是**同一 product 的字段重算**（§2.1.5），§2.7 是**换了一个 product**，新 slug 属预期行为。两处均已补「适用边界」说明。<br><br>**下游同步**：系统流程图 **V12.2 → V12.3**（⑤段分支节点由 1 个扩为 3 个，描述重建全过程；注释补 SPU 归属/只变类目也重建/已售出不重建/slug 边界四条；「人工确认」表述清除）；PRD 内流程图引用同步 V12.3。<br><br>**★开发评审 ToDo 落实（2026-07-20）**：<br>**① TOP 系列数据来源**（原 PRD 全文无"爬虫"字样，只定义了页面维护）：§2.11 补「数据来源」段，明确 V1.0 链路为「**爬虫抓取 → 产出 Excel → 运营导入**」，**爬虫不直连写库**（爬虫依赖三方站点结构、稳定性不可控，直连一旦抓错会直接污染定价且因"算得出结果"而不易察觉；走导入保留人工确认与导入历史，可定位可回滚）。TOP 系列的使用位置与路由逻辑原已齐全（②定价：Top+C → 公式3/1/2/4/5/7，非Top/无C/宝格丽 → 公式6），本次未改。<br>**② TRR 导入改为按品牌全量覆盖**：原 PRD 无导入策略，原型为 `replace/keep` 二选一即**按 key upsert**，文件中不存在的旧行会残留、四维匹配可能命中过期行导致 C 值错误。改为**导入文件声明品牌范围、该品牌下旧行全删后重建**（不做整表覆盖以免误删其他品牌）；补文件内四维查重、导入结果展示新增/更新/删除/无效四项计数。<br>**②-顺带** §2.9 补「批量导入」（原型早有完整弹窗，PRD 页面元素漏写）：映射表为增量维护的规则源，采用**按 key 覆盖（upsert）**，与 §2.11 的全量覆盖策略差异已在文中说明。<br>**③ 款型ID 定义消歧义**：评审暴露"款型ID"易被误读为品牌系列。§1.6 / §2.5.1 明确其为**红布林款型库的款型标识**（红布林侧已有的标品主数据，一个款型 = 一个具体规格的标准品），**不是 series**——系列是 SPU 级营销归类、一个系列下有多个款型，款型是 SKU 级具体规格，故与 looply 标品SKU 天然同粒度。**R1 据此重写**：概念层 1:1 已成立，剩余风险收窄为"红布林实操中是否存在粗粒度维护的款型"，应对由"可能整体返工"降级为"局部改用款型ID+销售属性定位，不推翻整体设计"。<br>**④ 定价数据依赖 — 按决策不修正**：§2.4 降级规则（无 C → 公式6 等）维持原样，**未引入基于数据日期的自动降级**。§2.11 补充的 `样本数 / 数据日期` 两列仅同步原型既有字段、供运营判断可信度，**不参与定价逻辑**，文中已显式声明。<br>**⑤ 标品SKU 规格要求 — 按决策暂不处理**：与 R5「属性作用域配置覆盖不足」合并，待与商品模块对齐 `attribute_scope_config` 的 SPU/SKU/ITEM 三档归属后再补 §2.5.2。（配件已明确为 **ITEM 级**、不参与标品SKU 规格，原 PRD 已定义，本次未改。）<br>**下游同步**：原型 **v16 → v17**（TRR 导入弹窗改按品牌全量覆盖 + 品牌范围必选、页面副标题补数据来源、导入历史增「品牌范围」「删除」两列）；系统流程图 **V12.3 → V12.4**（③段补款型ID 定义两条注释）；PRD 内引用同步 v17 / V12.4。<br><br>**★菜单 IA 重构（方案A · 对接层命名空间，2026-07-21）**：原菜单为「红布林商品库（首页）+ 数据维护（分组）+ 价格变更商品列表（孤儿一级页）」，存在两处不合理——① 价格变更列表孤零零一个一级菜单无 peer；② 语义不明（「数据维护」泛、页面裸挂顶层）。因规划将本模块并入商品系统，据 §1.2 跨模块归属判据重构为**对接层命名空间**：`商品系统 → 红布林货源 → [红布林商品库、调价处理、对接规则 → (主数据映射、翻译术语库、TRR 参考价)]`。命名：分组「红布林货源」、「数据维护」→「对接规则」、「价格变更商品列表」→「调价处理」（表名 `plm_price_change_todo`、记录名「价格变更待办」、组件 `PriceTodoInner`、page key `price-todo` **均不变**，仅页面显示名变）；「红布林商品库」「主数据映射」保留原名（用户指定）。TRR 参考价 V1.0 暂放本分组，其上移商品系统「商品定价」域的可能性列入后续评估（商品系统 v40 原型已有「商品定价」分组）。§1.5 由「全局页面流转」升级为「后台菜单地图与页面流转」。**下游同步**：原型 **v17 → v18**（menuItems 三级重构、defaultOpenKeys、面包屑改「商品系统 / 红布林货源 / …」、header 归属、调价处理页 H1 与弹窗标题更名，JSX parse 通过）；PRD 内原型引用 v17→v18 共 18 处。流程图不涉及菜单结构，保持 V12.4 不变。<br><br>**菜单更名 v19（2026-07-21）**：「调价处理」→「**商品调价处理**」（用户指定）。原型 **v18 → v19**（菜单标签 + 页面 H1 + 两处弹窗标题；内部 key `price-todo`/表名 `plm_price_change_todo`/组件 `PriceTodoInner` 不变）；PRD 命名同步 10 处 + 原型引用 v18→v19。附：本次校验已加入「未声明图标差集检查」——v18 曾因新增 `ShopOutlined` 图标未加入解构块导致运行时 `<undefined />` 崩溃、页面卡在 loading（babel parse 不报运行时 ReferenceError），v19 起该检查与 JSX parse 一并执行，结果无未声明图标。<br><br>**★决策：本模块不单独出 ER 图（2026-07-21）**：原 §1 输入源说明、§2.1 节说明、§3.2 风险 R3 三处均写「ER 图待补 / 开发前补出」，现按决策统一为**不出 ER 图**——快照层/款型ID映射表/主数据映射表/翻译术语库/TRR 参考价等实体的字段定义**以 PRD §2.1 为数据模型权威输入**，表结构/索引/约束由**技术方案保证**；映射产 ID 的目标实体（品牌/系列/类目/属性标准记录）属商品系统既有 ER v2.6，本模块只引用不重画。R3 由「待办风险」改为「已决策约定」，保留一条落地要求：开发前与商品模块核对映射目标实体与 ER v2.6 一致，避免外键落点冲突。此口径与登录注册模块 [待优化清单事项3「不建 ER，技术保证」] 一致。待优化清单第 12 条权威版本快照同步更新（原型 v17→v19、ER 待补→不出 ER）。<br><br>**★R1 风险关闭（已验证，2026-07-21）**：取得红布林实际数据抽样验证，确认款型ID 为红布林款型库的 **SKU 级标识**，同一款型ID 下颜色/尺码唯一，**款型ID ↔ 标品SKU 1:1 假设坐实**。§3.2 风险表 R1 由 open 改为「已验证关闭」并保留作追溯；无需引入「款型ID + 销售属性」两级定位，③标品对齐按现设计执行。对应开发评审 ToDo「标品信息核查」闭环。至此本模块 open 风险清零（R3 已决策不出 ER、R1 已验证），无阻塞交付项。 |

| V1.1 | 2026-07-22 | 产品 | **吸收 2026-07-21 会议 22 问（OQ-001~022）+ 2026-07-22 基于原始规则 v3.1.2 核实的 R1~R8 最终修订**（权威依据：`looply-红布林商品对接-问题核对与解决方案-v1.md` §三/§四/§六；冲突时以 §六 R1~R8 为准）。产物版本引用同步：流程图 V12.4→**V13.0**、原型 v19→**v20**。<br><br>**【OQ-018 · §1.6/§2.4/附录7.4】汇率可配置 + 修量纲 + 定价错误阻断**：① `A=折扣价/汇率`，汇率**可配置**（V1 默认 6.86，不硬编码、不接实时汇率），每次存汇率值/版本可复现；② **删除**旧「折扣价(CNY)≤B(USD)」跨量纲直接比较（真实 bug），成本与挂牌价一律 **USD 维度**比较；③ B=ceil 整数、CostPrice 从第三位小数向上取整两位；④ 取整后 **A>B → 定价错误**，阻断成本/价格写入与自动上架，不截断成本伪造利润。<br><br>**【R4 · §2.4/附录7.4】删除"超 TRR→不选中"**：依 04-1，`B>1.10C`=公式7兜底=**仍选中**；"不选中(否)"收敛为 4 类（价格无效 / 养护·改色·边油重做 / 非目标包袋 / Speedy 低价例外）。<br><br>**【OQ-017 · 附录7.4 新增定价公式表】**：固化公式 1~7 + 公式6 + 非包袋，每条给稳定规则ID（PRICE-F1~F7/F6/NB）、适用条件（顺序命中·上界≤）、B 表达式、区间边界、低价修正（B≤500→B146，Speedy 例外）、示例输入输出、命中总路径。<br><br>**【1B · §2.1.3/§2.6】下架状态机重画**：普通下架→product_status **「待销售」(可逆)**（不再「已失效」）；重新上架**需重走上架校验**→「销售中」；「已失效」收窄为真正永久失效/删除终态。新增下架/售出/失效/重新上架 → Product/Listing/inventory 逐态映射表。<br><br>**【OQ-004 · §1.6/§2.5.3/§3.1】成本权威归 sourcing 域**：去掉 `listing.CostPrice` 与 `inventory.unitCost` 双写/三项回读；inventory 仅管数量与预占/锁定；成本经 sourcing 接口/投影读取。依赖表新增 sourcing 域行、库存模块行去 unitCost。<br><br>**【OQ-002/OQ-003 · §2.5.4】上架校验改组合**：由「5 项」改为**「商品域共享校验 + 红布林来源校验」两段组合**，汇总为统一可定位错误集合，每失败项返回 稳定错误码/问题字段/修正入口/是否可自动重试；slug 本期不作新增卡口（唯一性复用商品域，通用治理由商品域/SEO 独立立项）。<br><br>**【OQ-006 · §2.1.4/§2.2】list_err 修正入口**：上架校验异常「去修正」→ 携上下文/返回地址跳**渠道商品编辑页**，保存后重跑校验、通过恢复自动上架、失败更新原 list_err 不建重复渠道商品。<br><br>**【3A · §2.7】身份字段 vs 原地重算（基础版）**：身份字段=**品牌/系列/类目** 3 个 → 下架旧+重建新 product；其余全部属性 → 原地重算不重建。完整字段级矩阵 + 影响预览 + 运营确认闸门**留规则变更专项新 PRD（V2）**。<br><br>**【R1/R2/R3/R5 · §1.1/§1.4/§2.1.1(6)/§2.8】⑥商品调价处理重写**：① 比对基准=**系统建议价 B vs 当前 Looply 挂牌价**（B=公式(新折扣价)，折扣价是输入非比对对象）；② **取消「采纳/维持/自定义」决策层与自定义价**，运营流程=分波筛选→勾选本波 ready→执行本波（改到 B），「维持原价」=不勾选；③ 价格写入 4 决策保留；④ 降价「新划线价−目标价<$30」→ `invalid_discount` **硬性不执行**（保留 3.4，不改筛选项）；⑤ TRR 比对**仅信息展示**（公式7=高于市场参考价提示），**不设人工卡口**；⑥ 预览分类=可改价/无需改价/差价不足$30/未匹配无效。删除旧「采纳/维持/自定义决定表」「超TRR需人工」；`plm_price_change_todo` 删 `decision`/`target_price`。<br><br>**【R6/R8 · §2.9】主数据映射去审批**：取消候选值审批/处理流程（原 OQ-021），未匹配值由运营**手动新增映射**；映射修改**不走审批**，编辑给**影响范围预览** + 全部操作**留操作记录（OpLog）**。KPI 去「待申请新增标准值」、操作去「申请新增标准值」。<br><br>**【OQ-010 · §2.11】TRR 批次审计与错误恢复**：按本次文件涉及品牌**全量替换**（识别品牌集合→旧有效记录全失效仅留本次→未出现品牌保留→不做细粒度差量）；导入前校验+预览品牌范围与新旧数量、原子提交；批次版本号+审计（原始文件/导入人/时间/品牌集合/解析器版本/结果摘要）；错误批次可暂停参与新定价、上传正确文件同品牌前向修正；**不做一键回滚**；错误定价可回查命中的 TRR 批次。导入历史增批次版本/导入人/品牌范围/删除数/暂停列。<br><br>**【R7/决策4 · §2.12 新增章节】定价规则治理（简化版）**：新增「定价规则」运营页——**可读 + 可编辑（增改/调序/新增/删除）+ 操作记录**；DB 为权威、Nacos 分发已配置快照；**不做 影响预览/提交审批/发布版本/Steps 发布流程**（同一波人操作、变更直接生效、留操作记录）。完整审批发布治理留 V2。②定价规则不再写死代码，改读规则快照。<br><br>**【2B · §2.13 新增章节】自动化运行门槛与 SLA**：**不建**队列可视化页；列出需定义的 SLA 指标（吞吐/时延/积压/暂停恢复/对账/告警值班）；运行保障（队列/重试/积压/恢复）**归技术后台**；门槛未签字前自动建品/上架默认禁用。<br><br>**【OQ-009 · §2.1.4】重试分类**：技术瞬时错误指数退避≤3 次（同 来源记录+阶段+输入版本+错误指纹 累计）；确定性业务错误（中文残留/缺映射/图片缺失/价格非法/定价错误）进 process_err/map_err/list_err，修正后由变更事件触发**新处理轮次**（不沿用旧计数），审计用"重新提交处理任务"。<br><br>**【OQ-008 · §1.7/§2.10】翻译三层归属**：plm_glossary（中文→英文标准值）是产英文源文的**工具**留 sourcing；英文源文写入 Product 字段；后续多语言归**翻译模块**（`translation` 表 `resource_type='product'`，**尚未上线**），sourcing 不自建平行翻译卡；翻译模块未上线前 V1 先产英文、Key 注册待商品多语言方案。<br><br>**【OQ-020】主数据跨环境治理**：稳定业务键 + 版本化基线包——经确认**属技术/部署层，本 PRD 不含**，由技术方案处理。<br><br>**【一致项 OQ-011/012/013/016 部分】**：多渠道多市场预留、订单履约/批量打标非目标、术语库与映射串联 已一致沿用；OQ-016 完整矩阵留 V2（本期基础版见 3A）。<br><br>**版本规划同步**：§4.1 页面 5→**6**（新增定价规则）；§4.2 新增 规则变更专项 V2 / 定价规则完整治理 V2 / 翻译模块接入 三项。附录 §7.1 页面索引增定价规则行、全部 v19→v20；§7.3 相关文档 V12.4→V13.0、v19→v20、增会议核对文件。 |

| V1.2 | 2026-07-22 | 产品 | **业务方 6 项调整**。核心方向：**把技术实现细节从 PRD 剥离交技术环节** + **定价取整口径修正** + **多语言模块上线措辞纠正**。产物版本引用同步：流程图 V13.0→**V13.1**；原型保持 **v20**（仅清过时注释，无页面变化，按拍板不升版本号）。<br><br>**【1 · 删除 §2.1.1 数据实体与字段定义】**：6 张表（`plm_product_snapshot` / `plm_style_sku_mapping` / `plm_master_data_mapping` / `plm_glossary` / `plm_trr_reference` / `plm_price_change_todo`）整节删除——字段定义、表结构、索引、约束**统一交技术环节设计**。<br>**拍板：只删表，业务结论保留**——① 表内「多对一能力保留」「一致性告警不阻断」两条业务规则**搬迁至 §2.9 主数据映射**；② 「取消采纳/维持/自定义决策层」经核实 **§2.8 已完整表述**（功能描述 + R2 说明 + 页面元素注三处），无需搬迁；③ 正文中的表名/字段名（`plm_glossary`、`suggest_price_b`、`source_value_en` 等）**全部保留**作业务语义锚点。<br>**连带修改**：§2.1 标题「公共机制：数据实体与状态模型」→「**公共机制：状态模型**」；§2.1 节引言与文首「本模块不单独出 ER 图」导语改写为「不在 PRD 定义数据实体与字段，交技术环节设计并保证」；原引用 §2.1.1(3) 的 2 处改指 §2.9、引用 §2.1.1 的 1 处删除；**子章节重编号 2.1.2~2.1.5 → 2.1.1~2.1.4**（避免删除后编号从 2.1.2 起断档），15 处交叉引用用占位符法同步（§2.1.4→§2.1.3 五处、§2.1.5→§2.1.4 九处、§2.1.2→§2.1.1 一处），校验无 §2.1.5 孤儿引用。<br><br>**【2 · 删除 §2.13 自动化运行门槛与 SLA（机制型）】**：整节删除——SLA 指标（吞吐/时延/积压/暂停恢复/对账/告警值班）与「门槛未签字前自动建品上架默认禁用」的放量门槛不在 PRD 声明，运行保障归技术后台。**连带**：附录 §7.1 页面索引删「§2.13 无页面（SLA 门槛，2B 不建队列页）」行；§4.1 v1.1 吸收清单删「SLA 门槛（2B，不建队列页）」项。章节编号无需重排（§2.13 为末节）。<br><br>**【3 · 定价成本取整口径修正】**：`CostPrice = A = 折扣价/汇率`（USD）由**「从第三位小数向上取整到两位」改为「保留整数、小数位向上取整」**（如算得 100.1 → 定价 **101**）。落点 3 处：§1.6 术语表 CostPrice 条目、§2.4 阶段一定价第 5 步、附录 §7.4 量纲与舍入口径。<br>**核实未受影响项**：挂牌价 B 原即 `ceil` 到整数 USD，口径不变；「取整后 A>B → 定价错误阻断」判定逻辑不变；附录 §7.4 三条示例（7546/6.86=1100、3430/6.86=500、13720/6.86=2000）计算结果恰为整数，示例数值无需改动。<br><br>**【4 · 多语言翻译模块上线措辞纠正】**：已核实翻译模块**已上线**（多语言模块 PRD v3.1，交付包 `多语言-交付开发 V1.4-20260623`，§4.6「前端读取时的 Fallback 策略」存在、引用编号正确），原「尚未上线」表述已失实。<br>**拍板：只改上线状态，职责边界不动**——本模块仍只产英文源文，英文之外语种的译文生产与多语言 Key 注册归翻译模块按其规范执行，**不新增接入要求、不影响开发排期**。落点 **7 处**：§1.7C 已核实句、三层归属表「第三层」行、关键界定第 2 条（「翻译模块未上线前 V1 只产英文源文」→「本模块只产英文源文，译文生产与 Key 注册归翻译模块」）、Fallback 规则条（去掉「上线后适用」限定，直接引用多语言模块 PRD v3.1 §4.6）、§2.10 翻译术语库 OQ-008 定位澄清、§3.1 依赖表翻译模块行、§4.2 后续迭代「翻译模块接入」行（触发条件由「翻译模块上线」改为「商品多语言 Key 注册方案落地」——前者已达成）。<br>**注**：后 3 处（§2.10/§3.1/§4.2）为初次 grep 被截断遗漏、终检时发现补改，已全文校验「尚未上线/未上线」零残留。<br><br>**【5 · 删除「★ 重试策略分类（v1.1 · OQ-009）」】**：位于 §2.1.3（原 §2.1.4）统一异常处理机制下的分类表（技术瞬时错误指数退避≤3 次 / 确定性业务错误不盲目重试、计数归零、审计用「重新提交处理任务」）及其说明整体删除。<br>**连带改写**：该节「异常处理」由「技术瞬时错误超 3 次退避仍失败→标记人工介入并告警；确定性业务错误保持异常态直到修正」改为「三类异常各自保持对应异常态（`process_err`/`map_err`/`list_err`）直到修正事件到达，由运营或规则源修正后触发新的处理轮次；**技术层面的重试、退避与告警策略由技术环节设计并保证**」；§4.1 v1.1 吸收清单删「重试分类（OQ-009）」项。<br>**保留未删**：§2.1.3 异常处理表中「映射异常→系统定时自动重试（映射库更新后自愈）」、§2.5.4 上架校验四要素中「是否可自动重试」——均为业务规则而非技术重试策略，不在删除范围。<br><br>**【6 · 删除规则存储与分发的实现描述】**：§2.4 技术说明中「、**DB 为权威**、Nacos 分发已配置的规则快照；②加工流水线读取当前生效的规则快照执行。翻译/清洗等仍为后端流水线实现。」删除，仅保留「定价规则不再写死代码，以『定价规则』运营页（§2.12）维护，②加工流水线按当前生效的规则执行」。<br>**拍板：一并删 §2.12**——「权威与分发」表（DB为权威 / Nacos分发 / 变更生效 三行）改写为「**变更生效**」段落，仅保留「同一波人操作、变更直接生效（无审批/发布版本闸门）、每次变更留操作记录」业务规则，**规则的存储与分发方式由技术环节设计**。避免同一件事 PRD 里一处删一处留。<br><br>**下游同步**：① **系统流程图 V13.0 → V13.1**（数据驱动，改 `gen_product_flow_v13_1.py` 数据源后重新生成，未直接改 SVG）——②加工段定价节点文案改整数取整口径、删 2 处「★OQ-009重试」注释行（②加工段 + ③上架校验段）、脚本头部注释同步（OQ-018 取整描述、删 OQ-009 注释块含一行孤儿续行、移除「2B不建队列页/OQ-019 SLA门槛」提及）；rsvg-convert 渲染 2672×6436 目检两处改动区域，说明块无残留、无版式断裂。② **原型 v20 保持不变**，仅清定价规则页一处过时 CSS 注释（「决策4：完整可编辑规则引擎 · DB权威 + Nacos分发已审批快照」→「R7 简化版：可读 + 可编辑 + 操作记录，无审批发布流程」，该注释在 v1.1 降级为 R7 时即已滞后），不影响任何页面元素与渲染。③ PRD 内流程图引用 V13.0→V13.1 共 4 处（输入源、§2.8 用词口径、附录 §7.3、页脚）。<br><br>**对开发的净影响**：① **必改**——成本 CostPrice 取整由两位小数改为整数（向上取整）；② **责任转移**——数据实体字段定义、重试退避策略、定价规则存储分发方案三项由 PRD 声明改为技术方案自行设计，PRD 不再是这三项的依据；③ **无需改动**——多语言措辞纠正不改变任何接口与逻辑。 |

---

## 说明

- 本文件与 PRD 正文同目录，正文内不再保留修订记录章节
- PRD 版本号递增规则：小改动（补字段、改文案、加校验）→ 小版本；大改动（新增模块、重构章节结构）→ 大版本
- 每次升版**新建文件**，不覆盖历史版本
- 本模块尚未交付开发 V1.0，按 `req-versioning` 的「V1.0 前例外」规则，暂不建立模块级变更日志；首次交付时需补建 `looply-红布林商品对接-变更日志.md`

# 归档设计说明 → PRD 覆盖核查

> 核查日期:2026-07-27 · 核查人:晴天(营销活动模块核查员)
> 核查对象:`归档/折扣内容-金额与百分比设置逻辑说明.md`(v1.0 · 2026-07-25)、`归档/营销资损防控方案.md`(v1.0)
> 承载方:`PRD/looply-营销活动-PRD-v1.0.md`(v1.0 · 2026-07-26,全文核读)
> 取代判定依据:`营销活动模块-设计主线与落地计划-v1.0.md` §5 决策记录(A1-A6、D1-D7、2026-07-26 评论驱动变更)
> 核查口径:只核**结论性内容**(规则/机制/判定标准/职责边界/对账口径/防线清单);论证过程、举例演算、"为什么"的原理阐述不属于 PRD 必须承载项

**分类标记**:①已覆盖 · ②被更新决策取代(PRD 按新口径为正确) · ③真遗漏

---

## 一、文件 1:折扣内容-金额与百分比设置逻辑说明.md

| # | 归档结论 | 分类 | 承载/取代说明 |
|---|---|---|---|
| 1-1 | §0 判定规则:无量纲值放规则层全市场统一;绝对货币金额按币种显式录值 | ①+② | 原理有效部分("绝对货币金额按币种显式录值、无实时汇率")→ PRD §2.1.1 全局约定;"下沉市场层/promotion_market 逐市场录值"的结构性表述 → **被 A6 取代**(2026-07-27:活动-市场 1:1,金额单币种上提规则层,promotion_market 已删) |
| 1-2 | §1 value_type(percentage/fixed)二选一 + percentage_value 无量纲统一,归"折扣内容"分区 | ① | PRD §2.2.2 折扣内容分区(value_type/percentage_value 字段与规则一致) |
| 1-3 | §1 "固定金额不在折扣内容分区出现,必须放投放市场分区"的表单边界 | ② | 被 A6 取代:单市场化后 fixed_amount 回归折扣内容分区,带所属市场币种符号录值(PRD §2.2.2),语义仍是"按币种显式录值",无冲突 |
| 1-4 | §1/§附 折扣内容 vs 投放市场职责边界表(promotion_market 一市场一行,currency_code/fixed_amount/min_amount/max_discount_amount 下沉) | ② | 被 A6 取代:promotion_market 表已删,"投放市场"分区改为"所属市场"单选(第一字段、创建后锁定),全部金额字段按所属市场币种(PRD §2.2.2 基本信息分区);字段归属以 ER v1.5 为准 |
| 1-5 | §2 系统不做实时汇率换算、金额一律录值、系统无能力从基准币推导他币金额 | ① | PRD §2.1.1"系统不做实时汇率换算,买家下单/结算/核销全程不用汇率"+§2.8 规则说明"所有金额计算按市场币种,无汇率参与"+§三 汇率模块仅作运营录值参考。原理阐述本身(购买力差异等)属论证,不要求 PRD 承载 |
| 1-6 | §2/§3 演算举例(美/英/加三市场、8 折/固定减) | — | 举例演算,非结论性内容,不要求承载;且三市场并行录值场景已被 A6 单市场模型取代 |
| 1-7 | §4.1 max_discount_amount 是绝对货币量,须按币种录值(percentage 专用封顶) | ①+② | 值性质与封顶机制 → PRD §1.6 术语"按市场币种录值"+§2.2.2(percentage 时可配,带币种符号)+§2.8.6 封顶参与单件计算;"下沉 promotion_market"的层级 → 被 A6 取代(上提规则层单币种) |
| 1-8 | §4.2 min_amount 满额门槛是绝对货币量,按币种录值 | ①+② | PRD §2.2.2(min_amount 带币种符号,按所属市场币种)+§2.8.3 门槛判定;层级同上被 A6 取代 |
| 1-9 | §4.3 量纲判定统一原则(带货币单位→按币种录值;不带→统一值),可复用到后续营销字段 | ① | 作为设计原则由主线文档 §2.3 核心设计原则第 1 条承载(概览为方案总纲);其对 V1 的全部落地结果已进 PRD:percentage_value/min_quantity 全市场统一(§2.2.2),金额字段全部按所属市场币种(§2.1.1)。A6 后"规则层 vs 市场层"二分坍缩为"金额按所属市场币种录值",PRD 口径正确 |
| 1-10 | §5 设计收益(资损防控/运营精细度/口径一致/可扩展) | — | 收益论证,非结论性内容,不要求 PRD 承载 |

**文件 1 小结**:10 项中结论性 8 项——①覆盖 5 项(含 3 项复合)、②取代 3 项(全部对应 A6),③真遗漏 **0 项**。

---

## 二、文件 2:营销资损防控方案.md

### 2.1 三道防线框架与七条铁律(特别核查项)

| # | 归档结论 | 分类 | 承载/取代说明 |
|---|---|---|---|
| 2-1 | §2 三道防线框架:事前拦截/事中兜底/事后监控 | ① | PRD §1.1 目标 3 逐字对应:事前(配置试算拦截+封顶/限次)、事中(实付兜底+核销原子扣减+到手价监控)、事后(退款回滚+日对账+一键熔断);§1.4 核心场景 4/5 落为流程 |
| 2-2 | P1 实付永不为负、不低于保护线(V1 至少 0,进阶成本线) | ① | A2 拍板纳入 V1(保护线取 0):PRD §2.8.8"实付=max(应付−优惠,0),订单计算引擎统一夹逼执行一次"+§2.6"Total 不会低于 $0"。成本线为归档文中"进阶(依赖商品成本数据)"选项,未拍板入 V1,见 §四备注 B3 |
| 2-3 | P2 金额一律按币种录值、杜绝汇率资损 | ①+② | 原理 → PRD §2.1.1(同 1-5);"promotion_market 分币种存"结构 → 被 A6 取代 |
| 2-4 | P3 核销幂等+原子扣减+唯一约束 | ① | A2 拍板:PRD §2.9.1——(rule_id,order_id) 唯一约束防重复、计数校验与自增同一原子操作、达 total_usage_limit 拒绝、事件重试幂等。归档建议约束键 (码/规则,订单,用户),V1 1:1 模型+单码单订单下 (rule_id,order_id) 等价成立,口径正确 |
| 2-5 | P4 封顶与门槛前置配置、危险值二次确认 | ① | 封顶能力:PRD §2.2.2 max_discount_amount 可配;门槛:min_type/min_amount/min_quantity;危险值确认升级为**保存前价格试算拦截**(§2.2.3,超阈值商品标红警示+确认才落库,2026-07-26 评论变更 ④ 新增),强于归档的"二次确认" |
| 2-6 | P5 叠加口径显式定义、默认从严(同类不叠加取优惠最大) | ①+② | D4 拍板为折扣级布尔开关 can_combine(默认不可叠加):PRD §2.2.2 叠加策略+§2.8.5(不可叠加互斥取整单优惠最大一条;可叠加按金额从大到小顺序作用)。归档"叠加是 V2 显式能力"被 D4 取代(V1 即有开关,类别模型 V2),PRD 按新口径正确 |
| 2-7 | P6 退款回滚保证限次与对账一致(status valid→reverted、释放限次) | ①+② | D3 拍板细化口径:**整单退款**回滚(status→reverted+reverted_at、used_count−1、限次释放),**部分退款不回滚不重算**——PRD §2.9.2 + §1.2 边界声明。归档"退款/取消事件驱动"中支付后取消并入退款链路,支付前取消释放码占用不产生核销(PRD §2.6),口径完整 |
| 2-8 | P7 可观测、可急停(status=paused 熔断,止损优先于查因) | ①+② | 熔断:PRD §2.1.4 状态机 paused+§2.3.2 排查抽屉一键暂停(即时止损,处置留痕);可观测已**升级取代**归档口径:告警落表 promotion_price_alert+双阈值监控(累计 50%/环比 20%,PRD §2.3.1 告警类型表),取代归档"监控不建表"口径,PRD 按新口径正确 |

### 2.2 防控机制映射与 V1 落地清单(§3、§6)

| # | 归档结论 | 分类 | 承载/取代说明 |
|---|---|---|---|
| 2-9 | 最大优惠封顶机制:单次让利=min(应付×比例,封顶) | ① | PRD §2.8.6:减免=round(现价×比例)后按 max_discount_amount 封顶;层级归属被 A6 取代(同 1-7) |
| 2-10 | 限领限用:total_usage_limit+per_user_limit+used_count,达上限拒绝 | ① | PRD §2.2.2(两字段+编辑不得小于已核销)、§2.8.4 限次判定、§2.9 计数口径(仅统计 valid) |
| 2-11 | 时区口径 timezone_mode | ② | 被 D5+A6 取代:活动起止时间按所属市场本地时区判定,单市场化后 timezone_mode 字段废弃、无需配置(PRD §2.1.2 明确标注"D5 更新") |
| 2-12 | 优惠码随机生成防猜(≥8~10 位、字母数字混合随机,防 SAVE001 类枚举) | ①+② | D6 拍板修订为"手动输入+随机生成两种方式,手动码防猜校验":PRD §2.2.2 code 字段——随机生成按钮、长度≥8、字母数字混合、禁纯连续序列、存储归一大写;(market,code) 组合唯一为 A6 新增。归档"仅随机生成"被 D6 放宽(手动亦可但过防猜校验),PRD 按新口径正确 |
| 2-13 | 门槛校验(满额按币种、下单达门槛才生效) | ① | PRD §2.8.3(满额按市场币种合计/满件判定,不达门槛剔除)+§2.6 校验失败枚举"未达门槛" |
| 2-14 | V2 规划五项:预算封顶/上线审批流/人群定向/跨活动叠加引擎/多账号识别 | ① | PRD §1.2 不做项逐条声明(预算字段预留、审批流 V1 以试算+监控替代、eligibility V2、叠加类别模型 V2)+§四 V2 方向,边界口径一致 |

### 2.3 跨模块职责边界表(§4,特别核查项)

| # | 归档职责行 | 分类 | 承载/取代说明 |
|---|---|---|---|
| 2-15 | 规则定义与存储唯一来源=营销 | ① | PRD §2.2 配置表单+§2.8"营销域对外供给的统一计算能力";promotion_market 已删由 ER v1.5 承接 |
| 2-16 | 优惠命中判定+金额计算+行级分摊 主责=订单计算引擎 | ② | **被 2026-07-26 评论驱动域边界二次修正取代**:分摊规则定义+分摊计算执行均归营销,订单只落地存储 order_discount——PRD §2.8(营销输出行级分摊明细)+§三 依赖表 ①(订单按营销返回的分摊落 order_discount),PRD 按新口径正确 |
| 2-17 | 实付兜底只在订单侧执行一次,避免双重夹逼 | ① | PRD §2.8.8"由订单计算引擎统一夹逼执行一次"+§三 依赖表 ②"仅执行一次",口径原样保留(A2) |
| 2-18 | 核销写入归营销,由订单支付成功事件触发;回滚由订单退款事件驱动、营销被动响应 | ① | PRD §2.9 触发条件与处理流程完全对应+§三 依赖表 ③事件必达(至少一次+幂等) |
| 2-19 | 成本保护线数据主责=商品模块 | ② | 被 A2 取代:V1 保护线拍板取 0,不依赖商品成本线,故 PRD §三 商品域依赖未列成本线,口径自洽(成本线兜底为将来增强,见 §四备注 B3) |
| 2-20 | 多账号/羊毛识别主责=用户风控,营销领取时调用 | ① | V2 边界:PRD §1.2/§四(依赖风控),与归档分层一致 |
| 2-21 | 金额币种一致性:订单必须用与订单币种一致的规则,取不到即不生效 | ② | 被 A6 取代为更强约束:活动仅属一个市场,候选集=所属市场=买家当前市场(PRD §2.8.1),币种错配在模型层被消除,PRD 按新口径正确 |

### 2.4 事后监控与对账(§5,特别核查项)

| # | 归档结论 | 分类 | 承载/取代说明 |
|---|---|---|---|
| 2-22 | 监控指标清单(5 项):核销增速/优惠总额/异常订单率/单用户核销集中度/退款未回滚数 | ② | 被"V1 监控方案升级拍板"取代:V1 监控实例化为**到手价双阈值告警落表**(promotion_price_alert,累计 50%+环比 20%,PRD §2.3 全套工作台:告警状态机/去重/排查抽屉/监控设置/扫描机制)+**三向日对账**。5 项映射:退款未回滚→对账②;used_count 一致→对账③;优惠总额观测→详情核销 Tab KPI(告警联动预算属 V2);核销增速与集中度告警→依赖预算/风控,归 V2(归档 §6 V1 清单原本亦未含此两项,属方法论建议非 V1 拍板结论)。见 §四备注 B1/B2 |
| 2-23 | 一键熔断:paused 立即停止命中与核销、已产生订单不受影响、止损优先于查因、修正后恢复 | ① | PRD §2.1.4 状态机(paused 熔断/恢复→active)+§2.3.2(一键暂停即时对 C 端止损、告警不自动转已处理、处置留痕);"有权限角色恢复"按 V1 不分级简化(PRD §六) |
| 2-24 | 三向对账口径:核销 vs 订单优惠 / 有效核销 vs 回滚+退款订单 / used_count vs valid 实际笔数 | ① | PRD §2.10 三向核对逐条对应(①valid 核销笔数 vs order_discount ②回滚核销 vs 整单退款订单 ③used_count vs valid 计数);金额以订单侧 order_discount 为准、营销不记金额(§2.10 规则说明+§1.6 核销术语)。第 4 行"让利 vs 预算"归 V2 预算池(PRD §1.2) |
| 2-25 | 对账频率:不是月底动作,关键指标日频/实时;不平即当潜在资损处理 | ① | PRD §2.10 每日定时+支持手动触发;不平→生成差异记录+站内提醒运营排查(§1.4 场景 5)。"先熔断再定位"的处置动作由监控工作台一键暂停承接 |
| 2-26 | §0/§1 资损全景与根因四段分层 | — | 风险分析论证,非结论性内容;其结论已全部转化为上述机制项,PRD §三 风险行另有残余风险声明 |

**文件 2 小结**:26 项中结论性 24 项——①覆盖 17 项(含复合)、②取代 7 项(A6×3、D3/D4/D5/D6、域边界修正、监控升级、A2 保护线),③真遗漏 **0 项**。

---

## 三、三分类汇总

| 分类 | 条数 | 说明 |
|---|---|---|
| ① 已覆盖 | 22 | 三道防线、P1/P3/P4/P7(熔断半边)、限次/封顶/门槛机制、核销回滚链路、三向对账、无汇率原则、量纲判定落地结果、V2 边界、职责边界(营销定规则/订单落兜底/事件驱动回滚)均在 PRD §1.1/§2.1/§2.2/§2.3/§2.8-§2.10/§三 找到承载 |
| ② 被更新决策取代 | 10 | A6(promotion_market 删除、金额上提单币种、码 (market,code) 唯一、币种一致性模型化)×4 处;D3 部分退不回滚;D4 can_combine 开关;D5+A6 timezone_mode 废弃;D6 手动+随机双方式;域边界二次修正(分摊归营销);监控升级(告警落表+双阈值取代方法论指标清单与"不建表"口径);A2(保护线取 0,成本线不入 V1)。PRD 全部按新口径书写,无一处仍沿用旧口径 |
| ③ 真遗漏 | **0** | 无结论性内容既未被 PRD 承载又未被决策取代 |

**备注(非遗漏,建议按"延后问题按模块记录"规则补记待优化清单)**:
- B1 核销增速陡增告警(羊毛规模化监测):归档方法论建议项,V1 监控拍板范围外,PRD §四 V2 方向未显式列出 → 建议记入营销模块待优化清单
- B2 异常订单率监测(实付≤保护线/超封顶单的事后检出):V1 靠 A2 硬兜底+试算幂等预防,无事后专项检测 → 同上建议补记
- B3 实付保护线进阶为商品成本线:归档"进阶"选项,A2 拍板 V1 取 0 → 建议随 V2 预算池一并评估

---

## 四、最终结论

**两份归档文件作为 PRD 素材的使命已完结,可以确认。**

- 《折扣内容-金额与百分比设置逻辑说明》:结论性内容 8 项,覆盖 5 + 取代 3(全部对应 A6 单市场化),真遗漏 0。文档核心价值(量纲判定原则、无汇率显式录值)已由 PRD §2.1/§2.2 与主线 §2.3 承载;promotion_market 结构性表述已被 A6 合理取代。
- 《营销资损防控方案》:结论性内容 24 项,覆盖 17 + 取代 7,真遗漏 0。七条铁律 P1-P7 全部有承载或被更强机制升级(试算拦截、告警落表双阈值);跨模块职责边界、三向对账口径、优惠码防猜规则均按最新决策(A2/A6/D3/D4/D6、域边界修正)在 PRD 中成立。
- 后续动作:两文件保持归档状态不再维护;B1/B2/B3 三条建议项待用户确认后记入营销模块待优化清单(非本次核查的拦截项)。

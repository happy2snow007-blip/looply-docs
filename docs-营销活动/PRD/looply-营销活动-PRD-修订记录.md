# looply 营销活动 PRD 修订记录

> **文件指向说明（2026-07-29 过程文档清理）**：本表历史条目中提到的 `问题核对与决策清单-v1~v9.md`、`Figma核对报告-C端-20260728.md`、`本轮决策基线-20260728.md`、`技术方案边界待清理清单-v1.md` **均已删除**。各自去向：决策结论与决策码释义**即本表**（乙套 D1~D7 见 v1.9 条目）· 上游能力核实基线已按归属拆入 `商品/待优化清单.md` 条目 4、9 与 `商详/待优化清单.md`，PRD 防拆说明见 `待优化清单.md` · 技术方案边界 17 条落点见 `待优化清单.md` 末尾附录 · skill 改进素材已并入 `prd-writing` 自检 14/15。**历史条目原文不改，按本说明换算即可。**

| 版本 | 日期 | 修改人 | 修改内容 |
|---|---|---|---|
| v1.0 | 2026-07-26 | 产品(晴天代笔) | 首版。范围:V1 Amount off products(auto+code);运营端(促销活动管理+优惠监控)、C 端(商详/购物车/checkout/订单详情营销触点)、机制(试算+行级分摊/核销与整单退回滚/到手价监控/日对账)。输入:概览 v1.0(G1 12 项决策)、ER v1.3、运营端原型 v3、C 端 UI 改动稿×4。两处口径为本版新拍(供 review):①一键暂停活动后告警不自动转已处理,需运营手动标记;②告警去重=同商品+同市场存在 pending 时更新不新增 |
| v1.0(评审期补充) | 2026-07-27 | 产品(晴天代笔) | ①§2.1 补多市场投放口径(一活动=同一策略跨市场,市场间力度差异分市场建活动,市场级覆盖 V2)——G3 评审问题澄清;②优惠监控升级双阈值:新增环比突变告警(相对上次扫描到手价,默认阈值 20%),告警类型枚举/设置/排查/扫描机制同步,ER v1.4(alert 表+alert_type/prev_final_price/change_pct)、原型 v4 同步 |
| v1.0(评审期补充二) | 2026-07-27 | 产品(晴天代笔) | A6 拍板:活动-市场关系改 1:1(建活动第一步选市场、创建后锁定;金额上提规则层单币种;优惠码 (market,code) 组合唯一;timezone_mode 废弃、按所属市场本地时区;跨市场同策略=复制活动)。ER v1.5(删 promotion_market 表)、原型 v5 同步;取代评审期补充①中"一活动可投多市场"口径 |
| v1.0(评审期补充三) | 2026-07-27 | 产品(晴天代笔) | 监控告警价格对比列口径修正:环比突变类展示"上次到手价→本次到手价(辅注原价)",累计类展示"原价→当前到手价";原型 v5 列表列+排查抽屉同步 |
| v1.0(评审期补充四) | 2026-07-27 | 产品(晴天代笔) | 双核查闭环:①§2.2.1 补列表头部"导出(含码值列)"(概览覆盖核查发现的唯一遗漏);②§2.1-4 补状态存储分层说明(paused 落规则层,ER 同步 DESIGN_RULES);概览 5 处滞后文字回补;3 条方法论建议项记入模块待优化清单。核查结论:概览 17 项 V1 能力+13 项决策全部有产出落点;两份归档设计说明素材使命完结 |
| v1.0(评审期补充五) | 2026-07-27 | 产品(晴天代笔) | 模拟评审闭环(P0×1+P1×9 全修):监控样例口径修正(原型);进行中编辑字段范围、优惠资格锁定时点(§2.1-9)、核销兜底拒绝处理、满额门槛基准、码无预占口径、复制规则、码级停用 V2 预留、列表/推荐位卡片展示、购物车勾选口径+依赖补购物车模块。P2/P3 留待 G4 review 定夺(见 评审记录/模拟评审-20260727.md) |
| v1.0(评审期补充六) | 2026-07-27 | 产品(晴天代笔) | G3 复测反馈:优惠码唯一性口径修正——由"(market,code) 组合唯一(不同市场可复用)"改为"**全局唯一(不同活动不可用同一码)**"(此前对 A6 中"code 不同市场不一样"理解有误)。ER v1.6(code 列 UK)、原型 v5(HK 示例码改 HKWELCOME、helper 改写)、PRD §2.2.2/§2.6(新增"码市场≠买家市场"失败态)同步 |
| v1.0(评审期补充七) | 2026-07-27 | 产品(晴天代笔) | G3 复测两反馈:①新增"适用用户"(A7,人群定向从 V2 提前到 V1):all/新客/指定人群标签,自动折扣与优惠码均受约束——ER v1.7(rule.eligibility_type+promotion_eligibility 表+ref_user 新客/标签字段)、原型 v6 适用用户分区、PRD §2.2.2/§2.8 候选集判定/§2.6 失败态/用户域依赖同步;②可叠加开启时展示当前市场进行中的可叠加活动清单(原型 v6 + PRD 交互说明) |
| v1.0(评审期补充八) | 2026-07-27 | 产品(晴天代笔) | G3 复测:①修复原型 v6 新建/编辑页崩溃(可叠加清单误引用 PROMOS,应为 DISCOUNTS);②适用用户模型简化——由 all/新客/指定人群 三档改为 all/指定用户标签 两档,新客并入用户标签(定义与打标归用户管理模块,营销侧仅引用不定义新客规则)。ER v1.8(eligibility_type=all/tag、promotion_eligibility 存 user_tag_id、ref_user 统一 user_tags)、PRD §2.2.2/§2.8/§2.6/依赖、原型 v6 同步 |
| **v1.1** | 2026-07-27 | 产品(晴天代笔) | **《营销活动-待决策问题汇总》9 条问题闭环**(核对与决策全过程见模块内 `问题核对与决策清单-v1.md`):①#2 列表页告警 Alert 条移除,改为左侧菜单「优惠监控」红色角标引导;②#3 新增 `scheduled` 未开始状态枚举(解决已发布未到开始时间显示为进行中的歧义),需新增 scheduled→active / active→ended 两个定时任务;「已暂停」加可恢复 Tooltip 消歧义;③#4 操作列形态**按该行自身的操作数逐行判定**——≤4 个全平铺;>4 个则前 3 个平铺、第 4 个起收进「更多」,可见元素恒为 4(同一列出现"4 平铺"与"3+更多"两种形态属正常,不按列强行统一),同步改写 antd-admin-prototype skill 规范 A.1.1;④#5 列表新增「创建信息」列(创建人+创建时间)+ 默认创建时间倒序;⑤#6 告警类型改造:**取消独立「类型」列**,比较基准直接写进降幅列(`较原价 -55% /阈值50%`;突降类另加参考行);类型名**仅保留在筛选器**,措辞改大白话 `比原价降太多` / `比上次降太多`(枚举 key `cumulative`/`sudden_drop` 不变);⑥#7 适用用户(标签模型)经三方核对已一致,无改动——确认「全部用户」为动态口径,不等价于标签全选;⑦#8 移除「每单作用件数」`max_item_count`(多件资损由实付兜底+双阈值监控承接);⑧#9 所属市场改下拉选择(支持扩展至更多市场)、触发方式改普通单选(原大卡片式占用大量页面空间);⑨#10 APP 商详底部吸底栏加促销标签(Verified 入口与价格同步逻辑不动)。配套产出:ER v1.9、运营端原型 v7、APP 商详 UI v4。**注(2026-07-29 勘误)**:③⑤ 两条原记录的是本批第一轮的口径,在同日第二轮用户验收中被判"未解决/不满意"并返工,上文已更正为返工后的最终口径(与 PRD 正文一致) |
| **v1.2** | 2026-07-27 | 产品(晴天代笔) | **《营销活动-待决策问题汇总》「本次新增」4 条闭环**(核对与决策见模块内 `问题核对与决策清单-v2.md`):①「优惠监控」更名为「**价格监控**」(该页监控的是商品到手价降幅,不是优惠本身),并由「营销中心」子项**提升为一级菜单**,营销中心分组保留仅含促销活动;②**V1 全部折扣一律不叠加**——删除 `can_combine` 字段与全部叠加配置/计算逻辑,改为「同一商品行命中多条时取优惠最大的一条;一单内不同商品各享各的折扣(等同分开下单)」,后台表单改为只读说明块;③适用商品**新增 tag(商品标签)与 collection 两种圈选维度**,均为命中任一即适用(OR);tag 引用商品模块「标签管理」、collection 仅可选同市场且 active 的集合且命中随动态商品池实时跟随;④C 端视觉依据**改为 Figma 设计稿(出图中)**,本地 8 份营销改动 html 降级为历史参考、不再作为开发依据。**跨模块风险**:商品 ER 自 v1.5 起未画 `tag`/`tag_binding` 表(功能在 PRD v1.7 §2.9 与后台原型中存在),按 tag 圈选需开发对接时向商品侧确认表结构。配套产出:ER v2.0(删 can_combine、target_type 扩枚举、新增 ref_collection 引用表)、运营端原型 v8 |
| **v1.3** | 2026-07-27 | 产品(晴天代笔) | **《营销活动-待决策问题汇总》「本次新增」3 条闭环**(核对与决策见 `问题核对与决策清单-v3.md`):①原型适用商品 4 个多选框显示过窄——根因是 antd `Select` 无默认宽度、只写了 `maxWidth` 未写 `width`,已补 `width:100%` 并统一同表单内标签多选宽度(纯原型缺陷,PRD 无改动);②删除新建/编辑页「叠加规则」只读说明块——PRD 已在边界表/名词表/§2.8-6 三处写清,原型不重复承载纯规则说明(另 2 处说明块经确认保留);③**价格监控告警新增飞书群通知,纳入 V1**:采用群自定义机器人 webhook(无需应用权限/token),**每轮扫描聚合推送一条**汇总卡片(新增条数 + 降幅 TOP 5 + 去处理按钮),本轮无新增则不推送;Webhook 地址加密存储、前端脱敏、提供「发送测试」自验;推送失败仅记日志不落业务表,站内角标为兜底通道。**顺带补齐上一批遗漏**:外部依赖表新增 Collection 模块行、商品域行补商品标签依赖与 ER 缺口风险、新增飞书开放平台依赖行、风险行补 2 条。ER **不动**(阈值与 webhook 均为全局系统配置项,不产生业务表/字段)。配套产出:运营端原型 v9 |
| **v1.4** | 2026-07-27 | 产品(晴天代笔) | **取优与门槛口径重定义**(核对与决策见 `问题核对与决策清单-v4.md`):①**门槛改严格口径**——门槛必须由**实际使用该折扣的商品**满足,不允许被最终用了其他折扣的商品凑满;并明确**任何场景都是先判门槛再算优惠**,差异只在参与判定的集合(商详=当前这一件按挂牌价判;购物车/checkout=最终分配给该折扣的行,可凑单);②**修正 §2.8 展示口径错误**——v1.3 及以前写的"商详无门槛判定"与上述规则冲突且会虚标价格,本版起商详同样先判门槛;③**§2.8-6 取优重写**——严格口径打破了逐行独立取优的可分离性(门槛与取优互相依赖),改为"求整单减免最大的**合法组合**",并定义平局规则(折扣条数少者优先→折扣 ID 字典序)保证引擎幂等、求解口径(精确求解 + 必要性剪枝,超规模降级贪心并标记)、含正反例;④**满额/满件门槛语义写明**——它们是门槛不是减免方式,达标后**每件各减**;"整单只减一次"属订单级折扣,边界表显式列入 V2。已知并接受的风险:fixed 类无封顶(封顶字段仅 percentage 可配,件数上限已于 v1.2 删除),5 件×减$100=减$500 会真实发生,依靠试算拦截+双阈值监控+实付兜底三道防线。**ER v2.0 不动**(三项决策均无字段增删)。配套产出:运营端原型 v10(门槛语义说明块 + 两处 helper 文案) |
| **v1.5** | 2026-07-27 | 产品(晴天代笔) | **PRD 表达优化(不含任何业务规则变更)**,核对见 `问题核对与决策清单-v5.md`:①**§2.8 按场景重构**——原 9 步"订单级试算"流程里混入了商详场景规则、步骤 6 内部又嵌了四段把流程撑爆、末尾还用"执行步骤 1/2/3/6/7"编号引用让读者来回跳;现拆为 §2.8.1 场景与共用口径(四场景→规则对照表 + 共用口径 A/B/C)、§2.8.2 **规则一·商详单品到手价**、§2.8.3 **规则二·购物车/checkout 最优组合**、§2.8.4 分摊与实付、§2.8.5 通用说明;每条规则开头加"一句话"总述,举例改为对照表格。**规则内容一一对应,无增删**;②"订单计算引擎统一**夹逼**执行一次"改为大白话:"算出来是负数则按 0 收;这个兜底只由订单引擎做且整条链路只做一次——营销侧不做截断,两边都截会导致金额对不上",并补上原文缺失的"为什么只能做一次";③修复重构导致失效的 4 处交叉引用(§2.2.3/§2.3.4/商详章节/checkout 章节原先指向 `§2.8-6`、`§2.8 展示口径` 等已不存在的锚点);④顺带清理同类生硬术语:收敛→拆成、归一→统一转成、幂等→"重试不会重复计数"、"至少一次+幂等"→"允许重复送达但重复不能造成重复计数"。**原型 v10 与 ER v2.0 均不动** |
| **v1.6** | 2026-07-27 | 产品(晴天代笔) | **商详到手价统一输出 + 价格监控覆盖非折扣变化**(核对与决策见 `问题核对与决策清单-v6.md`):①**到手价统一输出**——§2.8.2 规则一无论是否命中折扣一律输出到手价,**无折扣时到手价=挂牌价、减免=0**(此前无折扣不输出,下游要自己分支);下游用"减免>0"判断促销态;②**§2.4 补划线价/Save 场景对照表**(有折扣:现价=到手价、划线价=挂牌价、Save=挂牌价−到手价;无折扣:现价=挂牌价、划线价=对比价、Save=划线价−挂牌价),口径与商详 PRD v1.8 §2.7 对齐,名词表补 挂牌价/到手价/Save;**跨模块待对齐**:商详 PRD v1.8 L515 与 L547 对"有促销时划线价"写法不一致(listing_price vs max(listing_price,compare_at_price)),按 L515 口径供给,已提请商详侧修正——按 max 会把零售参考价与平台售价的差算进促销优惠,虚增力度且有 FTC 风险;③**修正 v1.4 遗留矛盾**:§2.4 原留有"满额/满件门槛折扣在商详只出标签不改价格(V1 简化)",与 v1.4 定的"按挂牌价判门槛、达标即减"打架,已改为达标即计入到手价、不达标才仅出标签;④**价格监控扫描范围扩大**——原只扫"活动命中的商品",未被折扣命中的商品挂牌价错录成 1/10 也无告警;现为三路径:周期扫(活动命中)+ **事件扫(挂牌价变更,不限是否有折扣)** + 事件扫(活动变更);不做全站周期扫(listing 量大、有效告警占比低);⑤**告警新增成因 `cause_type`**(折扣配置/挂牌价下调/两者叠加),列表加成因列与筛选、命中活动可为空;**处置区按成因分流**——纯挂牌价成因不出"暂停活动"(无活动可暂停),改为"查看商品"+提示营销侧无法直接处理;核实结论:环比监控本就能捕获非折扣变化,缺的只是扫描范围与成因区分,故未新增告警类型。配套产出:ER v2.1(alert 加 cause_type、handle_action 补 notify_product)、原型 v11(成因列/筛选/分流处置 + 新增一条无折扣挂牌价告警样例) |
| **v1.7** | 2026-07-27 | 产品(晴天代笔) | **开发评审 TODO 25 项闭环第一轮(落地 19 项)**,核对见 `问题核对与决策清单-v7.md`。**拍板**:①删除权限收紧为**仅草稿可删**(已发布过的一律不可删,保全核销记录可追溯;草稿无核销故保留可删);②**保留「已暂停」为独立状态**(5 态不变),但**进行中不可直接编辑**——须走「暂停→编辑→恢复上线」,消除改配置时 C 端仍按旧规则成交的窗口期。**其他落地**:A1 引用类下拉统一「名称(ID)」防同名选错;A3 满件门槛最小 2(填 1 等同无门槛);A6 补齐 7 类操作的二次确认覆盖表;A7 移除列表「操作日志」重复入口、操作人改英文名;A8 补 both 成因样例、排查抽屉去掉「告警类型」;B1 阈值措辞与评审用语对照 + **触发时两个阈值都要快照**(原只存触发类型对应的一个);B2 新增**全量触发场景清单**(含活动暂停/结束导致到手价回升也要重扫,否则基线停在低位);B3 口径澄清**命中≠生效**——不叠加下一个商品只生效一条,暂停入口只对生效那条(暂停未生效的活动对降幅无用还会误伤);C1 新增下游取价改造要求表;C4 补「码力度不如当前最优」提示;D1 新增**多商品优惠组合全场景枚举表 12 条**;D4 候选折扣评估规模 10→20;E3 §1.7 B 段按多语言 v3.2 补齐 B-1 字段清单/B-2 建卡信息/B-3 聚合组三块;E4 时区明确取 **Market 基准时区**(辨析 market 三层时区概念);E5 §2.2.2 大段文字改为两张表。**核实结论**:B1 的两个阈值、C2 划线价、D2 计算约束、D3 商详排除码 **4 项经核实已满足**,无需改动。**skill 根因修复**:prd-writing 多语言 B 段原只给字段清单表、漏掉建卡信息与聚合组,且"去读规范"触发条件设成"判断不清时"——缺的恰是意识不到的部分;已改为三块结构 + 强制对照规范自检两条。**未完成**:A2 商品 ID 导入、C3 规则标签模板、C5 Figma 版本对比(需材料)、E2 技术方案删除层级(清单已备)。配套产出:ER v2.2、运营端原型 v12 |
| **v1.8** | 2026-07-27 | 产品(晴天代笔) | **开发评审 TODO 第二轮(A2/C3/E2/C5)**:①**A2 商品 ID 粘贴导入**——一行一个(也支持逗号/空格),解析后分「有效/无效」两组回显、无效逐条给原因并可复制,只加入有效项;不做 Excel 上传(V1 量级不匹配);②**C3 活动规则标签自动拼接**——按「门槛×折扣值类型」六种模板生成(如「满 $300 减 $80」「8 折」),有封顶追加「最高减 X」,**不再取活动标题**;多语言口径:标签是「模板+变量」不是录入文本,**走前端语言包、不进翻译服务**(活动名 promotion_name 仍需接翻译,因为要出现在订单优惠行与快照里);③**E2 技术方案边界清理 5 处**——删求解降级贪心策略、平局字典序写法、定时任务扫描周期数值、原子操作/唯一约束等实现词、Webhook 加密存储实现句;**保留**业务结果口径(必须返回最优解、结果唯一可复现、同一订单只核销一次、并发不得超发、地址仅尾 4 位可见)与两段论证(为什么不全站周期扫、为什么不能逐行取最大);④**C5 Figma 帧索引落地**——新增 §七.1,列出 8 个页面画布的本期 section 与 17 个帧;**版本口径澄清**:Figma MCP 无版本历史能力,真正的"版本"是画布上**按日期命名的 section**,最新日期那个即本期改动范围;并标注 **PC checkout 的 section 不按日期排序**、定位时须看名称日期;**明确设计稿与 PRD 分工**:设计稿是版式依据,规则与文案口径以 PRD 为准——稿中 `Summer Sale`、$651/$652/Save $1 均为示例数据(用户 2026-07-27 确认)。**已核对**:PC 购物车「含优惠券」指底部 `Discount` 汇总行、非输码入口,与 §2.5 无冲突。配套产出:运营端原型 v13(ER v2.2 不动) |
| **v1.9** | 2026-07-28 | 产品(晴天代笔) | **C 端 Figma 定稿全量核对(17 帧)+ C 端四节重写**,核对报告见 `Figma核对报告-C端-20260728.md`,本批决策码 D1~D7 的释义即下文各条(原独立文件 `本轮决策基线-20260728.md` 已于 2026-07-29 删除,释义以本条为准)。**D2 优惠码约束粒度修正(最重要)**——原写法「V1 单订单仅支持输入 1 个优惠码」**作废**:它只描述了「单商品进入 checkout」这一种场景。优惠码的本质是「**需输码才触发的折扣**」,与自动折扣**同池竞争、受同一条约束——每个商品行至多生效一条折扣**;由此自然推出:一单**可以多个码同时生效**(作用于不同商品行)、单商品场景下新码更优则自然替换、码可能被自动折扣挤掉。新增 **Apply 四步处理顺序**(校验码自身→临时加入集合→整单重新求最优→看该码是否产生减免,不产生则回滚不加入并提示),移除某码同样触发整单重算。**D1 汇总口径统一**——`Subtotal` = **折后行价合计**(自动折扣已含在行价里,不再单列 `Item discounts`);`Discount` **恒为单行**(体现优惠码减免,多码生效也合并为一个数);**汇总区全程不透传活动名与码值**(不出 `Promo code XXXX` 明细行);移动端购物车无汇总区,优惠落吸底栏。**D7 补齐 10 项设计稿已有但 PRD 缺描述的内容**——新增 **§2.7.2 下单成功页(Order Confirmed)整章**(与 §2.7.1 订单详情为两个独立页面,版式与商品行元素不同、不可互相套用)、checkout 商品行营销展示、购物车不可售态、移动端吸底栏、Save 徽章、CTA 金额联动、PC 商详价格区排列顺序与视觉形态、部分退款态、Refund 层级、§七.1 补录 PC 商详主帧 `5212:3653`。**D6 删除 6 项不在本期范围的展示需求**(设计稿中不存在):折扣率标签 `-20%`、用码另减提示、订单详情商品行划线价+折扣标签、退款说明行、购物车 promo code 提示行、购物车行折扣标签;「Total 不低于 $0」从页面元素移出(它是引擎兜底不是展示项)。**D4 FTC**——设计稿任一帧均无合规说明文案,**删除该展示需求**;但 §1.6「划线价取值必须真实、不得虚构参考价」的**约束保留**(它是 §2.4 跨模块 max 取值风险提示的依据)。**D3 checkout 失败态**9 条规则全部保留,标注「展示形式设计稿未覆盖、待补稿」。**D5 多语言接入判定口径**——改为「**本期及未来有可能在 C 端用到的都要接入**」,不以当期是否已展示为准;`promotion_name` 本期虽无 C 端展示面**仍然接入**(理由由原「出现在订单优惠行与快照里」改为预先接入,原理由因 D1 作废);**`group_code` 由 `marketing` 改为 `promo`**——`marketing` 与 market 主数据模块只差两个字母,运营在翻译中心导航里极易看错、开发配置时也容易串,且与既有 `pdp` 类短缩写惯例一致。**连带修正**:§1.5 全局页面流转补「下单成功页」节点、§2.1-8 删「FTC 合规文案可供出图参考」、§2.2.2 活动名称改为「订单快照与运营后台识别来源,不直接用于 C 端展示」、§七 顶部索引表修复被拆散的两行。**同日四项拍板(2026-07-28,四项均维持 PRD 现状、无规则改动,仅措辞由「待确认」改为「已确认」)**:①PC 商详以 `7.27营销样式更新` 主帧(`5212:3653`)为价格区唯一依据,`7.27折叠样式补充` 的价格区判为旧版复制残留、不作依据(依据:票券标签节点号 `5315:*` 只出现在主帧);②取消单(cancelled)的 `Discount` **保留成交快照值,不清零、不隐藏、不置灰**——买家回看、客服追溯、资损对账都依赖这个数;③`单一物流` 帧的 `Refund` 行**判为画错**,该行**仅在真实发生退款时出现**,层级恒为「Total 下方、不参与 Total 计算」;④移动端下单成功页**本期按 PC 元素构成实现、不等补稿**,补稿后回核 §2.7.2。**§七.1「设计侧待确认」整段重构为「设计稿待修正清单」**——5 项全部转为设计侧返工项(4 项修帧 + 1 项补「应用码前/后」对照帧),**均不改变 PRD 规则,开发不必等这些帧修好**。**原型 v13 与 ER v2.2 均不动**(本轮全部为 C 端展示口径,无运营端功能与数据模型变更) |
| **v2.0** | 2026-07-28 | 产品(晴天代笔) | **开发评审 TODO 第三轮 15 条闭环**(核对见 `问题核对与决策清单-v8.md`,4 个 agent 并行回原始文件逐条搜证)。**①C 端展示文案改英文源文案**:§2.4 活动规则标签六模板由中文改为英文(`{P}% off` / `Spend {Threshold}, get {Amount} off` / `Buy {N}+, get {P}% off` 等),封顶后缀定为 `(up to {Cap})` 且**只挂百分比三格**(封顶字段仅 percentage 可配);补齐 `{P}` 取值口径(**直接取 `percentage_value` 不换算**,原 `{N} 折` 的 N 无定义来源属隐藏歧义)、六组合渲染样例与溢出约定;中文折率换算 `(100−P)÷10` 单列说明,明确**英文为源、中文是语言包另一套模板,不得用同串替换**。§2.6.4 失败态 9 条同样给出英文源文案。**口径**:PRD 定义的英文**就是最终展示文案**,语言包只负责翻成其他语种——**不给语言包 key**(属开发侧实现细节)、不写"以语言包为准";中文列仅供运营理解、非交付物。**②失败态统一 Toast**:取消原「展示形式待设计补稿(D3)」,明确统一 Toast、不用行内红字与输入框错误态(9 类失败且部分带变量,行内位放不下且会顶掉汇总区版式);补「成功态不出 Toast」(chip + 金额联动已自洽);**新增第 2 条拆 2a/2b**——满额与满件的买家动作不同(花钱 vs 加件),一个模板串塞不下;§2.6.2/§2.6.3/§2.6.6/§2.8.3 四处提示文案**一次拉平**为同一条英文源文案。**③删除 §2.10 资损对账整节**(用户拍板"完全删除,以后按资损方案的环节设计"),含 6 处连带:§1.1 目标 3、§1.3 运营与系统职责、**§1.4 核心场景第 5 条整条删(核心场景 5 条→4 条)**、§四 V1 范围、§2.9 异常处理的日对账兜底句。**⚠️ 已知并接受的风险**:V1 期间若发生并发击穿(核销计数超发),PRD 内**无任何机制能自动发现**,只能等客诉或财务核对时暴露——已记入模块待优化清单。**④优惠资格锁定时点迁入 §2.9**:原 §2.1-9 整条是三条硬规则(锁定时点=订单创建时刻 / 活动后续暂停结束不影响已创建未支付订单 / **核销时不再校验活动状态**)且**全文无第二处承载**,§2.9 原文只有"唯一约束+原子计数+并发不超发"——直接删会导致开发在核销时加活动状态校验,使"活动结束前下单、结束后支付"的订单核销失败。故**原文照搬迁入 §2.9 开头**而非删除。**⑤promotion_code 不再建多语言卡**:删 B-1 字段行、B-2 建卡行(该行原 `storage_mode` 填 `translation_table` 与"判不翻译"自相矛盾)、B-3 sort 行,聚合组成员数 3→2,留痕段改为"优惠码是标识符不是文案,不翻译、不建 resource_type、不进聚合组";**保留 §2.2.2 的 ER 表名 `promotion_code.status`**(同名不同义,误删会丢字段)。**⑥清理"ended 可删"错误残留**:v1.7 已收紧为"仅 draft 可删",但状态机表 ended 行仍写「**仅可删除**」、§2.2.5 共享弹窗触发条件仍写 `draft/ended`、scheduled 说明句尾"需先结束"暗示结束后可删——三处与主规则**正面冲突**,且删掉主规则句后这些残留会成为文中唯一表述,故**先清残留再收敛规则**。**⑦§2.2.4 补状态可见性**:明确活动详情**全部状态(含 ended)均可访问、两 Tab 不随状态隐藏**,ended 的核销记录与规则配置永久可追溯——这是"已发布不可删除"的配套要求(原型已实现但 PRD 无明文)。**⑧其他**:§2.3.1 类型名"仅保留在筛选器"(删"与排查抽屉",但**保留筛选器项与 alert_type 枚举定义**——前者原型真实存在、后者是 ER v2.2 依赖的唯一定义处);§2.3.2 抽屉元素补「价格对比」并明确不展示告警类型;§2.1 引用类下拉的"已选中项在回显/详情页/导出文件同样带 ID"末句**并入规则本体后**再删解释;§2.5 不可售态描述删除前把"营销元素保留展示(灰化)"并入展示规则;§七.1 删 Figma 核对结论与待修正清单两大段前,把「PC 购物车帧名『含优惠券』不是输码入口」搬入 §2.5 UI 关联、「单一物流帧 Refund 行画错不要照排」搬入 §2.7.1。**删除类另含**:§2.3.4「为什么不全站周期扫」、§2.4 promotion_name 翻译说明段(§1.7 L102 已有完整理由)、§2.5「原 v1.8 Item discounts 不成立」留痕块(实际在 §2.5 非 §2.4)、§2.5「D 本期不出现的元素」。**本版不含**:技术方案边界清理(条目 15)——18 条候选 + 应保留项 + skill 改进建议已存 `技术方案边界待清理清单-v1.md`,用户定为本次不处理、后续用于优化 prd-writing skill 边界规则。配套产出:运营端原型 v14(ER v2.2 不动) |
| **v2.1** | 2026-07-28 | 产品(晴天代笔) | **PRD v2.0 遗留问题闭环**(核对见 `问题核对与决策清单-v9.md`,3 agent 并行回 PRD 全文 / Figma / 跨模块原始文件搜证)。**①优惠码失败态由 Toast 改为行内错误提示**:PRD 中 C 端相关 7 处 Toast 全改(§2.6.1 元素6 / §2.6.2 第4步 / §2.6.3 / §2.6.4 表头说明 / §2.6.4 表头列名 / §2.8.3 第4步),位置定为优惠码输入框正下方;**PC 与移动端形态刻意不同**——PC 输入框边框不变、仅框下红字(依据新核到的 Figma 帧 `looply PC Checkout — 营销code输入报错` `5778:1667`,PRD 此前从未登记该帧),移动端红框+框下红字(复用移动端既有表单报错组件 `1013:16791`,**不单独出稿**);补「报错时输入框保留已输入的码、chip 不出现、金额不变」;删掉 v2.0「9 类失败带变量、行内位放不下且会顶掉汇总区版式」的论证(已被设计稿实物推翻);**§2.6.6 试算服务异常保留 Toast** 并补写"为什么这里用 Toast 而 §2.6.4 用行内"(服务不可用 vs 输入校验失败,错误归属不同);九条英文源文案不变,并**点名设计稿上的 `Incorrect discount code` 为示例数据**,防开发照抄。**②移动端下单成功页补稿落地**:新增帧 `支付成功页增加优惠样式`(`5668:30702`)已核,§2.7.2 删除「仅有 PC 1 帧 / 未出稿 / 不等补稿」全部措辞与两条「待对齐」,§七.1 注②改写为正式登记;元素表由 PC 单端「左栏/右栏」改为**两端版式表**(PC 双栏 / 移动端单列纵向,元素构成一致仅排列不同);**补 `Tax` 行**(补稿实为 `Subtotal (2 items) → Discount → Shipping → Tax → Total`,原写「设计稿无 Tax 行」的事实基础已不成立,差异对照表同步改为 ✅ 有);**「明确不做的元素」补「活动规则标签」**(补稿商品行无票券形标签,防开发从 §2.4 套商品行画法时带上)。**③`Subtotal` 展示标签统一带件数**:§2.6.1 元素4/4.1、§2.7.2 补为 `Subtotal (N items)`(§2.7.1 原已有;§2.8.4 是计算口径定义,保持裸 `Subtotal` 不动)。**④划线价口径定案**:有折扣时取 `listing_price`,§2.4 删除「请商详侧修正 L547」「若商详最终判 max 则本模块 §2.4 与 §2.8.2 需回改」等悬置措辞,改为带举例的定案说明(原始零售价 $2,000/挂牌 $1,200/促销 $1,000 → 本口径 Save $200 vs max 口径 Save $1,000,后者虚增促销力度、有 FTC reference pricing 风险);**§三 风险⑦整条删**;商详侧本轮不改 PRD,已在 `海外业务/商详/待优化清单.md` 补「划线价口径——已定案」段落(含结论、依据、商详侧待办修正 L547)。**⑤tag / Collection 由风险项改为明确的外部依赖**:商品域依赖行改写为「提供按类目/品牌/标签/listing 圈选商品的能力 + 判定指定商品是否命中某标签的能力」「挂牌价变更必须对营销侧可感知」「营销侧仅引用,不自建标签体系、不缓存商品池」,删掉「商品 ER 未画 tag 表、开发对接前需确认表结构与查询接口」的警告;Collection 依赖行标注为独立模块(非商品域);**§三 风险④⑥整条删**。**口径**:依赖只写业务能力与归属,**接口拆几个、单值还是批量,全部属技术方案层、PRD 不体现**。**⑥§2.3.4 扫描机制清理技术方案层描述**:删「三条路径(周期扫/事件扫/事件扫)」整表、删全量触发场景清单的「触发路径」列(改为「是否需识别」)、删「兜底 周期扫」整行、删「监控服务内部维护基线」半句,处理流程首句由「按上述三条路径取待扫商品」改为「对需监控的商品 × 市场」,并补一句「本节只规定必须能识别到哪些变化,以何种方式识别/频率/补偿手段均由技术方案确定」;连带修正 §2.3.1「告警生成与去重规则」中的「系统周期扫描」与「基线不落业务表」(否则与 §2.3.4 自相矛盾)。**⑦删除全部对设计稿示例数据的评价性描述**:§2.7.1 删「⚠️ 设计稿有一处画错,不要照排:PC 订单详情 `单一物流` 帧……」整段,**但 `Refund` 行本身的业务规则(仅真实发生退款时展示)以正面表述保留**;§七.1 注①改写——PC 商详 `7.27折叠样式补充` 的价格区**已由设计补上活动标签**,原「旧版复制残留」判断作废,但**仍明确以主帧 `5212:3653` 为价格区唯一依据**(避免两帧并存时开发不知对哪份)。**⑧§七.1「设计稿与 PRD 的分工」由 3 条扩为 4 条**,新增两条全局口径:**「PC 与移动端不要求样式一致」**(同一功能允许两端形态不同,不得以"应一致"为由把一端形态套到另一端,也不得因不一致就判设计稿有误)、**「设计稿金额一律视为示例、不要求自洽」**(各帧金额相加与 Total 对不上、前后帧数值未刷新均不构成实现依据、也不作为设计返工要求)。**本版关闭的设计返工**:v1.9 §七.1 遗留 5 项 + 本轮新核出 1 项(移动端下单成功页 Total 漏减 Discount),经产品拍板**全部判为纯示例、一项都不需要设计动手**。**本版不含**:技术方案边界统一清理(条目 15)——除本版 §2.3.4 一处外其余候选仍存 `技术方案边界待清理清单-v1.md`;新增第 6 类越界类型「接口拆分与调用粒度」已补录该清单 §五。**配套产出**:无——流程图、运营端原型 v14、ER v2.2 **本轮均不动**(全部改动为 C 端展示口径、跨模块依赖表述与技术方案边界清理,无状态机/运营端功能/数据模型变更) |

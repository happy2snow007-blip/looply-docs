# looply 订单支付模块变更日志

## 基线版本（已交付开发）

**V1.0（2026-06-29）**
- 交付内容：PRD v1.0、实体关系图 v8.0、后台原型 v5、支付渠道对接说明 v1.0
- 交付位置：~/Desktop/海外业务/订单支付/订单支付-交付开发 V1.0-20260629/

**V1.1（2026-06-30）**
- 交付内容：在 V1.0 基础上新增系统流程图 v1.0；其余文件版本不变
  - PRD v1.0、实体关系图 v8.0、系统流程图 v1.0、后台原型 v5、支付渠道对接说明 v1.0
- 交付位置：~/Desktop/海外业务/订单支付/订单支付-交付开发 V1.1-20260630/

---

## V1.2（已交付 2026-07-14）

本版为订单数据模型层改造，累计了 PRD v1.0→v1.1→v1.2 两轮迭代。

### 新增交付物
- **状态设计说明 v9.1**：首次纳入交付范围
- **邮件通知方案 v1.1**：首次纳入交付范围（含 5 个邮件模板）

### 变更内容（相对 V1.1 基线）

#### ✅ 待付款态 + 支付后拆单
- ER 图 v8.0 → v9.1：parent_order 新增 parent_status / payment_deadline / paid_at / cancelled_at
- PRD v1.0 → v1.2：待付款态上移 parent_order；下单只建父订单，支付到账后拆子订单
- 流程图 v1.0 → v2.0：① 段建父订单不拆单，② 段到账后拆子订单
- 后台原型 v5 → v7：STATUS_MAP 加 pending_payment、新增待付款 Tab

#### ✅ 取消时序修复
- 已付款取消改为「退款成功后才置 cancelled」，退款失败挂人工、订单保持 paid
- 流程图 ④⑤ 段合并为「④ 订单取消与退款」
- PRD §4.1.2 取消订单说明 + 异常处理同步

#### ✅ 待付款超时 / 同步异步支付
- 新增 payment_deadline（下单+1自然日）超时机制
- 新增卡风控异步场景（processing 态、订单已提交页）

### 影响范围
- PRD：已更新（v1.2）
- 实体关系图：已更新（v9.1）
- 状态设计说明：已更新（v9.1，新增交付）
- 系统流程图：已更新（v2.0）
- 后台原型：已更新（v7）
- 邮件通知方案：已更新（v1.1，新增交付）
- 支付渠道对接说明：无影响（v1.0）

### 一致性修复（交付前已处理）
- [x] Klarna 本期保留，流程图 v2.0 补回 Klarna 路由
- [x] 后台支付单管理页枚举补「处理中（processing）」
- [x] PRD §五章邮件标题「4 个节点」更正为 5 类
- [x] PRD 附录标注「订单已提交页（待付款变体）」⚠️ 待补
- [ ] 【设计待办】补待付款变体 / 订单已提交页 Figma 设计稿（非开发阻塞）

### 交付范围说明
- 本次开发交付包只含 V1.1 同类型文件：PRD / ER图 / 流程图 / 后台原型 / 支付渠道对接说明
- 过程中另产出的状态设计说明 v9.1、邮件通知方案 v1.1 不随开发交付包提供

### 交付位置
~/Desktop/海外业务/订单支付/订单支付-交付开发 V1.2-20260714/
差异汇总：同目录 V1.1-V1.2-核心差异汇总.md

---

## V1.2 交付后修订

**2026-07-17｜PRD 补齐订单取消通知邮件（#5）**
- 背景：邮件通知方案 v1.1 已含 Email #5「订单取消通知」，但 PRD v1.2 第五章缺失该模板章节；PRD 正文三处流程（渠道回调失败、待付款超时、72h 兜底查询失败）均引用「发送取消通知」却无对应模板定义。
- 改动内容（仅 PRD v1.2）：
  - 新增 §5.4b 订单取消通知邮件：触发时机（三条路径）、Subject/Preheader、正文结构、取消原因文案表、发送边界（仅 pending_payment 取消发送，已付款取消走退款邮件）
  - §5.1 概述「共 5 类」→「共 6 类」，补入订单取消通知
  - §5.6 模板变量清单新增「订单取消专属」：{{cancelReason}} / {{items[]}} / {{shopUrl}}
  - 翻译字段注册表新增 5 个 notif-email key：cancel_subject / cancel_body / cancel_cta / cancel_reason_unverified / cancel_reason_timeout
  - 三处流程引用挂章节号（§3.2 步骤3/4、§2.2.6 兜底分支②、状态流转规则表）：「发送取消通知」→「发送订单取消通知邮件（见 5.4b）」
- 影响范围：
  - PRD：已更新（v1.2 原地修订）
  - 邮件通知方案：无改动（v1.1 已含 #5，本次是 PRD 向方案对齐）
  - ER 图 / 流程图 / 后台原型：无影响（取消动作已在流程图 ④ 段、后台状态枚举中存在，仅邮件文案层补充）
- 编号口径说明：方案用 Email #1~#5，PRD 用 5.2/5.2b/5.3/5.4/5.4b/5.5。#5 订单取消对应 PRD §5.4b。

**2026-07-17｜技术反馈处理：弃单转化口径统一 + 72h 人工核账边界**
- 来源：技术团队在《v1.2-变更说明-大白话版》飞书文档评论 + 文末「需 PM 确认清单」提出的问题。
- 根因（弃单口径打架）：V1.2「待付款态上移」改造时，把 §2.2.1 结算会话层的转化口径改成了「提交支付即转化」（口径 A），但 §4.2 后台弃单管理章节沿用 V1.1 旧文案「已完成支付才转化」（口径 B）未同步。V1.1 是「先收钱后建单」，转化本就等于付款成功，故 §4.2 在旧语境正确；改造漏改导致同一 converted 枚举两处定义冲突。
- 决策：转化口径统一为 **B（支付成功才转化）**。
- 改动内容（仅 PRD v1.2）：
  - §2.2.1 checkout_status：converted 触发点「提交支付」→「支付成功（parent_order 转 paid）」；pending 含「已提交支付但付款未成功」；新增「转化时点说明」+「数据结构与召回策略解耦」声明
  - §3.2 下单处理步骤3：提交支付时会话保持 pending（原为置「已转化」）
  - §3.2 到账成功步骤2：支付成功那步补「结算会话置 converted」
  - §2.2.6 交互链路：去支付时会话 pending，回调成功才 converted，取消时保持 pending
  - §4.2 弃单枚举：文案对齐（口径 B 下语义本正确，补全触发场景描述）
  - §5.5 弃单恢复邮件：新增「发送范围排除」——召回仅面向「从未提交支付」的会话，已提交支付（审核中/失败/取消）不发；明确弃单率统计（含失败/超时取消，真实流失）与召回发送对象是两层
- 设计原则修正：召回排除条件落在邮件触发策略层（§5.5），不因「发不发邮件」反向改数据结构枚举（保留用户两态设想）
- #4 72h 人工核账边界（技术清单 #4）：开发结论「本期只读、不做手动置状态」。§2.2.6 改「手动操作支付单状态」为「线下跟进」+ 补「本期只读」边界声明；§4.3 支付单管理原本即无手动置状态入口（仅详情/关闭），符合结论未改
- 待优化清单（§后续迭代新增 2 项）：① 支付单人工核账受控操作（下期）；② §4.2「发送恢复链接」按钮显示条件收口，与 §5.5 召回范围对齐（下期）
- 技术清单其余项落位：#2 付款失败可重试（PRD 已按「可重试」实现，确认理解无误，本次无改动）；#3 #5 邮件模板（上一条修订已补 §5.4b）；#6 Order Placed 页（开发确认按 PRD 文案实现，附录「待补」标注保留）；#7 14天异步关单任务（倾向下线，eCheck 本期不做，待确认）
- 影响范围：PRD 已更新（v1.2 原地修订）；ER 图 / 流程图 / 后台原型无影响（converted 仍为两态枚举，无字段/状态增减，仅置位时点与文案调整）；重复下单规则（技术清单 #5）按 PM 决策不写入 PRD

**2026-07-17｜异步邮件触发规则落地（5 分钟阈值）**
- 背景：技术反馈指出「命中风控=异步」仅是业务场景描述，无可执行的系统判定规则（渠道不一定回传明确的「审核中」信号）。PM 决策：以「提交付款后满 5 分钟支付单仍未变 succeeded」作为异步判定的系统规则。
- 决策边界（关键）：5 分钟阈值**仅作为「订单已接收」邮件(#4)的发送判定**，不影响支付单状态流转、C 端页面跳转、库存锁定、超时等任何其它逻辑（PM 明确「5 分钟只作为异步邮件触发阈值，不影响其他逻辑」）。
- 改动内容（仅 PRD v1.2）：
  - §5.2 发送策略：「异步场景下单时先发」改为「提交付款后 5 分钟内 succeeded → 仅发 #1；满 5 分钟仍 processing → 发 #4 再发 #1」，并声明阈值仅用于邮件、不影响其它逻辑
  - §5.2b 触发时机：「进入风控审核时触发」改为「提交付款满 5 分钟仍 processing 时触发，由延迟 5 分钟的定时检查读取支付单状态判定」
  - §3.2 支付到账处理：新增第 6 项「#4 邮件触发」，点明阈值独立于 C 端跳转判定
  - §3.3 静默注册：设置密码链接挂载改为「挂用户收到的首封含订单信息邮件」——同步挂 #1，异步挂 #4，同一 Token 只挂首封避免重复引导
  - §附录变更记录 v1.2 行：追加交付后修订四项标注
- 实现口径：一个延迟 5 分钟的定时/延时任务，到点读支付单状态——仍 processing 发 #4，已 succeeded 不发。
- 影响范围：PRD 已更新（v1.2 原地修订）；ER 图 / 流程图 / 后台原型无影响（无字段/状态变更，纯邮件触发规则 + C 端文案挂载调整）；C 端页面跳转逻辑按 PM 决策不动（异步跳转判定沿用现状，与邮件阈值是两套，PRD 已注明）

---

## V1.3（设计中 · 2026-08-31）

**2026-08-31｜弃单数据补齐与导出**

- 需求来源：产品提出「弃单模块收集的数据信息比较少，需补齐优惠码填写情况和收货地址填写情况，同时支持弃单数据导出」。同批次的「订单管理菜单导出字段扩充」经产品确认**本次不含**，另行处理。
- 核实结论（动手前盘点 PRD v1.2 / ER v9.1 / 原型 v7 / 营销活动迭代 PRD V1.3）：
  - **优惠码在系统里完全没有落点**。营销 V1.3 明确「链接不落库、会话里的码位记在浏览器侧不落库」，只有下单成功才产生核销记录；弃单是没下单的，后台查不到任何优惠码痕迹。
  - **收货地址只有一个 `address_id` 指针**，游客边填的地址无落点；且 ER 里**账单地址连字段都不存在**，而 PRD §4.2.2 已经画了账单地址区块 —— 属产物间打架。
  - **结算会话没有买家邮箱字段**，但 PRD §4.2.1 列表有「买家 = 结算时填写的邮箱」这一列；静默注册在支付成功后才发生，弃单买家大多无用户记录，该列取不到值 —— 同属产物间打架。
  - **PRD §1.2 与 §7 的「结算页不展示优惠码输入框」口径已过期**，被营销活动模块 V1.3（2026-08-25 交付）推翻。
- 数据依据：模块目录下 Shopify 弃单导出 `checkouts_export_1 (4).csv`（711 个结算会话）—— 36% 填了优惠码、63% 填了收货地址、**65% 填了账单地址（比收货地址还多）**、38% 走到支付环节。优惠码码值分布中 `67`(80次) / `777`(32) / `45` / `sarah` / `123` 等**过半是买家瞎猜的码**，构成「记每一次尝试而非只记生效码」的直接依据。
- 产品决策（2026-08-31 拍板，四项全按推荐）：
  - 优惠码 → **记每一次尝试**（码值 / 来源 / 通过与否 / 未通过原因 / 减免金额 / 被移除时间）
  - 地址 → **存完整快照，账单地址本期一起做**
  - **加结算进度**（六值、只进不退），用于定位流失环节
  - 导出 → **一行一个弃单会话**，商品合并成一列
- 改动内容：
  - **PRD v1.2 → v1.3**（新建文件，v1.2 留档不动）：§1.2 删过期排除项；§1.6 新增 3 条术语；§2.2.1 新增 3 组枚举（结算进度 6 值 / 地址填写状态 3 值 / 优惠码使用情况 3 值）；**§4.2 整章重写**（新增 §4.2.0 数据采集机制、§4.2.3 数据导出，4.2.1 加 4 筛选项 3 列，4.2.2 加优惠码尝试记录区块 + 地址区块改常显）；§6 依赖新增营销活动模块、风险新增 3 条；§7 MVP 与后续迭代同步；§8 附录加 ER 待出 v9.2 标注；§9 版本记录加 v1.3
  - **新增迭代 PRD**：`迭代需求/looply-订单支付-迭代PRD-V1.3-20260831.md`（596 行，自包含，供研发 coding）
- 边界声明（开发必读）：**营销侧的「码位不落库」本次不推翻**。营销记的是买家点链接后跟随浏览的待用码（浏览器侧），订单侧记的是结算页每次实际提交校验的结果（落库）。优惠码怎么带、怎么判、失败文案全部沿用营销 V1.3，**校验逻辑零改动**。
- 影响范围：
  - ⚠️ **ER v9.1 → 待出 v9.2**：结算会话新增 28 个字段 + 新增「结算优惠码尝试记录」实体及关系
  - ⚠️ **后台原型 v7 → 待出 v8**：弃单列表 4 筛选项 / 3 列 / 导出按钮；详情页优惠码尝试记录区块、结算进度、地址区块常显；新增导出弹窗（复用对账管理形态）
  - ❌ 系统流程图 v2.0 无影响（不改结算与支付流转）
  - ❌ C 端设计稿无影响（结算页零新增元素，买家无感知）
  - ❌ 邮件模板无影响（恢复邮件内容与触发逻辑不变）
  - ❌ 营销活动 PRD / ER 无影响（只读取判定结果，不改营销规则与数据）
- **2026-08-31 补：ER v9.2 与原型 v8 已出**
  - **ER v9.1 → v9.2**（`实体关系图/looply-订单模块实体关系图-v9.2.svg`，数据源 `gen_订单_er.py`）：checkout_session 12 → **40 字段**（新增 28）；新增实体 **checkout_discount_attempt**（10 字段）+ 1:N 关系；DESIGN_RULES 44 → 49（新增 checkout_progress_enum / checkout_address_snapshot / checkout_discount_attempt_rule / discount_code_marketing_boundary / checkout_data_capture，并重写 checkout_abandonment）。全表 17 → 18，关系 21 → 22。JSON 与 YAML 两块 metadata 已校验一致。
  - **原型 v7 → v8**（`原型/looply-订单管理后台原型-v8.html` + 同名 `-report.md`）：弃单列表 +4 筛选项 +3 列 + 导出按钮与弹窗；弃单详情 + 结算进度 / 地址区块常显 + 空态 / 优惠码尝试记录区块 / 时间线新事件；详情页改为按点击的会话渲染（原来 6 条点进去都是同一条）；mock 扩成 6 种典型形态。顺手修了 `.lp-detail` 缺 `align-content:start` 的存量缺陷。
  - ⚠️ **一个流程教训**：`check.sh` 14 项全过（Fail 0），但产物**其实编译不过**——新增列拼 style 少一层大括号，浏览器打开必白屏。原因是 check.sh **不含 JSX 编译**，而第 14 项「渲染仿真」因为详情页组件入参不匹配脚本的识别规则被静默跳过，却仍打印「✅ 无渲染硬伤」（零结果假阴性）。补跑 Babel 编译 + 本地依赖 headless 渲染后才抓到，并顺带修了时间线时间倒挂、表头折行、创建时间被固定列遮挡 3 处。**结论：这份产物的自检必须额外跑「Babel 编译」与「真实渲染」两步，不能只信 check.sh。**
- **交付包 V1.3-20260831 已打**（7 件）：《V1.2-V1.3 核心差异汇总》· 迭代 PRD V1.3（开发依据）· 完整 PRD v1.3 · 原型 v8 · ER v9.2 · 系统流程图 v2.0（未变，沿用）· 支付渠道对接集成说明 v1.0（未变，沿用）。
- **已上文档中心**（2026-08-31 18:08，Pages 构建成功、线上实测 5 个链接全 200）：ER v9.2 / PRD v1.3 / 原型 v8 / 交付包 V1.3 四项由 sync.py 自动更新；**迭代 PRD 走 `static_docs` 注册**（源在 `迭代需求/`，同步到文档中心 `PRD/`，本地存放规范不变），index.html 新增「迭代 PRD」card。顺带补回被漏掉的 PRD V1.2 历史版本条目。

**2026-08-31｜交付后修订：收敛展示面（原型 v8 → v9）**

- 触发：V1.3 打包上线后产品 review 两条——① 原型改动太大，弃单**列表不要增加列**；② 详情页的**优惠码尝试记录融合进时间线**，不单独加模块。
- 改动（**只动展示，数据口径一个字没改**）：
  - **弃单列表撤销新增的 3 列**（优惠码 / 收货地址 / 流失环节），列宽全部恢复 v7 原值，回到原有 7 列。**4 个新筛选项与导出按钮保留**——筛选负责定位、详情与导出负责细节。
  - **详情页删除「优惠码尝试记录」独立表格模块**（含底部「累计尝试 N 次」汇总），尝试记录**并入事件时间线**逐条呈现，事件详情补全原表格承载的信息：`{码值}（{码来源}）· 已应用，减免 {金额}` / `{码值}（{码来源}）· 未通过：{原因}` / `{码值} · 买家主动移除，本单不再享受该优惠`。
  - 判断依据：时间线里本就有这三类事件，独立表格是**同一份数据讲第二遍**；且买家在结算页的动作本是一条时间序列，拆两处会让运营来回对时间才能还原经过。
- ⚠️ **`checkout_discount_attempt` 与导出 41 列未变**，ER v9.2 不动。
- 产物更新：**原型 v8 → v9**（+ v9 自检报告）· 迭代 PRD V1.3 与完整 PRD v1.3 **原地修订**（同日交付后修订，比照本模块 V1.2 先例，版本号不递进，在各自版本记录里标注）· 交付包 V1.3-20260831 **重打**（原型换 v9、两份 PRD 更新、核心差异汇总补 §9 交付后修订）· 已重新同步文档中心（2026-08-31 18:18，Pages 构建成功、线上 4 个链接全 200）。
- 上一条的待确认项产品已答复：**筛选也撤**（见下一条）。

**2026-08-31｜交付后修订 2：筛选项撤销 + 文档瘦身（原型 v9 → v10）**

- 触发：产品 review 四条——① 原型的筛选项也去掉；② PRD 里那些被我判为「疑似猜码」的 code **大部分是真实的 code**（除 test 开头的），不需要界定输入的真假；③ 迭代 PRD 去掉「三、数据记录要求」；④ 整个迭代 PRD 保留核心改动。
- 改动：
  - **原型 v10**：撤销弃单列表新增的 4 个筛选项，筛选区回到原有 3 项。**至此列表页与 v7 完全一致，v1.3 对本页的唯一改动是右上角多一个「导出」按钮**。顺带删掉 5 个已无引用的枚举常量（枚举定义以 PRD/ER 为准，原型不承载字典）。
  - **迭代 PRD**：删除「三、数据记录要求」整章（字段清单以 ER v9.2 为准，不再重复罗列）；背景章删除码值分布表与真伪判读；全文按核心改动精简 **596 → 463 行**，章节号顺延为一~六。
  - **完整 PRD**：同步撤筛选项、改口径、UI 关联指向原型 v10。
  - **两份 PRD 统一口径**：「优惠码码值——**不校验、不判定码的真伪，买家提交什么就记什么**」。
- ⚠️ **我的事实错误已更正**：原 PRD 依据里写「过半优惠码输入是买家在猜码」，那是**我从码值形态（纯数字、人名）自行推断的**，产品指出这些大多是真实配置的优惠码。已删除该判读，改写为不带真伪推断的表述——「36% 填过优惠码，只记生效码就看不到填了没用上的那批，而失败原因直接指向规则配置是否合理」。**记录颗粒度的决策本身不变**（仍是每次尝试都记），变的是论证依据。
- ⚠️ **数据采集与导出口径两轮修订都未动**：`checkout_discount_attempt` 字段一个不少，导出仍 41 列，ER v9.2 不动。
- 产物更新：原型 **v10**（+ v10 自检报告）· 两份 PRD 原地修订 · 交付包 V1.3 **第三次重打**（原型换 v10、两份 PRD 更新、核心差异汇总 §9 补第二轮）· 已重新同步文档中心（2026-08-31 18:31，Pages 构建成功、线上 4 个链接全 200）。

**2026-08-31｜交付后修订 3：补两类被漏掉的场景 + 修文档中心时间戳失效（原型 v10 → v11）**

- 触发：产品 review 三条——① 文档中心迭代 PRD 的时间还停在 16:07，18 点多明明改过；② 核实弃单时间线规则，**code 自动填写的场景也要记录**；③ 补**已登录**场景——contact 和收货地址大部分会自动拉取出来，也要记进时间线。

- **① 时间戳失效的根因与修复**：迭代 PRD 那张卡片是**手工写进 index.html 的**，而 sync.py 的「更新于」只对它自己管理的 artifacts 条目（changed_items）刷新，手工卡片永远不动 → 被冻在写入时的值。
  - **没有采用全局按 mtime 刷新**：实测全库 143 个带「更新于」的条目中 68 个与文件 mtime 不符，但其中大量是 clone / 批量复制造成的假 mtime（多个不同模块的文件 mtime 都是 `2026-07-08 11:34`、`2026-08-10 20:40`），全局刷新会把更多条目改成**更错的时间**。
  - **修法**：sync.py 的 `static_docs` 增加可选 `index_href`，新增 `refresh_static_doc_stamps()`——**只刷新显式声明了 index_href 的手工卡片**，按仓库内该文件 mtime，其余一概不动。已验证生效（`[更新] index.html 手工卡片时间戳 x1`，线上 16:07 → 19:18）。

- **② 链接自动带入的码（易漏，已补进两份 PRD）**：营销 §2.3.4 规定链接码校验失败对买家**完全静默**（不提示、不回填、页面与「没有这个码」一致）。若订单侧照搬「静默」理解成不记录，**运营排查「投放链接带的码为什么没人用成」时一条都查不到**——而这正是本需求要解决的核心场景之一。已明确三条：自动应用成功 / 买家侧静默失败 / 未达门槛的中性引导，**三种都记**。总原则写成：**一次校验 = 一条记录，判断依据是「有没有发生过一次校验」，不是「买家看没看见」。**

- **③ 已登录买家的自动带出（连带修掉一个指标失真）**：
  - 时间线区分三类事件：**自动带入联系方式 / 自动带入收货地址**（取自登录账户）· 填写邮箱 / 填写收货地址（买家手动）· **修改收货地址**。混记会让运营把系统填的看成买家填的。
  - 🔴 **自动带出不推进结算进度**：进度仍是「进入结算页」，直到买家有实际动作。**原口径（自动带出即算已填）会让每个已登录买家一进页面进度就是「已填收货地址」，哪怕看都没看就关了——「流失环节」对老客群体整体失真**，而老客恰是召回价值最高的一批。
  - 新增字段口径：**联系方式来源 / 收货地址来源**（自动带出 / 买家填写 / 自动带出后被修改）。

- **迭代 PRD 新增「五、待确认项」3 条**（均附推荐方案与不做的后果）：T2 撤销与买家主动移除是否用字段显式区分 · 自动带出算不算「已填」（本文已按「不算」落笔）· 两个来源字段要不要落库（落则 ER 出 v9.3）。

- 产物更新：原型 **v11**（mock 补 4 单已登录自动带出、1 单链接码静默失败、**新增第 7 单「已登录进来什么都没动就走」**——地址满而进度停在「进入结算页」，正是新口径的关键样例）· 两份 PRD 原地修订 · 核心差异汇总补 §3.2b 与第三轮 · 交付包 V1.3 第四次重打 · 已同步文档中心（2026-08-31 19:22，Pages 构建成功，线上时间戳已刷新为 19:18~19:21）。
- ⚠️ 数据采集口径三轮修订都未动 ER v9.2；若待确认项 1、3 拍板要做，需出 ER v9.3。

**2026-08-31｜产品拍板三条口径，落地为原型 v12 + 两份 PRD 修订**

上一条修订留下的三个待确认项，产品已拍板：

| # | 问题 | 拍板 | 随之接受的代价 |
|---|---|---|---|
| 1 | T2 撤销与买家主动移除要不要用字段显式区分 | **不区分**，共用「被移除时间」 | 分辨时需看该码后面有没有紧跟一条「未通过」记录，不够直观 |
| 2 | 自动带出的邮箱与地址算不算「已填」 | **算已填**，与买家手填一视同仁 | **「进度 = 已填收货地址」不再等于「买家填过地址」**——已登录买家一进页面通常就是这个进度；判断买家有没有真动手只能看时间线 |
| 3 | 联系方式 / 收货地址「来源」要不要落库 | **不落库**（产品未直接答，按前两条的简化方向落笔并已告知可推翻），只体现在时间线事件里 | 导出的「收货地址填写情况」不区分自动带出与买家填写，按该列做召回分析时筛不掉「系统带出、买家没动过」的那批 |

- 三条指向同一个取舍：**不为区分来源而扩数据结构，代价由时间线兜底**。**ER v9.2 因此无需再改**（原预留的 v9.3 不用出了）。
- 我原先按「自动带出不推进进度」落笔并给出了失真论证，产品拍板取反 → 已全量改回，并在两份 PRD 的进度章节保留一句**读法提醒**：进度不等于买家动过手，要配合时间线看。**论证删掉了，但这个已知影响留痕，避免以后有人只看进度做召回判断。**
- 产物更新：原型 **v12**（nina 单进度由「进入结算页」改为「已填收货地址」；注释同步「来源不落库」，防止开发照旧注释去建字段）· 两份 PRD 原地修订（迭代 PRD 的「五、待确认项」改为「五、本次拍板的三条口径」，含代价栏）· 核心差异汇总补第四轮 · 交付包 V1.3 第五次重打 · 同步文档中心。

**2026-08-31｜导出补两列：商品小计 + 支付方式（41 → 43 列）**

- 触发：产品问「导出的 41 列和本地 Shopify 表格导出的表头一样吗」→ 拿 `checkouts_export_1 (4).csv`（MD5 `9e2b1a02afe5a489e0d49ade74f5e408`，869 行 / **711 个去重会话** / **71 列**）逐列比对。
- 比对结论：**语义重合 27 项**；我们独有 13 项（结算进度 / 优惠码尝试系列 / 地址填写情况 / 恢复邮件 / 转化订单号等，Shopify 全无）；Shopify 独有 44 项。
- **Shopify 独有字段逐列拉真实数据核实后的处置**（不是看字段名猜的）：
  - **全空 0/711**：Financial Status / Paid at / Cancelled at / Refunded Amount / Receipt Number / Phone / Tags / Risk Level / Notes → 订单才有的字段，弃单永远为空，**不补**
  - **单一值无信息量**：Fulfillment Status 恒 `unfulfilled` · Blocked due to fraud 恒 `no` · Source 恒 `web` · Taxes 恒 `0.00`（本期不算税）→ **不补**
  - **商品行级**：Lineitem price / sku / compare at price / Vendor 等 → 与「一行一会话」的粒度选择冲突，**不补**
  - ✅ **真缺口，已补**：`Subtotal` → **商品小计**（第 8 列）· `Payment Method` → **支付方式**（第 13 列）
- **支付方式为什么值得补**：实测 **272/711（38%）有值**，取值分布 `Airwallex Klarna 220 / Airwallex 47 / PayPal Express Checkout 1 / 组合 4`——**选 Klarna 分期的占八成**，是很强的流失信号；而结算进度本就有「已填支付信息」这一档，说明系统知道买家选了什么，只是没进导出。
- ✅ **已定（2026-08-31）**：`Accepts Marketing`（营销订阅同意，实测 451 yes / 260 no）产品明确**不进弃单导出**。**导出定版 43 列，本模块 V1.3 无遗留待定项。**
- 产物更新：两份 PRD 导出清单 41 → **43 列**（编号已重排、连续性已校验）· 核心差异汇总补第六轮 · 交付包重打。**原型无需改版**——导出弹窗只有范围/状态/时间/格式四项，不列字段，v13 不受影响。

**2026-09-08｜订单管理运营增强：新出迭代 PRD V1.4（待评审，未打交付包）**

运营提 4 组需求（导出加字段 / 详情加展示 / 列表加查询维度 / 详情新窗口打开）。回原始文件核实后，有四处与需求字面不一致，已按核实结果落笔：

- **「导出增加字段」实为首次定义**：PRD v1.3 §4.1.1 只有一句「右上角导出按钮」，原型 v13 的按钮无 onClick、无弹窗，**订单导出从来没有过字段规格**。本次按弃单导出的既定形态定 57 列。
- **导出粒度定「一行一商品」**，判据取自订单域自身的业务粒度（发货按件且支持分批 · 退款按件且 `order_item.refund_status` 是唯一事实源 · 红布林一单一件 · 找货按 HBLitemID），不是迁就运营给的模板表（产品明确指出该模板只覆盖单商品单，不能当唯一依据）。金额重复用「订单级金额只在该订单第 1 行输出」解掉，商品级与订单级都能直接求和。
- **三笔跨模块欠账一并还（10 个新增字段里占 7 个）**：① C 端订单 PRD v1.5 §1.5 早已要求订单落 `brand_snapshot` / `brand_id` / `condition_code` / `snapshot_lang`，订单 ER v9.2 **全模块零命中**；② 营销 PRD v2.4 要求「多码生效时按码维度分别落一条 `order_discount`」，而 `order_discount` 无 code、无商品行归属；③ 本模块 PRD v1.3 详情页要展示承运商，ER 无 `carrier`。
- **退款取数写成三段优先级 + 强制「退款数据来源」列**：设计上 `refund_order` 是唯一资金落点（售后 PRD v1.0「售后域直接对接支付域退款单」），但 2026-09-03 实测系统与设计不符——`/refund-orders` 漏售后退款（含一笔 $333），另有 9/100 单日志写「审批通过」但退款单查不到。**硬性要求：`refund_status = refunded` 却取不到金额时必须输出「记录缺失」，不得静默留空**（留空会被读成"没退款"）。

**顺带订正 PRD v1.3 与线上不一致的 4 处**：筛选项 4 → 6（线上已有卖家ID / 子单 / 取消原因，且「商家」实为文本框不是下拉）· 列表列补 类型/发货状态/退款状态、「付款时间」实为「创建时间」、无「优惠」列 · `order_package` 缺 `carrier` · 物流单号三种叫法（追踪号/快递单号/快递追踪号）统一为「物流单号」。

**已锁定的四条决策**（产品 2026-09-08 拍板）：导出一行一商品 · 「红布林商品ID」= **HBLitemID**（不是 `hbl_info_id`，两者都是纯数字极易混淆，PRD 已写死警示）· 筛选区 ID 类合并为「搜索类型下拉 + 关键词」（11 框降到 8 控件两行）· 退款缺口加「退款数据来源」列。

**仍未闭环**：① 线上详情页是否已有独立地址待产品实测确认（决定需求 4 是「加新标签」还是要先做地址改造）② 退款申请与订单商品行的标识不是同一套编码，能否按件拆退款需研发确认，对不齐则降级为订单级合计 ③ 红布林订单号能否定位到商品行（关联挂父订单层）④ 全局后台时区规范三方案仍标「待确定」，本期按「与列表页一致 + 表头标注时区」实现。

产物：`迭代需求/looply-订单支付-迭代PRD-V1.4-20260908.md`（523 行）。**配套待出：ER v9.3（10 个字段）· 后台原型 v14 · 完整 PRD v1.4**。系统流程图与 C 端产物无需改版（不新增状态、不改流转、不动 C 端）。

**2026-09-09｜产品拍板四条，迭代 PRD V1.4 原地更新**

上一条留下的四个未闭环项，产品已确认三条、退款按件拆待拍板：

| # | 问题 | 结论 | 对 PRD 的影响 |
|---|---|---|---|
| 1 | 线上详情页有没有独立地址 | **已有**，形如 `.../transactions/orders/13113954123885368425`，点击后地址栏回拼订单号 | §2.5.1 从「前置条件」改为「前置条件已满足」，**需求 4 降为纯前端交互改动**，不含地址改造 |
| 2 | 退款能不能按件拆 | **待拍板**（已用「一单两件退一件、$500 该落哪一行」的例子说明，产品未回） | §2.2.4 前置项改为显式待拍板块，两条路各写代价：作为硬性要求 → 研发先对齐两侧标识；本期不做 → 降级为订单第 1 行输出合计并标 `订单级合计` |
| 3 | 红布林订单号能否定位到商品行 | **可以** | 第 29 列按行输出，删除订单级合并的降级方案；依赖表该项标为已解除，并加一句「研发若发现只到父订单层须回报产品、不得自行改成订单级合并」 |
| 4 | 导出时间用哪个时区 | **美东（US/Eastern）**，含夏令时，按美东自然日切分 | §2.2.7 时区行由「与列表页一致」改为写死美东；验收项 7 补「跨夏令时切换日不出现 1 小时偏移」 |

**顺带新增一条风险（第 4 条的连带）**：平台规划美国 + 香港两个 market，导出写死美东后，**香港站上线时与当地自然日会差 12~13 小时**。本期只有美国站在跑，按美东实现；香港站上线前需重新确认是继续统一美东（跨站可直接汇总、香港运营要换算）还是跟随订单所属 market 时区（各站直观、跨站汇总要先对齐），届时与订单缺 `market_id`（待优化清单 #1）一并解决。

产物：`迭代需求/looply-订单支付-迭代PRD-V1.4-20260908.md`（523 → 530 行）。仍待办：退款按件拆拍板 · ER v9.3 · 原型 v14 · 完整 PRD v1.4。

**2026-09-09（第三轮）｜按飞书批注精简重写迭代 PRD V1.4：530 → 339 行**

产品把 PRD 转成飞书文档 review，删了部分内容并留两条批注。

| 批注 | 处置 |
|---|---|
| §1.4 三笔欠账处：「这些不会影响订单设计的核心结构，对这些细节不介入 ER 图的设计，技术来保证即可。可以直接删掉」 | 删除「三笔欠账」与「新增 10 个字段清单」两节；**PRD 不再定义数据结构**。快照口径（品牌会改名 / HBLitemID 可编辑 / 商品会下架，故必须取下单冻结值）保留为 §二 的三行业务规则，字段怎么承载交技术方案 |
| §2.2「导出（首次定义）」处：「非首次定义，线上 ops 系统已经实现一个版本了」 | **我的错，且是重复犯的**。见下 |

**根因：把「产物里没有」当成「系统没有」（第三次栽，前两次是 2026-07-28 营销活动）**

- **症状**：据订单 PRD v1.3 只有一句「右上角导出按钮」+ 原型 v13 的导出按钮无 onClick 无弹窗，写成「订单导出**首次定义**」「**至今没有**字段定义」。
- **同类回扫（第一层）**：同一份 PRD 内同型错误共 **4 处**——另 3 处是「ER v9.2 全模块零命中 `brand_snapshot`」「`order_discount` 无 code」「ER 只有 `tracking_no` 无 `carrier`」，全部只能证明 ER 没画、推不出系统没落。**这 3 处正是「三笔欠账」+「新增 10 个字段」两整节的立论依据，前提整体是错的**——与产品「不介入 ER 设计」的批注恰好指向同一处该删的内容。
- **机制（不是"疏忽"）**：记忆规则 `verify-artifact-vs-system` 早就有，但触发条件写的是「**跨模块**核实上游能力时」，查自己模块的产物时不触发。而"这是我自己模块的 PRD，我很清楚"恰恰是最不设防的时候。**当时手上有线上截图、有 ops 接口实测记忆，核实成本接近于零，仍然没核。**
- **固化（第二、三层）**：① 记忆 `verify-artifact-vs-system` 触发范围由「跨模块查上游」扩到「**任何产物，含本模块自己的**」，补高危措辞清单（首次定义 / 至今没有 / 从来没做过 / 全模块零命中 / 尚未实现）；② `issue-doc-to-delivery` 步骤② 的 grep 增加**新增型推论**分支——原 pattern 只覆盖「产物里没有 → 降级/砍/阻塞」的削减型，对「产物里没有 → 所以要新建」**零命中**。阳性对照：拿出错那版跑新 grep 命中 5 处，改完后只剩版本记录留痕 1 处。

**其余按产品在文档里的删改执行**：删「支付账户」列 · 删 ER `carrier` 那条口径订正 · 删性能约束表（压成一句性能底线）· 删三条风险项 · 删多处论证段落；编号断口（导出列 47、订正第 3 条）已重排。导出列 57 → **55**（另删「下单语言」列，它依附于 ER 字段）。

**仍待确认**：① **线上现有导出的列清单**——没有它无法标注哪些列是新增、哪些已有 ② 退款金额要不要落到具体商品行（已用「一单两件退一件、$500 落哪行」的例子说明，未回）。

产物：`迭代需求/looply-订单支付-迭代PRD-V1.4-20260908.md`（339 行）。配套待出：后台原型 v14 · 完整 PRD v1.4。**ER 不再出 v9.3**（产品定：不介入 ER 设计）。

**2026-09-09（第四轮）｜拿到线上现有导出表头，导出章节落成「保留 9 + 新增 46」**

产品提供线上现有导出表头（9 列）：`类型 / 订单号 / 父订单号 / 买家邮箱 / 订单状态 / 币种 / 订单金额 / 商品数 / 下单时间`。这 9 列全部落在目标态 55 列之内，因此本次是**扩充**而非重做。

| 事项 | 结论 |
|---|---|
| 新增/现有标注 | §3.3 每列标 ✅现有 / ★新增。**保留 9 列 + 新增 46 列** |
| 列名沿用 | **现有列名一律不动，只改一个**：「商品数」→「**订单商品总数**」。改成一行一商品后，「商品数」会被读成"这一行几件"，而它实际是"这一单共几件" |
| 枚举值（新规则） | 截图显示现有导出的订单状态是 `paid` / `cancelled` **英文枚举名**，页面上却是中文。定：**导出一律输出中文展示值，与页面一致** |
| 金额小数位（新规则） | 截图显示现有导出是 4 位小数（`605.0000`）。定：**按币种标准小数位，USD 保留 2 位**——4 位既占宽又容易被误读成有零头 |

**飞书文档同步方式（已确认）**：飞书 MCP **没有「用 markdown 覆盖已有文档」的能力**——`导入` 只能生成新文档（新链接），`创建块` 需手工拼 block JSON 且表格单元格要逐个建（本文 15 张表约 900 格，不可行）。三条路摆给产品后，选定**由产品在飞书里全选删除后粘贴 md**（飞书自动转标题与表格），保持同一链接、格式完整。**本地 md 是正本，飞书是 review 载体。**

产物：`迭代需求/looply-订单支付-迭代PRD-V1.4-20260908.md`（349 行）。仍待办：退款按件拆拍板 · 后台原型 v14 · 完整 PRD v1.4。

**2026-09-09（第五轮）｜按飞书删减定稿迭代 PRD，并出原型 v14 + 完整 PRD v1.4**

产品把第三轮的迭代 PRD 转成飞书文档后人工删减，据此定稿并更新配套产物。

**删减对齐（55 列 → 40 列）**

| 段 | 删掉 |
|---|---|
| A 订单级 | 取消原因、商品小计、运费、税费 |
| B 商品级 | 实物商品ID、单价、数量 |
| C 收货地址 | 拆列的 8 项（收件人 / 电话 / 地址行 1·2 / 城市 / 州省 / 邮编 / 国家），**只留「完整地址」一列** |
| 章节 | §二的三行论证表 · §3.4 取数优先级第 3 路 · §3.6「PII 与留痕」行 · §4.2 论证 · §4.3「与导出的差异 / 留痕」· §5.3 性能与权限 · §七口径订正 · 依赖与风险 / 待确认 / 配套产物 / 版本记录；「查看详情」并入 §五作为 5.3；标题改为「运营后台订单管理优化」 |

**两处删减留下的内部打架，已找产品拍板**：① 列名由「退款到账日期」改为「退款申请日期」但取值说明留空 → 定为**只要申请日期**（退款申请单创建日），§3.4 取数源随之改；② 删了「从操作日志取」这一路但枚举仍留着「仅操作日志」、文案仍写「三处」→ 定为**确实不查日志**，枚举收为三值、文案改「两处」，并写明**那 9 单会全部落到「记录缺失」是有意为之**——让坏数据显形，而不是伪装成「没退款」。

**我自己合并的两处（非产品原话，已标注）**：① 飞书那份基于第三轮，用的是旧列名（子订单号 / 订单类型 / 订单总额），本地第四轮已按产品给的线上表头改为**沿用线上列名**（订单号 / 类型 / 订单金额），合并时保留了后者；② 飞书删了 §3.6 的「PII 与留痕」行，但验收表第 13 条「导出留痕」仍在——判定**留痕需求仍成立**，规则并入 §3.6，验收项保留。

**产物**

- **迭代 PRD V1.4 定稿**：40 列（✅现有 9 / ★新增 31）、13 条验收，零残留
- **后台原型 v14**：筛选区重构（搜索类型下拉 + 关键词，11 控件降到两行）· 订单号列与查看详情改新标签页（用 `<a href target=_blank>`，支持 Cmd/Ctrl+点击、中键、右键菜单四种原生行为）· 导出弹窗 · 详情页优惠码列 / 品牌两行 / 收货与账单地址复制按钮 + 国家改全称
- **完整 PRD v1.4**：§4.1.1 筛选与列表按现状 + 本次改动重写、新增 §4.1.3 订单数据导出（沉淀不随迭代变化的口径）、§4.1.2 详情页三处、UI 关联 v13 → v14、版本记录补 v1.4

**⚠️ `render_sim.py` 又一次静默跳过详情页**（它按「组件名含 Detail 且入参有 record/row」识别，`OrderDetailPage` 入参是 `{go}`），照样打印「✅ 无渲染硬伤」。按记忆里的既定纪律补跑：`check_runtime.py` 8 页全绿（同时证明 Babel 编译通过）+ 两张整页截图目视逐项确认。**check.sh 13 Pass / 1 Warn / 0 Fail**，唯一 Warn 与 v13 基线逐字相同，本次未引入新 UI 语言写法。

**报出一处既有缺陷（未擅自修）**：订单列表「付款时间」列被右侧固定操作列遮挡，1440px 下要横向滚动才看得全。已核实 v13/v14 列定义逐字相同、列宽合计同为 1000px，**是 v13 就有的，不是本次引入**；本次 PRD 未涉及列表列调整，故只报不改。

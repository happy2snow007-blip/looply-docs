# looply 订单支付 · 迭代 PRD V1.3 — 弃单数据补齐与导出

> **迭代主题**：弃单补记「优惠码尝试 / 地址快照 / 结算进度」三类数据，并支持弃单数据导出
> **文档定位**：本文自包含，开发按本文即可 coding，无需回读完整 PRD。完整 PRD v1.3 为归档沉淀版本，规则以本文与完整 PRD 一致为准。
> **基线版本**：订单支付 PRD v1.2（交付开发 V1.2-20260714）· ER v9.1 · 后台原型 v7
> **改动范围**：运营后台「弃单管理」+ 结算会话数据采集。**不涉及 C 端页面新增元素、不涉及订单管理菜单**。

---

## 一、背景与目标

### 1.1 为什么做

弃单管理页现在能看到的只有六列：会话编号、买家、商品数、预估金额、状态、创建时间。运营拿着这几列回答不了任何一个真正想问的问题——

- 这个买家是**卡在填地址**走的，还是**已经填完地址、到支付那一步**才走的？
- 他**填过优惠码吗**？填了为什么没用上？是码写错了，还是没达门槛、地区不支持？
- 想做定向召回，**寄到哪个国家、哪个州**都不知道，没法按地域分批。

而这些数据在**当前在用的 Shopify 后台里是有的**——弃单导出自带 `Discount Code` / `Discount Amount` / 完整 `Shipping Address` / `Billing Address` / `Payment Method`。新系统一旦切换，运营现有的弃单分析动作会直接断掉。

**真实数据佐证**（取自 `订单支付/checkouts_export_1 (4).csv`，Shopify 弃单导出，711 个结算会话）：

| 指标 | 会话数 | 占比 |
|---|---|---|
| 填写了买家邮箱 | 711 | 100% |
| **填写了优惠码** | **257** | **36%** |
| 填写了收货地址 | 449 | 63% |
| 填写了账单地址 | 463 | 65% |
| 走到了支付方式环节 | 272 | 38% |

同一份数据里的**优惠码码值分布**，是本次「记录颗粒度」决策的直接依据：

| 码值 | 出现次数 | 判读 |
|---|---|---|
| `save30` | 80 | 像真码 |
| `67` | 80 | **纯数字，疑似买家瞎猜** |
| `777` | 32 | 同上 |
| `45` / `20` / `30` / `123` / `888` / `66` | 40（合计） | 同上 |
| `sarah` / `deal` | 20 | 人名 / 通用词，同上 |
| `LOOPLY30` / `test-xue` / `test1841` | 5 | 真码与测试码 |

**超过一半的优惠码输入是买家在猜码。** 这意味着：只记录「最终生效的那个码」会把最有价值的信号——**买家在期待有优惠、试了却用不了，然后走了**——全部丢掉。所以本次按「每一次尝试都记」设计。

### 1.2 本次做什么

| # | 事项 | 章节 |
|---|---|---|
| 1 | 结算会话补记**买家邮箱**（游客也有） | §2.1.1 |
| 2 | 结算会话补记**收货地址 + 账单地址完整快照**，并给出「填写完整度」判定 | §2.1.2 |
| 3 | 新增**优惠码尝试记录**：每输入一次记一条（码值 / 来源 / 通过与否 / 未通过原因 / 减免金额） | §2.1.3 |
| 4 | 新增**结算进度**：买家最远走到结算页哪一步 | §2.1.4 |
| 5 | 弃单**列表页**加筛选项（**列不变**）| §2.2 |
| 6 | 弃单**详情页**地址区块改为始终展示；优惠码尝试**并入事件时间线**，不单独成模块 | §2.3 |
| 7 | 弃单**数据导出**（一行一会话） | §2.4 |
| 8 | 订正三处已过期 / 打架的口径 | §2.5 |

### 1.3 本次不做什么

| 不做的事 | 说明 |
|---|---|
| **订单管理菜单的导出字段扩充** | 属另一条需求，本期不含。本文任何内容都不改动订单管理页 |
| **改动营销模块的优惠码机制** | 校验链、码位跟随、失败文案全部沿用营销 V1.3，本次**只做记录，不改判定**。边界见 §1.5 |
| **弃单优惠码的补发 / 定向发码** | 「给填了码但没达门槛的买家补一张券」属营销域的召回玩法，本期不做 |
| **按优惠码维度的聚合报表** | 后台不做图表统计，运营导出后线下透视 |
| **C 端页面新增任何元素** | 结算页视觉与交互完全不变，买家侧无感知 |
| **历史数据回填** | 上线前已存在的结算会话没有这些字段，展示为空。见 §4 |

### 1.4 现状盘点：三处「设计上有、数据上空」

动手前核实了 PRD v1.2 / ER v9.1 / 原型 v7，发现**现有产物之间已经打架**，本次一并修掉：

| # | 现状 | 问题 |
|---|---|---|
| 1 | PRD §4.2.1 弃单列表有「买家」这一列，说明写的是「结算时填写的邮箱」 | ER 的结算会话里**只有买家 ID（关联用户表）**，没有邮箱字段。而静默注册是**支付成功后**才发生的——弃单买家绝大多数根本没有用户记录，**这一列取不到值** |
| 2 | PRD §4.2.2 弃单详情画了「收货地址」和「账单地址」两个区块 | ER 的结算会话里只有一个「收货地址 ID」指针，**账单地址连字段都不存在**；且游客边填的地址不会进地址簿。**两个区块目前都是空的** |
| 3 | PRD §1.2 写着「Discount Code / 优惠券 — 后续迭代，结算页不展示优惠码输入框」 | 已被**营销活动模块 V1.3（2026-08-25 交付）**推翻——结算页优惠码输入框、链接自动带码、chip 展示均已上线。**这条口径过期了** |

### 1.5 与营销模块的边界（**开发必读**）

营销 V1.3 明确写了两件"不落库"：

> 「**链接本身不落库**——它是「域名 + `/discount/` + 优惠码」的拼接结果，平台不追踪已投放的链接。**会话里的码位也不落库**——记在浏览器侧。」

**本次不推翻这条。** 两者记的是不同的东西：

| | 营销侧的「码位」 | 本次新增的「优惠码尝试记录」 |
|---|---|---|
| 记的是什么 | 买家点了链接后**跟随他浏览的那个待用码** | 买家**在结算页实际提交校验的每一次输入** |
| 存在哪 | 浏览器侧，会话级，不落库 | 结算会话下，落库 |
| 谁在用 | C 端自动带码逻辑 | 运营后台弃单分析 |
| 本次是否改动 | **否，完全不动** | 新增 |

**一句话给开发**：优惠码怎么带、怎么判、失败提示什么文案，**全部沿用营销 V1.3 §2.3 不变**；本次只是在校验有结果的那一刻，**额外把结果记一条**。校验逻辑本身零改动。

### 1.6 多语言

本次改动全部在**运营后台**（弃单管理列表 / 详情 / 导出），界面仅面向内部运营，本期仅中文，不接入多语言。

优惠码校验的 C 端失败文案由**营销模块**定义和维护（营销 V1.3 §2.3 校验链），本次记录的「未通过原因」是**后台侧的中文枚举**，与 C 端展示文案是两套，不互相引用。

---

## 二、需求详细描述

### 2.1 结算会话数据记录（新增采集）

**功能类型**：机制型

#### 功能描述

买家在结算页填写的信息，随填随存到结算会话，使其在未完成支付（即弃单）时仍可被后台完整查看。

#### 触发条件

买家进入结算页创建结算会话后，在页面上填写 / 修改任一信息时。

#### 记录时机与更新规则（**通用规则，下列各项均适用**）

| 规则 | 说明 |
|---|---|
| **随填随存** | 买家在结算页填写的信息需在填写过程中保存到结算会话，不能等到点击下单才存——**弃单买家永远不会点下单** |
| **覆盖更新** | 同一会话内买家反复修改地址，以**最后一次填写的内容**为准，不保留修改历史（优惠码除外，见 §2.1.3） |
| **只进不退** | 结算进度只记录**最远到达**的环节，买家回退修改不会把进度降回去 |
| **转化后保留** | 会话转化为订单后，这些数据**照常保留在结算会话上**，不因转化而清空——「已转化」的弃单记录同样要能查看当时填了什么 |
| 保存的具体时机与频率 | 由技术方案定，PRD 只要求「买家离开页面时，他已填写的内容必须已被记录」 |

---

#### 2.1.1 买家邮箱与身份

| 记录项 | 说明 |
|---|---|
| **买家邮箱** | 买家在结算页 Contact 区块填写的邮箱。**游客、未登录、已登录三种情况都要记**：已登录用户取账户邮箱，未登录取买家手填的邮箱 |
| **是否注册用户** | 该会话创建时买家是否已有账户。用于运营区分「老客流失」与「新客流失」 |

> **为什么必须补**：弃单召回邮件发给谁、后台列表「买家」这一列显示什么，全靠这个字段。现有数据结构里它不存在（见 §1.4 第 1 条），弃单买家大多是游客，取不到用户记录。

---

#### 2.1.2 收货地址与账单地址快照

**记录内容**：买家填了多少就存多少，**不要求填完整才存**。

| 地址类型 | 记录字段 |
|---|---|
| **收货地址** | 收件人名、收件人姓、收件人全名、收件人电话（含区号）、地址行 1、地址行 2、城市、州/省、邮编、国家 |
| **账单地址** | 同上一套字段（收件人字段对应为账单联系人姓名 / 电话） |
| **账单地址是否同收货地址** | 买家勾选「账单地址与收货地址相同」时记为「是」，此时账单地址各字段不再重复存值 |

> **字段口径与订单表完全一致**（同名同义），这样会话转化为订单后，运营可以直接比对「结算时填的」和「最终下单用的」是否一致。

**填写完整度判定**（收货地址与账单地址各自独立判定）：

| 状态 | 判定条件 |
|---|---|
| **未填写** | 该地址的所有字段均为空 |
| **部分填写** | 有任一字段有值，但必填项未齐 |
| **已填完整** | 必填项全部有值 |

**必填项定义**：收件人姓名、电话、地址行 1、城市、州/省、邮编、国家。地址行 2 为选填，不参与判定。

> 账单地址勾选了「同收货地址」时，账单地址的完整度**跟随收货地址的判定结果**。

> **为什么账单地址必须做**：实测 Shopify 数据里填了账单地址的会话（463）**比填了收货地址的还多**（449）——买家在支付环节填卡时会连带填账单地址。只做收货地址会漏掉这批已经走到很后面的高价值流失。

---

#### 2.1.3 优惠码尝试记录

**核心规则：买家每提交一次优惠码校验，就记一条记录，不管通过与否。** 一个会话可以有多条。

**每条记录包含**：

| 记录项 | 说明 |
|---|---|
| **优惠码码值** | 买家实际提交的字符串，**统一转成大写后存储**（与营销侧存储口径一致）。买家瞎猜的码同样照实记录 |
| **码来源** | 「链接带入」（买家点投放链接进来，码自动应用）/「买家手输」（在优惠码输入框手动输入） |
| **校验结果** | 「已应用」/「未通过」 |
| **未通过原因** | 校验结果为「未通过」时必填，取值见下方枚举表 |
| **本次减免金额** | 校验结果为「已应用」时记录该码实际减免的金额 + 币种；未通过时为 0 |
| **尝试时间** | 该次校验发生的时间 |
| **被移除时间** | 该次「已应用」的码后来被买家点 chip 上的 ✕ 移除的时间。未被移除则为空 |

**未通过原因枚举**（与营销 V1.3 §2.3 校验链逐条对应，**不新增判定逻辑，只是把营销已有的判定结论记下来**）：

| 枚举值 | 含义 | 对应营销校验链 |
|---|---|---|
| 码无效或已失效 | 码不存在、已停用、或所属活动非进行中 | 第 1 项 |
| 地区不支持 | 活动所属市场与买家当前市场不符 | 第 2 项 |
| 商品范围未命中 | 订单内没有任何商品在该码的适用范围内 | 第 3 项 |
| 未达使用门槛 | 满额或满件门槛未达标 | 第 4 项 |
| 总次数已用尽 | 该码的总使用次数已用完 | 第 5 项 |
| 该买家已用过 | 每人限次已用尽 | 第 6 项 |
| 该买家不在适用范围 | 标签未命中 / 邮箱不在指定买家名单内 | 第 7 项 |
| 已有更优惠的折扣 | 商品上已有自动折扣且更优，该码不叠加 | 营销 §2.3 自动折扣互斥 |
| 未产生实际减免 | 校验全部通过，但整单求最优组合后该码不产生任何减免，故未加入已应用集合 | 营销 §2.3 求解后为零 |

> **枚举随营销校验链演进**：营销后续增删校验项时，本枚举需同步。两侧口径必须保持一一对应，**不允许订单侧另立一套判定标准**——本次记录的是营销给出的判定结论，不是重新判一次。

**几个边界情况的处理口径**：

| 场景 | 怎么记 |
|---|---|
| 买家连续输错 3 次再输对 1 次 | 记 **4 条**：3 条「未通过」+ 1 条「已应用」 |
| 买家应用成功后又点 ✕ 移除 | 仍是 **1 条「已应用」**，在该条上补记「被移除时间」，**不新增记录** |
| 移除后刷新页面，链接码自动重新应用 | **新增 1 条「已应用」**（这是一次新的应用行为，营销 V1.3 明确这是预期行为） |
| 链接带入的码 A 被链接 B 覆盖，A 从已应用集合移除 | A 那条记录补记「被移除时间」；B 新增一条记录 |
| 同一个码在同一会话被反复应用 / 移除 | 每次应用各记一条，不去重——运营需要看到买家的犹豫过程 |
| 买家在**买家身份明确前后**得到不同校验结果（营销的 T1/T2 两时点） | 各记一条。T1 通过、T2 因限次被拒的，会是「已应用」+「未通过（该买家已用过）」两条 |

**会话级冗余（用于列表展示与筛选，避免每次都翻明细）**：

| 记录项 | 取值规则 |
|---|---|
| **当前生效优惠码** | 当前处于「已应用且未被移除」状态的码。无则为空 |
| **当前优惠减免金额** | 当前生效码的减免金额合计。无则为 0 |
| **优惠码使用情况** | 「未填写」（无任何尝试记录）/「填写未通过」（有尝试记录但当前无生效码）/「已应用」（当前有生效码） |

---

#### 2.1.4 结算进度（流失环节）

**记录买家在这个会话里最远走到了哪一步**，用于回答「他是卡在哪儿走的」。

| 进度值 | 含义 | 判定时机 |
|---|---|---|
| 进入结算页 | 创建了会话，但没填任何信息 | 会话创建即为此值 |
| 已填邮箱 | Contact 区块的邮箱已填写 | 邮箱字段首次有值 |
| 已填收货地址 | 收货地址达到「已填完整」 | 收货地址完整度首次判定为「已填完整」 |
| 已选配送方式 | Shipping 区块已选定配送方式 | 配送方式首次有值 |
| 已填支付信息 | Payment 区块已选支付方式并开始填写支付信息 | 买家在支付区块选定支付方式 |
| 已提交支付 | 点击了下单按钮，已创建支付单，但付款未成功（含渠道审核中、失败、超时取消） | 支付单创建时 |

**规则**：

- **只进不退**：买家填完地址又回头改邮箱，进度仍是「已填收货地址」，不回落
- **可跳跃**：已登录买家进页面时邮箱和地址都已带出，进度可直接从「进入结算页」跳到「已填收货地址」，中间值不必逐个经过
- **与状态的关系**：结算进度描述**流失位置**，弃单状态（待转化 / 已转化）描述**转化结果**，两者独立。已转化的会话进度停在「已提交支付」

> **为什么这一项值得做**：地址填没填只能二分，而「已提交支付但没付成」和「进页面就走了」是两种完全不同的流失——前者是支付环节出了问题（该找支付渠道排查），后者是流量质量问题。这两类混在一起看，弃单率这个数就没有可行动性。数据来源与地址快照是同一套随填随存机制，一起做的增量成本很低。

---

### 2.2 弃单列表页改动

**功能类型**：页面型（在 PRD v1.2 §4.2.1 基础上增量）

#### 筛选/搜索字段（**新增 4 项**，原有 3 项不变）

| 字段 | 控件类型 | 说明 | 新增 |
|---|---|---|---|
| 买家邮箱 | 文本输入 | 模糊搜索 | 原有 |
| 状态 | 下拉选择 | 全部状态 / 待转化 / 已转化 | 原有 |
| 创建时间 | 日期范围选择 | 起止日期 | 原有 |
| **优惠码** | 文本输入 | 按码值模糊搜索，**命中该会话任意一条尝试记录即算命中**（不只匹配当前生效码） | ✅ |
| **优惠码使用情况** | 下拉选择 | 全部 / 未填写 / 填写未通过 / 已应用 | ✅ |
| **收货地址填写情况** | 下拉选择 | 全部 / 未填写 / 部分填写 / 已填完整 | ✅ |
| **结算进度** | 下拉选择 | 全部 / 六个进度值 | ✅ |

> 优惠码搜索必须能查到「填了但没通过」的码——运营排查「为什么 save30 这个码没人用成」时，查的正是这批失败记录。只匹配生效码会一条都查不到。

#### 列表字段（**本次不改**）

沿用现有 7 列，一列不增不减：会话编号 / 买家 / 商品数 / 预估金额 / 状态 / 创建时间 / 操作。

> **为什么不加列**：优惠码、地址填写情况、流失环节这三项都是**排查用的细节**，放进列表会把表格撑到需要横向滚动，而运营在列表这一层要的是「哪些单该跟」，不是「这单为什么没成」。前者靠筛选定位，后者进详情看。本次新增的筛选项负责定位，详情页与导出负责细节。

#### 工具栏操作

| 操作 | 说明 | 新增 |
|---|---|---|
| 恢复策略设置 | 原有按钮，不变 | 原有 |
| **导出** | 右上角新增，见 §2.4 | ✅ |

#### KPI 统计卡片

**本次不改动**，仍为：今日弃单数 / 弃单率 / 已恢复数 / 恢复率。

---

### 2.3 弃单详情页改动

**功能类型**：页面型（在 PRD v1.2 §4.2.2 基础上增量）

#### 基本信息区（3 列布局，**新增 1 项**）

| 第 1 列 | 第 2 列 | 第 3 列 |
|---|---|---|
| 会话编号 | 状态 | 创建时间 |
| 买家邮箱 | 商品数 | 设备（UA 信息） |
| 注册时间 | 预估金额 | IP 地址（脱敏） |
| **结算进度** | — | — |

> 「注册时间」在买家为游客时显示「未注册」。

#### 收货地址 / 账单地址区块（**展示规则修正**）

| 原规则 | 新规则 |
|---|---|
| 「如用户未填写收货地址，整个区域不显示」 | **区块始终显示**。未填写时展示空状态提示「买家未填写收货地址」；部分填写时展示已填字段、未填字段显示 `—` |

> **为什么改**：本次的目的就是让运营看到「填写情况」。区块直接不显示，运营分不清是**买家没填**还是**系统没数据**——而这两件事的处理动作完全不同。

**账单地址区块补充规则**：买家勾选了「账单地址与收货地址相同」时，区块显示「同收货地址」一行，不重复罗列字段。

#### 事件时间线（**优惠码尝试记录并入本区块，不单独成模块**）

优惠码的每一次尝试都在时间线里出一条事件，**不再另起一张表**。买家在结算页的动作本来就是一条时间序列——填邮箱、填地址、试码、选配送、付款失败、离开——把试码单独抽成表格，运营要在两个地方来回对时间才能还原发生了什么。

在 PRD v1.2 已定义的事件类型基础上补充 4 类：

| 新增事件 | 事件详情格式 | 颜色 |
|---|---|---|
| 填写邮箱 | 买家填写的邮箱 | 普通-灰色 |
| 应用优惠码 | `{码值}（{码来源}）· 已应用，减免 {金额}` | 普通-灰色 |
| 优惠码校验失败 | `{码值}（{码来源}）· 未通过：{未通过原因}` | 警告-橙色 |
| 移除优惠码 | `{码值} · 买家主动移除，本单不再享受该优惠` | 警告-橙色 |

**码来源**取「链接带入」/「买家手输」。三类优惠码事件由 §2.1.3 的尝试记录逐条派生：**一条尝试记录 = 一条时间线事件**；被移除的那条额外派生一条「移除优惠码」事件。

> **数据照记不变**：§2.1.3 的尝试记录该存的字段一个不少（含码来源、未通过原因、减免金额、被移除时间），本条改的只是**后台怎么展示**——从独立表格改为时间线事件。导出仍按 §2.4 输出全部优惠码字段。

#### 其余区块

结算商品列表、恢复邮件记录、页头操作按钮**本次不改动**。

---

### 2.4 弃单数据导出

**功能类型**：流程型

#### 功能描述

运营在弃单列表页导出弃单数据，用于线下做召回分析、地域分布分析、优惠码失效排查。

#### 导出粒度

**一行 = 一个弃单会话**。商品信息合并到一列，不按商品展开。

> **为什么不按商品展开**：Shopify 现有导出是按商品行展开的（711 个会话产生 869 行），导致**会话被重复计数**——运营用它算弃单率、算平均金额时必须先去重，实测极易算错。弃单分析的单位是「会话」不是「商品」，一行一会话可以直接透视。
> 代价：无法直接分析「哪些商品最常被放弃」。该诉求本期由「商品名称」列的文本内容承载，若后续成为高频需求，再增加按商品展开的导出维度。

#### 操作流程

1. 运营在弃单列表页设置好筛选条件
2. 点击右上角「导出」按钮 → 弹出导出配置弹窗
3. 确认导出范围与文件格式 → 点击「开始导出」
4. 导出完成后弹窗切换为成功状态，展示文件名与「完成」按钮

#### 导出配置弹窗

| 字段 | 控件 | 说明 |
|---|---|---|
| 导出范围 | 单选 | 当前筛选结果（N 条）/ 全部数据（N 条）。默认选中「当前筛选结果」 |
| 状态 | 下拉选择 | 全部 / 待转化 / 已转化。默认带入列表当前筛选值 |
| 创建时间 | 日期范围选择 | 默认带入列表当前筛选值 |
| 文件格式 | 单选 | Excel (.xlsx) / CSV (.csv)。默认 Excel |

> 弹窗形态与交互**复用本模块「对账管理」已有的导出弹窗**（PRD v1.2 §4.6），不新造样式。

顶部提示文案：「导出当前筛选条件下的弃单数据，包含买家联系方式、地址、优惠码使用情况等信息，用于线下召回分析」。

#### 导出字段清单

| # | 列名 | 取值说明 |
|---|---|---|
| 1 | 会话编号 | — |
| 2 | 买家邮箱 | 结算时填写的邮箱 |
| 3 | 是否注册用户 | 是 / 否 |
| 4 | 状态 | 待转化 / 已转化 |
| 5 | 结算进度 | 六个进度值的中文 |
| 6 | 商品数 | 件数 |
| 7 | 商品名称 | 会话内全部商品名称，多个用分号 `;` 连接 |
| 8 | 预估总金额 | — |
| 9 | 币种 | ISO 4217 |
| 10 | 运费 | 未选配送方式时为空 |
| 11 | 配送方式 | 未选时为空 |
| 12 | 优惠码使用情况 | 未填写 / 填写未通过 / 已应用 |
| 13 | 当前生效优惠码 | 无则为空 |
| 14 | 优惠减免金额 | 无则为 0 |
| 15 | 优惠码尝试次数 | 该会话的尝试记录总条数 |
| 16 | 最后一次尝试的优惠码 | 无则为空 |
| 17 | 最后一次未通过原因 | 最后一次尝试为「未通过」时填；否则为空 |
| 18 | 全部尝试过的优惠码 | 该会话所有尝试的码值去重后用分号连接，供排查「哪些码在被反复试」 |
| 19 | 收货地址填写情况 | 未填写 / 部分填写 / 已填完整 |
| 20 | 收件人 | — |
| 21 | 收件人电话 | — |
| 22 | 收货地址行 1 | — |
| 23 | 收货地址行 2 | — |
| 24 | 收货城市 | — |
| 25 | 收货州/省 | — |
| 26 | 收货邮编 | — |
| 27 | 收货国家 | — |
| 28 | 账单地址填写情况 | 未填写 / 部分填写 / 已填完整 / 同收货地址 |
| 29 | 账单联系人 | 「同收货地址」时为空 |
| 30 | 账单联系电话 | 同上 |
| 31 | 账单地址行 1 | 同上 |
| 32 | 账单地址行 2 | 同上 |
| 33 | 账单城市 | 同上 |
| 34 | 账单州/省 | 同上 |
| 35 | 账单邮编 | 同上 |
| 36 | 账单国家 | 同上 |
| 37 | 恢复邮件发送次数 | 累计发送次数，未发送为 0 |
| 38 | 最后一次恢复邮件发送时间 | 未发送为空 |
| 39 | 创建时间 | 进入结算页的时间 |
| 40 | 最后活动时间 | 会话最后一次被更新的时间 |
| 41 | 转化订单号 | 状态为「已转化」时填写对应的买家订单号；否则为空 |

**未采集到的字段一律导出为空**，不填「无」「未知」等占位文本——空值在 Excel 里可以直接筛选和计数，占位文本不行。

#### 规则说明

| 规则 | 内容 |
|---|---|
| **导出内容与筛选一致** | 导出结果必须与列表当前筛选条件下看到的数据完全一致，不能出现「列表 100 条、导出 120 条」 |
| **时间口径** | 所有时间字段导出为**平台运营时区**的可读格式（`YYYY-MM-DD HH:mm:ss`），不导出时间戳 |
| **金额口径** | 金额导出为数值，不带货币符号；币种单独成列 |
| **单次导出上限** | 单次导出上限 **50000 条**。超出时不执行导出，提示运营缩小筛选范围（如收窄时间范围）。是否走异步生成、超大数据量如何处理，由技术方案定 |
| **导出操作留痕** | 弃单数据含买家邮箱、电话、地址等个人信息，**每次导出必须记录操作人、操作时间、筛选条件、导出条数**，可供事后审计。这是隐私合规要求，不可省略 |
| **IP 与设备信息不导出** | 详情页展示的 IP（脱敏）与设备信息**不进入导出文件**——它们对召回分析无用，却会扩大个人数据的外流面 |

#### 异常处理

| 异常场景 | 处理方式 |
|---|---|
| 筛选结果为 0 条 | 导出按钮点击后提示「当前条件下没有可导出的数据」，不生成空文件 |
| 导出条数超过上限 | 弹窗内提示「本次筛选结果 {N} 条，超出单次导出上限 50000 条，请缩小时间范围后重试」，不执行导出 |
| 导出过程失败 | 弹窗展示失败状态与失败原因，提供「重试」按钮 |

---

### 2.5 口径订正

以下三处在完整 PRD v1.3 中同步修正：

| # | 位置 | 原内容 | 改为 |
|---|---|---|---|
| 1 | §1.2 不做什么 | 「Discount Code / 优惠券 — 后续迭代，结算页不展示优惠码输入框」 | **删除该行**。结算页优惠码能力已由营销活动模块 V1.3（2026-08-25）交付，本模块结算会话侧对应记录见 §4.2 |
| 2 | §7 版本规划 · 后续迭代 | 「Discount Code / 优惠券 — 结算页增加优惠码输入框，对接促销模块」 | **删除该行**（同上，已交付） |
| 3 | §4.2.2 弃单详情 · 地址区块 | 「如用户未填写收货地址，整个区域不显示」（账单地址同） | **区块始终显示**，未填写时展示空状态提示。理由见 §2.3 |

---

## 三、数据记录要求

本节列出本次需要新增记录的数据项，供开发与 ER 图（待出 v9.2）对照。**表结构与存储方式由技术方案定**，本节只规定「必须能记下什么」。

### 3.1 结算会话（checkout_session）新增字段

| 字段名 | 中文业务名 | 说明 |
|---|---|---|
| `buyer_email` | 买家邮箱 | 结算时填写的邮箱，游客同样记录 |
| `is_registered_user` | 是否注册用户 | 会话创建时买家是否已有账户 |
| `checkout_progress` | 结算进度 | 六值枚举，只进不退，见 §2.1.4 |
| `recipient_first_name` | 收件人名 | 以下 10 项为收货地址快照，字段口径与订单表一致 |
| `recipient_last_name` | 收件人姓 | |
| `recipient_name` | 收件人全名 | 冗余拼接 |
| `recipient_phone` | 收件人电话 | 含区号 |
| `shipping_address_line1` | 收货地址行 1 | |
| `shipping_address_line2` | 收货地址行 2 | 选填，不参与完整度判定 |
| `shipping_city` | 收货城市 | |
| `shipping_state_province` | 收货州/省 | |
| `shipping_postal_code` | 收货邮编 | |
| `shipping_country_code` | 收货国家编码 | |
| `shipping_address_status` | 收货地址填写状态 | 未填写 / 部分填写 / 已填完整 |
| `billing_same_as_shipping` | 账单地址是否同收货地址 | 是 / 否 |
| `billing_contact_name` | 账单联系人 | 以下 9 项为账单地址快照 |
| `billing_phone` | 账单联系电话 | |
| `billing_address_line1` | 账单地址行 1 | |
| `billing_address_line2` | 账单地址行 2 | |
| `billing_city` | 账单城市 | |
| `billing_state_province` | 账单州/省 | |
| `billing_postal_code` | 账单邮编 | |
| `billing_country_code` | 账单国家编码 | |
| `billing_address_status` | 账单地址填写状态 | 未填写 / 部分填写 / 已填完整；`billing_same_as_shipping` 为是时跟随收货地址状态 |
| `applied_discount_code` | 当前生效优惠码 | 会话级冗余，供列表展示与筛选 |
| `discount_amount` | 当前优惠减免金额 | 会话级冗余 |
| `discount_code_usage_status` | 优惠码使用情况 | 未填写 / 填写未通过 / 已应用 |
| `last_activity_at` | 最后活动时间 | 会话最后一次被更新的时间 |

> 原有字段 `address_id`（收货地址 ID）**保留不动**——买家选用地址簿里已存地址时它仍然有值。新增的快照字段与它并存：**快照是事实记录，指针是关联关系**，地址簿里的地址后续被买家改掉时，快照保证弃单记录不失真。

### 3.2 新增：结算优惠码尝试记录（checkout_discount_attempt）

一条记录 = 买家在结算页的一次优惠码校验。

| 字段名 | 中文业务名 | 说明 |
|---|---|---|
| `id` | 主键 | |
| `session_id` | 所属结算会话 | 关联 checkout_session |
| `discount_code` | 优惠码码值 | 统一转成大写后存储 |
| `code_source` | 码来源 | 链接带入 / 买家手输 |
| `attempt_result` | 校验结果 | 已应用 / 未通过 |
| `reject_reason` | 未通过原因 | 九值枚举，见 §2.1.3；结果为「已应用」时为空 |
| `discount_amount` | 本次减免金额 | 未通过时为 0 |
| `currency_code` | 币种 | ISO 4217 |
| `attempted_at` | 尝试时间 | |
| `removed_at` | 被移除时间 | 该条已应用的码被买家移除的时间；未被移除为空 |

### 3.3 需要新增的枚举

| 枚举 | 取值 |
|---|---|
| 结算进度 | 进入结算页 / 已填邮箱 / 已填收货地址 / 已选配送方式 / 已填支付信息 / 已提交支付 |
| 地址填写状态 | 未填写 / 部分填写 / 已填完整 |
| 优惠码使用情况（会话级） | 未填写 / 填写未通过 / 已应用 |
| 优惠码码来源 | 链接带入 / 买家手输 |
| 优惠码校验结果 | 已应用 / 未通过 |
| 优惠码未通过原因 | 码无效或已失效 / 地区不支持 / 商品范围未命中 / 未达使用门槛 / 总次数已用尽 / 该买家已用过 / 该买家不在适用范围 / 已有更优惠的折扣 / 未产生实际减免 |

---

## 四、依赖与风险

### 外部依赖

| 依赖方 | 本次需要它提供什么 |
|---|---|
| **营销活动模块** | 优惠码校验的**判定结果与失败原因标识**。本次记录的九类未通过原因需与营销 V1.3 §2.3 校验链一一对应；营销后续增删校验项时需同步通知订单域更新枚举。**营销侧的判定逻辑本次零改动** |
| **结算页（C 端）** | 买家填写过程中的数据需能被记录到结算会话——这是「随填随存」的前提。**页面视觉与交互不变，买家无感知** |
| **后台操作日志** | 承载导出操作留痕（操作人 / 时间 / 筛选条件 / 条数） |

### 风险项

| 风险 | 说明 | 处理 |
|---|---|---|
| **历史数据无值** | 上线前已存在的结算会话没有新增字段，列表与导出中这些会话的新增列全部为空 | 不做回填。运营在筛选时按「创建时间 ≥ 上线日」使用新字段，PRD 层面接受这段空白 |
| **枚举与营销脱节** | 营销校验链变更但订单侧枚举未同步，会出现「未通过原因」落不到任何已知枚举 | 需有兜底取值（记为「其他」并保留营销侧返回的原始标识），避免记录直接丢失 |
| **个人数据外流面扩大** | 导出文件含邮箱、电话、完整地址，属个人信息 | 已通过「导出留痕」+「IP/设备不导出」两项约束控制。导出文件的分发与留存由运营侧管理规范约束 |
| **随填随存带来的写入压力** | 结算页每次填写都要更新会话 | 属实现层面，由技术方案决定保存时机与合并策略。PRD 只要求结果：买家离开时已填内容必须已记录 |

---

## 五、验收要点

| # | 验收场景 | 预期结果 |
|---|---|---|
| 1 | 游客进结算页填邮箱后直接关页面 | 弃单列表能看到该会话，买家列显示邮箱，流失环节为「已填邮箱」，收货地址为「未填写」 |
| 2 | 买家填了一半地址（只填了收件人和城市）后离开 | 收货地址状态为「部分填写」，详情页地址区块显示已填的两项，其余显示 `—` |
| 3 | 买家连输 3 个错码再输 1 个对码，然后离开 | 详情页时间线出现 4 条优惠码事件：3 条「优惠码校验失败」（各带码来源与未通过原因）+ 1 条「应用优惠码」（带减免金额），与其他事件按时间顺序穿插 |
| 4 | 买家应用成功后点 ✕ 移除，然后离开 | 时间线先出「应用优惠码」再出「移除优惠码」两条事件；会话级「当前生效优惠码」为空，使用情况回落为「填写未通过」 |
| 5 | 买家点投放链接进来，码自动生效后离开 | 时间线出 1 条「应用优惠码」事件，码来源显示「链接带入」 |
| 6 | 买家勾选「账单地址同收货地址」 | 账单地址区块显示「同收货地址」，账单填写状态跟随收货地址 |
| 7 | 用优惠码 `SAVE30` 搜索列表 | 能搜到「填了 SAVE30 但未通过」的会话，不只是应用成功的（**列表不展示码值，命中与否看筛选结果条数，具体填了什么进详情时间线看**）|
| 8 | 筛选「结算进度 = 已提交支付」 | 只返回创建了支付单但未付成功的会话 |
| 9 | 导出当前筛选结果 | 行数与列表条数一致；41 列齐全；未采集字段为空而非占位文本；时间为可读格式 |
| 10 | 导出后查后台操作日志 | 能查到本次导出的操作人、时间、筛选条件、条数 |
| 11 | 筛选结果为 0 时点导出 | 提示无数据，不生成文件 |
| 12 | 会话转化为订单后查看该弃单记录 | 状态为「已转化」，此前记录的地址、优惠码尝试数据**仍然可见**，转化订单号有值 |

---

## 六、影响范围与配套产物

| 产物 | 当前版本 | 本次是否需要更新 | 说明 |
|---|---|---|---|
| **完整 PRD** | v1.2 | ✅ **v1.3** | 本文内容已合并，另含 §2.5 三处口径订正 |
| **实体关系图** | v9.1 | ✅ **已出 v9.2** | 结算会话新增 28 个字段 + 新增「结算优惠码尝试记录」实体及关系。字段清单见 §三 |
| **后台原型** | v7 | ✅ **已出 v9** | 弃单列表新增 4 筛选项 + 导出按钮（**列不变**）；详情页地址区块常显、基本信息加结算进度、优惠码事件并入时间线；新增导出弹窗 |
| **系统流程图** | v2.0 | ❌ 不需要 | 本次不改变结算与支付的流转逻辑 |
| **C 端设计稿（Figma）** | — | ❌ 不需要 | C 端零新增元素 |
| **邮件模板** | — | ❌ 不需要 | 恢复邮件内容与触发逻辑不变 |
| **营销活动 PRD / ER** | v2.4 / v2.3 | ❌ 不需要 | 本次只读取营销的判定结果，不改营销侧任何规则与数据 |

---

## 七、版本记录

| 版本 | 日期 | 修改人 | 修改内容 |
|---|---|---|---|
| V1.3 | 2026-08-31 | 产品 | 弃单补记优惠码尝试 / 地址快照（含账单地址）/ 结算进度三类数据；弃单列表加 4 筛选项；详情地址区块改常显；新增弃单数据导出（41 列，一行一会话）；订正「结算页不展示优惠码输入框」等三处过期口径 |
| V1.3 | 2026-08-31 | 产品 | **【交付后修订】** 产品 review 收敛展示面，**数据采集与导出口径未变**：① 弃单列表**撤销新增的 3 列**（优惠码 / 收货地址 / 流失环节），保持原有 7 列不动——列表负责定位、详情负责细节；② 详情页**取消「优惠码尝试记录」独立表格模块**，尝试记录**并入事件时间线**逐条呈现（含码来源、未通过原因、减免金额、移除）。配套原型 v8 → **v9** |
